# ARST023.fmx

Total extracted strings: 4961

## Procedure-like names

- `ADD_PROC`
- `ATTACH_ITEM_OTHRS_PKG`
- `B4SAVE_PRC`
- `CHECK_DOC_DEL_PRC`
- `CHK_B4SAVE_DTL_PRC`
- `CHK_B4SAVE_MST_PRC`
- `DELETE_PROC`
- `DEL_DET_REC_PRC`
- `ENA_DIS_ITM_PRC`
- `EXIT_PROC`
- `FILL_ALL_LIST_PRC`
- `GEN_PKG`
- `GET_ITM_SER_PRC`
- `IAS_ACTV_PKG`
- `IAS_ATTACH_ITM_PKG`
- `IAS_AUDIT_PKG`
- `IAS_AUD_SYS_PKG`
- `IAS_BRN_PKG`
- `IAS_CC_CODE_PKG`
- `IAS_CHECK_SYS_PKG`
- `IAS_CHK_BRN_UP_DEL_FNC`
- `IAS_CHK_BRN_UP_DEL_FNCIAS20142IAS_CHK_BRN_UP_DEL_FNC`
- `IAS_CSHBNK_PKG`
- `IAS_CST_PKG`
- `IAS_DBS_SYS_PKG`
- `IAS_FETCH_DATA_PKG`
- `IAS_GEN_PKG`
- `IAS_GET_DOC_DEL_FNC`
- `IAS_GET_ENC_PASS_FNC`
- `IAS_GET_ENC_PASS_FNCIAS20142IAS_GET_ENC_PASS_FNC`
- `IAS_GL_LMT_PKG`
- `IAS_INSRNCE_SCR_PKG`
- `IAS_ITM_PKG`
- `IAS_PJ_PKG`
- `IAS_PRMTR_PKG`
- `IAS_SALESMAN_PKG`
- `IAS_TRNS_PKG`
- `IAS_USR_PKG`
- `IAS_VNDR_PKG`
- `IAS_WCODE_PKG`
- `IAS_WEIGHT_SYS_PKG`
- `IAS_WEIGHT_SYS_PKGIAS20142CALC_AREA_SIZE_PRC`
- `IAS_WT_PKG`
- `IMP_F_EXCEL_PRC`
- `INSRT_APPRVD_LVL_PRC`
- `KEY_LISTVAL_PRC`
- `LIST_PROC`
- `LOV_PKG`
- `LOV_TRG`
- `LYSERP_LIB`
- `POST_FORMS_COMMIT_PRC`
- `PRE_FORM_PRC`
- `PRINT_PROC`
- `SAVE_PROC`
- `SET_POS_PRC`
- `SHOW_BAT_COL1_IN_SCREEN`
- `SHOW_BAT_COL2_IN_SCREEN`
- `SHOW_BAT_COL3_IN_SCREEN`
- `SHOW_BAT_COL4_IN_SCREEN`
- `SHOW_BAT_COL5_IN_SCREEN`
- `SRCH_DTL_PKG`
- `SYS_SCREEN`
- `UPDATE_PROC`
- `WHEN_NEW_FORM_INSTANCE_PRC`
- `WHEN_TAB_PAGE_CHANGED_PRC`
- `WHEN_TIMER_EXPIRED_PRC`
- `WIN_API`
- `YSERP_LIB`
- `YSERP_MNU`
- `YS_APPRVD_PKG`
- `YS_EMP_PKG`
- `YS_GEN_PKG`

## Referenced components

- `Excel.exe`
- `Inv_Itm_Trns.Fmx`
- `YSERP_MNU.MMX`

## SQL-like strings

```sql
Create -
```
```sql
Delete -
```
```sql
SELECT NVL(STAND_BY,0) FROM IAS_RT_BILL_MST_RQ WHERE RT_BILL_SER = :b1 AND ROWNUM <= 1"
```
```sql
RollBack
```
```sql
SELECT COUNT(1) FROM WAREHOUSE_DETAILS"SELECT W_CODE FROM WAREHOUSE_DETAILS"
```
```sql
Select Nvl(Up_Cnt,0)+1
```
```sql
Select 1 From Ias_Ret_Bill_Mst
```
```sql
SELECT EXTERNAL_POST FROM IAS_RT_BILL_MST_RQ WHERE RT_BILL_SER = :b1 AND ROWNUM <= 1"
```
```sql
SELECT 1 FROM IAS_RT_BILL_MST_RQ WHERE RT_BILL_SER = :b1 AND ROWNUM <= 1"
```
```sql
SELECT 1 FROM IAS_RT_BILL_DTL WHERE DOC_SER_REF = :b1 AND ROWNUM <= 1"SELECT EXTERNAL_POST,NVL(RT_BILL_PY,0),NVL(APPROVED,0) FROM IAS_RT_BILL_MST_RQ WHERE RT_BILL_SER = :b1 AND ROWNUM <= 1"
```
```sql
ROLLBACK
```
```sql
Select '','None' From dual union Select a_cy ,a_cy From account_curr
```
```sql
Select TO_CHAR(W_CODE)||'- '||DECODE(
```
```sql
=1) OR EXISTS (Select W_CODE From PRIVILEGE_WH
```
```sql
Select to_char(Cc_Code)||'- '||Decode(
```
```sql
Where c_s_m=(Select cc_type From cost_center_types Where affected_by_trans=1)
```
```sql
Exists(Select Cc_Code From privilege_cc
```
```sql
Select To_Char(PRM_CODE)||'-'||Decode(
```
```sql
Select R_code||'- '||Decode(
```
```sql
Select Reprs_Code||'- '||Decode (
```
```sql
Or(Exists (Select 1 From IAS_PRIV_SMAN
```
```sql
Select To_Char(Pj_No)||'- '||Decode(
```
```sql
Exists(Select Pj_No From Ias_Priv_Projects
```
```sql
Select To_Char(Actv_No)||'- '||Decode(
```
```sql
Exists(Select Actv_No From Ias_Priv_Actvty
```
```sql
Select To_Char(Typ_No)||'- '||Decode(
```
```sql
And Exists(Select S_Type From Ias_Priv_Ar
```
```sql
Select Brn_no||'-'||Decode(
```
```sql
Or Exists (select 1 From S_Brn_Usr_Priv
```
```sql
Select Ins_Cmp_No||'-'||Decode(
```
```sql
Select Bnf_No||'-'||Decode(
```
```sql
Select to_Char(Emp_No)||'- '||Decode(
```
```sql
Select measure, To_Char(measure_code)
```
```sql
Exists(Select 1 From Ias_Shw_Doc_Priv
```
```sql
Select 1 From Sales_Charges Where RowNum<=1
```
```sql
SELECT CUR_CODE FROM EX_RATE WHERE L_F = 1"SELECT CUR_CODE FROM EX_RATE WHERE STOCK_CUR = 1"SELECT NVL(USE_PROJECTS,0),NVL(YS_GEN_PKG.CHK_ACTV_SYSTEM(80,:b1),0),NVL(CALC_VAT_AMT_TYPE,1),CONN_ITM_ACT_BY_USR_PRIV,NVL(USE_BILLNO_CONN,1),NVL(USE_BILLNO_RTSALE_PY,0),NVL(USE_MOD_AR,0),NVL(USE_DEL_AR,0),NVL(SC_ITEM_TYPE,0),MUST_RET_SAME_WCODE,NVL(CUST_D_TYPE,1),NVL(DOC_GEN_AR,1),NVL(DATE_GEN_AR,1),CC_AVAIL,NVL(AR_AC_LINK_TYPE,1),NVL(AP_AC_LINK_TYPE,1),1 USE_NEGATIVE_INV,INVOICING_SERIALS,INVOICING_SERIALS_SR,NVL(SHOW_DISC_PER_ITEMS_AR,0),NVL(USE_BATCH_NO,0),NVL(USE_EXPIRE_DATE,0),NVL(DATE_SETTING_AR,0),NVL(USE_SERIALNO,0),NVL(USE_VAT,0),TO_DATE(ACTIVE_VAT_DATE,''DD/MM/RRRR''),NVL(USE_OUT_BILLS,0),1 USE_COSTING,NVL(COSTING_TYPE,1),NVL(AR_WC_TYPE,1),NVL(NO_OF_DECIMAL_AR,2),NVL(AR_CS_TYPE,1),NVL(AR_USE_FREE_QTY,0),NVL(YS_GEN_PKG.CHK_ACTV_SYSTEM(66,:b1),0),NVL(USE_AUDIT_DOC,0),NVL(WTAVG_TYPE,0),NVL(PRICE_TYPE,1),NVL(REQUEST_REFNO_AR,0),NVL(REQUEST_DESC_AR,0),NVL(USE_ITM_ATTACH,0),NVL(DEALING_UNDER_SELLING,0),NVL(IAS_PARA_AR.ALLOW_DOC_DEL_NO,1),NVL(STKCOST_FRACTION,0),NVL(RET_FREE_QTY_PER,0),NVL(USE_WT_SYS,0),DFLT_WT_ARGMNT,NVL(USE_STAND_BY,0),NVL(SR_RT_REASON_MANDTRY,0),NVL(IAS_PARA_AR.SI_DUE_DATE_MANDTRY,0),NVL(ITEM_POSTING_FLAG,1),NVL(SHOW_SUM_QTY_IN_SALES,0),NVL(APPRVD_BRACODE_SAME_BATCHNO,0),NVL(SC_WCODE_TYPE,1),NVL(P_RATE_TYPE,0),NVL(CONN_AC_PJ,0),NVL(IAS_PARA_GEN.USE_ACTVTY,0),NVL(CONN_CC_PJ,0),NVL(CONN_PJ_ACTV,0),NVL(AR_PJ_TYPE,0),NVL(AR_ACTV_TYPE,0),NVL(CONN_ACTIVITY_BY_WCODE_CC_CODE,0),NVL(USE_BALANCED_BRN,0),NVL(USE_SC_PRD,0),NVL(SI_REPCODE_MANDTRY,0),NVL(CHK_LMT_PRICE_MINUS_DISC_SI,0),NVL(CONN_SMAN_ITM_ACTVTY,0),NVL(CONN_CST_MULTI_SMAN,0),NVL(CONN_SMAN_WC_CSH_CC,0),NVL(CALC_SI_DISC_WITHOUT_ITM_DISC,0),NVL(SHW_ADD_FLD_IN_DTL,0),NVL(INPT_UNT_FACTOR_WT,1),NVL(SALES_DUPLICATE_ITEM,1),1 INV_SYS_TYPE,NVL(SHOW_ITM_BARCODE_AR,0),BATCHNO_COL_NO,SIZE_COL1,SIZE_COL2,SIZE_COL3,SIZE_COL4,SIZE_COL5,BATCH_NM_COL1,BATCH_NM_COL2,BATCH_NM_COL3,BATCH_NM_COL4,BATCH_NM_COL5,SHOW_BAT_COL1_IN_SCREEN,SHOW_BAT_COL2_IN_SCREEN,SHOW_BAT_COL3_IN_SCREEN,SHOW_BAT_COL4_IN_SCREEN,SHOW_BAT_COL5_IN_SCREEN,NVL(USE_INSRNCE_SYS,0),NVL(AR_EMP_TYPE,0),NVL(USE_PRICE_INCLUDE_VAT,0),NVL(SHOW_WT_UNT_ARGMNT_IN_TRNS,0) FROM IAS_PARA_AR,IAS_PARA_AP,IAS_PARA_GEN,IAS_PARA_INV,IAS_PARA_GL"SELECT WEIGHTED_PERFIX,WEIGHTED_ITEM_LENGTH,LENGTHED_PERFIX,WEIGHTED_LENGTH,WEIGHTED_BASIC,LENGTHED_LENGTH,LENGTHED_ITEM_LENGTH,LENGTHED_BASIC,REMOVE_WEIGHT_DIGIT FROM IAS_PARA_POS"SELECT NVL(PRINT_DOC,1),NVL(ALLOW_ENTER_ZERO_COST,0),NVL(USER_VIEW_DOC_ENTR,0),NVL(CHECK_RET_PER,0),NVL(CHECK_CST_BLK_LST,0),NVL(ALLOW_PRNTDOC_BFR_AUDIT_REF,0),USER_VIEW_DOC_ENTR,ALLOW_CANCEL_AUDIT_REF,NVL(ALLOW_SHOW_JRNL,0),NVL(FILL_OUTBILLS_ONCE,0),NVL(ALLOW_UPD_DISC_RTBILL,0),NVL(AR_ENTR_PRICE_MANUAL_SR,1),NVL(ALLOW_UPD_QTY_WT,1),NVL(ALLOW_FILL_SERIALNO,1) FROM PRIVILEGE_FIXED WHERE U_ID = :b1"SELECT NVL(VRFY_FLAG,0) FROM PRIVILEGE WHERE FORM_NO = :b1 AND U_ID = :b2 AND ROWNUM <= 1"SELECT NVL(PRINT_FLAG,0),NVL(VWREP_FLAG,0) FROM PRIVILEGE WHERE FORM_NO = :b1 AND U_ID = :b2 AND ROWNUM <= 1"SELECT REP_EXT FROM LANG_DEF WHERE LANG_NO = :b1"SELECT CUR_CODE FROM EX_RATE WHERE STOCK_CUR = 1"SELECT MIN(F_DATE) FROM S_PRD_DTL"SELECT NVL(HEAD_OFFICE,0) FROM IAS_PARA_LGHT"
```
```sql
Select 1 From Sales_Charges
```
```sql
SELECT 1 FROM IAS_RT_BILL_DTL_RQ WHERE RT_BILL_SER = :b1 AND ROWNUM <= 1"
```
```sql
SELECT N
```
```sql
VL(RT_BILL_PY,0),NVL(APPROVED,0) FROM IAS_RT_BILL_MST_RQ WHERE RT_BILL_NO = :b1 AND RT_BILL_SER = :b2 AND ROWNUM <= 1"SELECT REPRS_CODE FROM SALES_MAN WHERE W_CODE = :b1 AND ROWNUM <= 1"
```
```sql
DELETE FROM OTHER_CHARGES_OTHRS WHERE OTHER_CHARGES_OTHRS.SC_NO IN (SELECT SC_NO FROM SALES_CHARGES WHERE NVL(FILL_AUTO,0) = 1 AND NVL(SALES_CHARGES.INV_ITEM,0) = 0 ) AND BILL_TYPE = 136 AND BILL_NO = :b1 AND BILL_SER = :b2 AND NVL(INV_ITEM,0) = 0"INSERT INTO OTHER_CHARGES_OTHRS ( BILL_TYPE,BILL_DOC_TYPE,BILL_NO,BILL_SER,SC_NO,A_CODE,A_CY,AC_RATE,PER,AMT,INV_ITEM,RCRD_NO,DOC_DATE ) SELECT 136,IAS_RT_BILL_DTL_RQ.RT_BILL_DOC_TYPE,IAS_RT_BILL_DTL_RQ.RT_BILL_NO,IAS_RT_BILL_DTL_RQ.RT_BILL_SER,OTHER_CHARGES.SC_NO,OTHER_CHARGES.A_CODE,OTHER_CHARGES.A_CY,OTHER_CHARGES.AC_RATE,PER,(SUM(IAS_RT_BILL_DTL_RQ.I_QTY * IAS_RT_BILL_DTL_RQ.I_PRICE ) * PER ) / 100 ,0,OTHER_CHARGES.RCRD_NO,:b1 FROM OTHER_CHARGES,IAS_RT_BILL_DTL_RQ,SALES_CHARGES WHERE OTHER_CHARGES.SC_NO = SALES_CHARGES.SC_NO AND NVL(SALES_CHARGES.INV_ITEM,0) = 0 AND NVL(SALES_CHARGES.ALLOW_RETURN,0) = 1 AND NVL(SALES_CHARGES.FILL_AUTO,0) = 1 AND IAS_RT_BILL_DTL_RQ.BILL_SER IS NOT NULL AND OTHER_CHARGES.BILL_TYPE = 1 AND NVL(OTHER_CHARGES.PER,0) > 0 AND IAS_RT_BILL_DTL_RQ.BILL_NO = OTHER_CHARGES.BILL_NO AND IAS_RT_BILL_DTL_RQ.BILL_SER = OTHER_CHARGES.BILL_SER GROUP BY IAS_RT_BILL_DTL_RQ.RT_BILL_DOC_TYPE,IAS_RT_BILL_DTL_RQ.RT_BILL_NO,IAS_RT_BILL_DTL_RQ.RT_BILL_SER,OTHER_CHARGES.SC_NO,OTHER_CHARGES.A_CODE,OTHER_CHARGES.A_CY,OTHER_CHARGES.AC_RATE,PER,OTHER_CHARGES.RCRD_NO"
```
```sql
SELECT COUNT(1) FROM S_PRD_DTL WHERE PRD_NO = 1 AND :b1 >= F_DATE"
```
```sql
SELECT SC_NO,SC_NAME,A_CODE,DECODE(AMT_TYPE,1,AMT, NULL ) AMT,DECODE(AMT_TYPE,0,AMT, NULL ) AMT_PER,CALC_TYPE,NVL(SC_TYPE,1) SC_TYPE FROM SALES_CHARGES WHERE NVL(FILL_AUTO,0) = 1 AND NVL(SALES_CHARGES.ALLOW_RETURN,0) = 1 AND NVL(INV_ITEM,0) = 0"SELECT SC_NAME,A_CODE,DECODE(AMT_TYPE,1,AMT, NULL ) AMT,DECODE(AMT_TYPE,0,AMT, NULL ) AMT_PER,CALC_TYPE,NVL(SC_TYPE,1) SC_TYPE FROM SALES_CHARGES WHERE NVL(AMT_TYPE,0) = 0 AND NVL(FILL_AUTO,0) = 1 AND NVL(SALES_CHARGES.ALLOW_RETURN,0) = 1 AND NVL(INV_ITEM,0) = 0 ORDER BY CALC_TYPE"
```
```sql
SELECT COUNT(1) FROM SALES_CHARGES WHERE NVL(INV_ITEM,0) = 1"SELECT OTHER_CHARGES_ITEMS_OTHRS.I_CODE,OTHER_CHARGES_ITEMS_OTHRS.ITM_UNT,IAS_BILL_DTL.I_QTY,IAS_BILL_DTL.I_PRICE,OTHER_CHARGES_ITEMS_OTHRS.P_SIZE,SALES_CHARGES_ITEMS.AMT,OTHER_CHARGES_ITEMS_OTHRS.PER PER_AMT,OTHER_CHARGES_ITEMS_OTHRS.A_CODE,SALES_CHARGES.SC_QTY_TYPE,OTHER_CHARGES_ITEMS_OTHRS.AC_RATE,OTHER_CHARGES_ITEMS_OTHRS.A_CY,OTHER_CHARGES_ITEMS_OTHRS.SC_NO FROM OTHER_CHARGES_ITEMS_OTHRS,IAS_BILL_DTL,SALES_CHARGES,SALES_CHARGES_ITEMS WHERE SALES_CHARGES.SC_NO = SALES_CHARGES_ITEMS.SC_NO AND SALES_CHARGES_ITEMS.I_CODE = IAS_BILL_DTL.I_CODE AND DECODE(:b1,3,SALES_CHARGES_ITEMS.C_CODE,4,SALES_CHARGES_ITEMS.C_CODE,2,SALES_CHARGES_ITEMS.C_CLASS,5,SALES_CHARGES_ITEMS.SI_TYPE,1) = DECODE(:b1,3,'''' || :b3 || '''' ,4,'''' || :b3 || '''' ,2,:b5,5,OTHER_CHARGES_ITEMS_OTHRS.SI_TYPE,1) AND :b6 BETWEEN DECODE(:b7,0,:b6,SALES_CHARGES_ITEMS.F_DATE) AND DECODE(:b7,0,:b6,SALES_CHARGES_ITEMS.T_DATE) AND OTHER_CHARGES_ITEMS_OTHRS.BILL_SER = IAS_BILL_DTL.BILL_SER AND NVL(SALES_CHARGES.INV_ITEM,0) = 1 AND NVL(SALES_CHARGES.ALLOW_RETURN,0) = 1 AND OTHER_CHARGES_ITEMS_OTHRS.BILL_TYPE = 1 AND SALES_CHARGES.SC_NO = OTHER_CHARGES_ITEMS_OTHRS.SC_NO AND SALES_CHARGES_ITEMS.I_CODE = OTHER_CHARGES_ITEMS_OTHRS.I_CODE AND SALES_CHARGES_ITEMS.ITM_UNT = OTHER_CHARGES_ITEMS_OTHRS.ITM_UNT AND DECODE(:b11,2,SALES_CHARGES_ITEMS.W_CODE,1) = DECODE(:b11,2,OTHER_CHARGES_ITEMS_OTHRS.W_CODE,1) AND OTHER_CHARGES_ITEMS_OTHRS.I_CODE = :b13 AND DECODE(:b11,2,OTHER_CHARGES_ITEMS_OTHRS.W_CODE,1) = DECODE(:b11,2,IAS_BILL_DTL.W_CODE,1) AND OTHER_CHARGES_ITEMS_OTHRS.BILL_NO = :b16 AND OTHER_CHARGES_ITEMS_OTHRS.BILL_SER = :b17"
```
```sql
Select 1 From Priv_Acc Where
```
```sql
Select 1
```
```sql
Select Count(1) From Ias_Attach_Mst
```
```sql
Select 1 From Ias_Rt_Bill_Dtl_Rq
```
```sql
INSERT
```
```sql
Select 1 From Ias_Attach_Mst
```
```sql
Select To_Char(Ias_Attach_Dtl.Attch_Desc_No)||' -'||Decode(
```
```sql
And Exists(Select 1 From Ias_Itm_Attach Where I_Code='
```
```sql
Select To_Char(Attch_Desc_No
```
```sql
SELECT I_CODE FROM (SELECT I_CODE,W_CODE,EXPIRE_DATE,BATCH_NO,REC_ATTCH,SUM(NVL(I_QTY,0)) QTY,SUM(NVL(FREE_QTY,0)) FQTY FROM IAS_RT_BILL_DTL_RQ WHERE RT_BILL_SER = :b1 AND NVL(IAS_RT_BILL_DTL_RQ.USE_ATTCH,0) = 1 GROUP BY I_CODE,W_CODE,EXPIRE_DATE,BATCH_NO,REC_ATTCH MINUS SELECT I_CODE,W_CODE,EXPIRE_DATE,BATCH_NO,REC_ATTCH,SUM(NVL(I_QTY,0)) QTY,SUM(NVL(FREE_QTY,0)) FQTY FROM IAS_ITM_ATTACH_OTHRS WHERE DOC_TYPE = 5 AND DOC_SER = :b1 GROUP BY I_CODE,W_CODE,EXPIRE_DATE,BATCH_NO,REC_ATTCH ) WHERE ROWNUM <= 1"SELECT I_CODE FROM (SELECT I_CODE,W_CODE,EXPIRE_DATE,BATCH_NO,REC_ATTCH,SUM(NVL(I_QTY,0)) QTY,SUM(NVL(FREE_QTY,0)) FQTY FROM IAS_ITM_ATTACH_OTHRS WHERE DOC_TYPE = 5 AND DOC_SER = :b1 GROUP BY I_CODE,W_CODE,EXPIRE_DATE,BATCH_NO,REC_ATTCH MINUS SELECT I_CODE,W_CODE,EXPIRE_DATE,BATCH_NO,REC_ATTCH,SUM(NVL(I_QTY,0)) QTY,SUM(NVL(FREE_QTY,0)) FQTY FROM IAS_RT_BILL_DTL_RQ WHERE RT_BILL_SER = :b1 AND NVL(IAS_RT_BILL_DTL_RQ.USE_ATTCH,0) = 1 GROUP BY I_CODE,W_CODE,EXPIRE_DATE,BATCH_NO,REC_ATTCH ) WHERE ROWNUM <= 1"SELECT I_CODE,ITM_UNT,P_SIZE,ATTCH_NO1,ATTCH_DESC_NO1,ATTCH_NO2,ATTCH_DESC_NO2,ATTCH_NO3,ATTCH_DESC_NO3,ATTCH_NO4,ATTCH_DESC_NO4,ATTCH_NO5,ATTCH_DESC_NO5,FLEX_NO,ATTCH_NOTE,EXPIRE_DATE,BATCH_NO,W_CODE,I_QTY,FREE_QTY,FLEX_FIELD,C_CODE,RCRD_NO FROM IAS_ITM_ATTACH_OTHRS WHERE DOC_TYPE = 4 AND DOC_SER = :b1 AND REC_ATTCH = :b2 ORDER BY RCRD_NO"SELECT DECODE(:b1,1,NVL(ATTCH_A_NAME,ATTCH_E_NAME),NVL(ATTCH_E_NAME,ATTCH_A_NAME)) ATTCH_NAME FROM IAS_ATTACH_MST WHERE ATTCH_NO = :b2"SELECT RT_BILL_SER,REC_ATTCH,I_PRICE,DIS_AMT,VAT_AMT,STK_COST,CC_CODE FROM IAS_RT_BILL_DTL_RQ WHERE RT_BILL_SER = :b1 AND USE_ATTCH = 1"UPDATE IAS_ITM_ATTACH_OTHRS SET CC_CODE=:b1 WHERE DOC_TYPE = 136 AND DOC_SER = :b2 AND REC_ATTCH = :b3"SELECT IAS_RCRD_ATTCH.NEXTVAL FROM DUAL"SELECT FLEX_NO,FLEX_FIELD,ATTCH_DESC_NO1,ATTCH_DESC_NO2,ATTCH_DESC_NO3,ATTCH_DESC_NO4,ATTCH_DESC_NO5 FROM IAS_ITM_ATTACH WHERE FLEX_NO = :b1 AND ROWNUM <= 1"SELECT FLEX_NO,FLEX_FIELD,ATTCH_NO1,ATTCH_NO2,ATTCH_NO3,ATTCH_NO4,ATTCH_NO5 FROM IAS_ITM_ATTACH WHERE I_CODE = :b1 AND NVL(ATTCH_DESC_NO1,0) = NVL(:b2,0) AND NVL(ATTCH_DESC_NO2,0) = NVL(:b3,0) AND NVL(ATTCH_DESC_NO3,0) = NVL(:b4,0) AND NVL(ATTCH_DESC_NO4,0) = NVL(:b5,0) AND NVL(ATTCH_DESC_NO5,0) = NVL(:b6,0) AND ROWNUM <= 1"SELECT DECODE(:b1,1,NVL(ATTCH_A_NAME,ATTCH_E_NAME),NVL(ATTCH_E_NAME,ATTCH_A_NAME)) ATTCH_NAME FROM IAS_ATTACH_MST WHERE ATTCH_NO = :b2"
```
```sql
SELECT COUNT(1) FROM SALES_CHARGES WHERE NVL(INV_ITEM,0) = 1 AND NVL(ALLOW_RETURN,0) = 1"DELETE FROM OTHER_CHARGES_ITEMS_OTHRS WHERE BILL_TYPE = 136 AND BILL_NO = :b1 AND BILL_SER = :b2"SELECT OTHER_CHARGES_ITEMS.I_CODE,OTHER_CHARGES_ITEMS.ITM_UNT,IAS_RT_BILL_DTL_RQ.POST_CODE,IAS_RT_BILL_DTL_RQ.I_CODE RT_I_CODE,IAS_RT_BILL_DTL_RQ.ITM_UNT RT_ITM_UNT,NVL(IAS_RT_BILL_DTL_RQ.I_QTY,0) I_QTY,NVL(IAS_RT_BILL_DTL_RQ.FREE_QTY,0) FREE_QTY,NVL(IAS_RT_BILL_DTL_RQ.I_PRICE,0) I_PRICE,IAS_RT_BILL_DTL_RQ.W_CODE,IAS_RT_BILL_DTL_RQ.CC_CODE,IAS_RT_BILL_DTL_RQ.P_SIZE,OTHER_CHARGES_ITEMS.P_SIZE OTHR_PSIZE,OTHER_CHARGES_ITEMS.AMT,OTHER_CHARGES_ITEMS.UNIT_AMT,NVL(OTHER_CHARGES_ITEMS.PER,0) PER_AMT,IAS_RT_BILL_DTL_RQ.RCRD_NO,IAS_RT_BILL_DTL_RQ.SI_RCRD_NO,IAS_RT_BILL_DTL_RQ.BILL_SER,OTHER_CHARGES_ITEMS.A_CODE,OTHER_CHARGES_ITEMS.AC_RATE,OTHER_CHARGES_ITEMS.A_CY,OTHER_CHARGES_ITEMS.SC_NO,SALES_CHARGES.SC_QTY_TYPE FROM OTHER_CHARGES_ITEMS,IAS_RT_BILL_DTL_RQ,SALES_CHARGES,SALES_CHARGES_ITEMS WHERE SALES_CHARGES.SC_NO = SALES_CHARGES_ITEMS.SC_NO AND SALES_CHARGES_ITEMS.I_CODE = IAS_RT_BILL_DTL_RQ.I_CODE AND DECODE(:b1,2,SALES_CHARGES_ITEMS.W_CODE,IAS_RT_BILL_DTL_RQ.W_CODE) = IAS_RT_BILL_DTL_RQ.W_CODE AND DECODE(:b2,3,SALES_CHARGES_ITEMS.C_CODE,4,SALES_CHARGES_ITEMS.C_CODE,2,SALES_CHARGES_ITEMS.C_CLASS,5,SALES_CHARGES_ITEMS.SI_TYPE,1) = DECODE(:b2,3,'''' || :b4 || '''' ,4,'''' || :b4 || '''' ,2,:b6,5,OTHER_CHARGES_ITEMS.SI_TYPE,1) AND :b7 BETWEEN DECODE(:b8,0,:b7,SALES_CHARGES_ITEMS.F_DATE) AND DECODE(:b8,0,:b7,SALES_CHARGES_ITEMS.T_DATE) AND IAS_RT_BILL_DTL_RQ.RT_BILL_SER = :b12 AND NVL(SALES_CHARGES.INV_ITEM,0) = 1 AND NVL(SALES_CHARGES.ALLOW_RETURN,0) = 1 AND OTHER_CHARGES_ITEMS.BILL_TYPE = 1 AND SALES_CHARGES.SC_NO = OTHER_CHARGES_ITEMS.SC_NO AND SALES_CHARGES_ITEMS.I_CODE = DECODE(IAS_RT_BILL_DTL_RQ.P_SIZE,OTHER_CHARGES_ITEMS.P_SIZE,OTHER_CHARGES_ITEMS.I_CODE,IAS_RT_BILL_DTL_RQ.I_CODE) AND SALES_CHARGES_ITEMS.ITM_UNT = DECODE(IAS_RT_BILL_DTL_RQ.P_SIZE,OTHER_CHARGES_ITEMS.P_SIZE,OTHER_CHARGES_ITEMS.ITM_UNT,IAS_RT_BILL_DTL_RQ.ITM_UNT) AND DECODE(:b1,2,IAS_RT_BILL_DTL_RQ.W_CODE,1) = DECODE(:b1,2,DECODE(NVL(:b15,0),1,OTHER_CHARGES_ITEMS.W_CODE,SALES_CHARGES_ITEMS.W_CODE),1) AND SALES_CHARGES_ITEMS.SC_NO = OTHER_CHARGES_ITEMS.SC_NO AND IAS_RT_BILL_DTL_RQ.I_CODE = OTHER_CHARGES_ITEMS.I_CODE AND IAS_RT_BILL_DTL_RQ.SI_RCRD_NO = OTHER_CHARGES_ITEMS.RCRD_NO AND IAS_RT_BILL_DTL_RQ.BILL_NO = OTHER_CHARGES_ITEMS.BILL_NO AND IAS_RT_BILL_DTL_RQ.BILL_SER = OTHER_CHARGES_ITEMS.BILL_SER"SELECT (:b1 / DECODE(:b2,2,(NVL(I_QTY,0) + NVL(FREE_QTY,0) ),NVL(I_QTY,0)) ) FROM IAS_BILL_DTL WHERE BILL_SER = :b3 AND I_CODE = :b4 AND RCRD_NO = :b5 AND ROWNUM <= 1"INSERT INTO OTHER_CHARGES_ITEMS_OTHRS ( BILL_TYPE,BILL_DOC_TYPE,BILL_NO,BILL_SER,SC_NO,A_CODE,DOC_DATE,A_CY,AC_RATE,PER,AMT,UNIT_AMT,RCRD_NO,I_CODE,ITM_UNT,POST_CODE,P_SIZE,W_CODE,CC_CODE ) VALUES ( 136,:b1,:b2,:b3,:b4,:b5,:b6,:b7,:b8,:b9,ROUND((:b10 * DECODE(:b11,2,(NVL(:b12,0) + NVL(:b13,0) ),NVL(:b12,0)) ),:b15),ROUND(DECODE(:b16,:b17,:b18,(:b18 / :b17 )),:b15),:b22,DECODE(:b16,:b17,:b25,:b26),DECODE(:b16,:b17,:b29,:b30),:b31,:b16,:b33,:b34 )"DELETE FROM OTHER_CHARGES_OTHRS WHERE BILL_TYPE = 136 AND BILL_NO = :b1 AND BILL_SER = :b2 AND NVL(INV_ITEM,0) = 1"SELECT BILL_TYPE,BILL_DOC_TYPE,BILL_NO,BILL_SER,SC_NO,A_CODE,A_CY,AC_RATE,ROUND(NVL(SUM(AMT),0),:b1) AMT,BRN_NO,BRN_YEAR,CMP_NO,BRN_USR FROM OTHER_CHARGES_ITEMS_OTHRS WHERE BILL_TYPE = 136 AND BILL_NO = :b2 AND BILL_SER = :b3 GROUP BY BILL_TYPE,BILL_DOC_TYPE,BILL_NO,BILL_SER,SC_NO,A_CODE,A_CY,AC_RATE,BRN_NO,BRN_YEAR,CMP_NO,BRN_USR"INSERT INTO OTHER_CHARGES_OTHRS ( BILL_TYPE,BILL_DOC_TYPE,BILL_NO,BILL_SER,SC_NO,A_CODE,DOC_DATE,A_CY,AC_RATE,AMT,RCRD_NO,INV_ITEM,BRN_NO,BRN_YEAR,CMP_NO,BRN_USR ) VALUES ( 136,:b1,:b2,:b3,:b4,:b5,:b6,:b7,:b8,:b9, NULL ,1,:b10,:b11,:b12,:b13 )"
```
```sql
SELECT SC_NO,SC_NAME,A_CODE,CALC_TYPE,NVL(SC_TYPE,1) SC_TYPE,DECODE(AMT_TYPE,1,DECODE(A_CY,:b1,AMT,AMT * AC_RATE * 1 / :b2 ), NULL ) AMT,DECODE(AMT_TYPE,0,AMT, NULL ) AMT_PER FROM SALES_CHARGES WHERE NVL(FILL_AUTO,0) = 1 AND NVL(INV_ITEM,0) = 0 AND NVL(SC_RET_WITHOUT_BILLNO,0) = 1 ORDER BY SC_NO"SELECT SC_NAME,A_CODE,DECODE(AMT_TYPE,1,AMT, NULL ) AMT,DECODE(AMT_TYPE,0,AMT, NULL ) AMT_PER,CALC_TYPE,NVL(SC_TYPE,1) SC_TYPE FROM SALES_CHARGES WHERE NVL(AMT_TYPE,0) = 0 ORDER BY CALC_TYPE"
```
```sql
SELECT NVL(H_PRICE,2) FROM PRIVILEGE_FIXED WHERE U_ID = :b1"
```
```sql
Select (
```
```sql
SELECT COUNT(1) FROM SALES_CHARGES WHERE NVL(INV_ITEM,0) = 1 AND NVL(ALLOW_RETURN,0) = 1"DELETE FROM OTHER_CHARGES_ITEMS_OTHRS WHERE BILL_TYPE = 136 AND BILL_NO = :b1 AND BILL_SER = :b2"SELECT IAS_V_OTHER_CHARGES_ITEMS_PY.I_CODE,IAS_V_OTHER_CHARGES_ITEMS_PY.ITM_UNT,IAS_RT_BILL_DTL_RQ.POST_CODE,IAS_RT_BILL_DTL_RQ.I_CODE RT_I_CODE,IAS_RT_BILL_DTL_RQ.ITM_UNT RT_ITM_UNT,NVL(IAS_RT_BILL_DTL_RQ.I_QTY,0) I_QTY,NVL(IAS_RT_BILL_DTL_RQ.FREE_QTY,0) FREE_QTY,NVL(IAS_RT_BILL_DTL_RQ.I_PRICE,0) I_PRICE,IAS_RT_BILL_DTL_RQ.W_CODE,IAS_RT_BILL_DTL_RQ.CC_CODE,IAS_RT_BILL_DTL_RQ.P_SIZE,IAS_V_OTHER_CHARGES_ITEMS_PY.P_SIZE OTHR_PSIZE,IAS_V_OTHER_CHARGES_ITEMS_PY.AMT,IAS_V_OTHER_CHARGES_ITEMS_PY.UNIT_AMT,NVL(IAS_V_OTHER_CHARGES_ITEMS_PY.PER,0) PER_AMT,IAS_RT_BILL_DTL_RQ.RCRD_NO,IAS_RT_BILL_DTL_RQ.SI_RCRD_NO,IAS_RT_BILL_DTL_RQ.BILL_SER,IAS_V_OTHER_CHARGES_ITEMS_PY.A_CODE,IAS_V_OTHER_CHARGES_ITEMS_PY.AC_RATE,IAS_V_OTHER_CHARGES_ITEMS_PY.A_CY,IAS_V_OTHER_CHARGES_ITEMS_PY.SC_NO,SALES_CHARGES.SC_QTY_TYPE FROM IAS_V_OTHER_CHARGES_ITEMS_PY,IAS_RT_BILL_DTL_RQ,SALES_CHARGES,SALES_CHARGES_ITEMS WHERE SALES_CHARGES.SC_NO = SALES_CHARGES_ITEMS.SC_NO AND SALES_CHARGES_ITEMS.I_CODE = IAS_RT_BILL_DTL_RQ.I_CODE AND DECODE(:b1,2,SALES_CHARGES_ITEMS.W_CODE,IAS_RT_BILL_DTL_RQ.W_CODE) = IAS_RT_BILL_DTL_RQ.W_CODE AND DECODE(:b2,3,SALES_CHARGES_ITEMS.C_CODE,4,SALES_CHARGES_ITEMS.C_CODE,2,SALES_CHARGES_ITEMS.C_CLASS,5,SALES_CHARGES_ITEMS.SI_TYPE,1) = DECODE(:b2,3,'''' || :b4 || '''' ,4,'''' || :b4 || '''' ,2,:b6,5,IAS_V_OTHER_CHARGES_ITEMS_PY.SI_TYPE,1) AND :b7 BETWEEN DECODE(:b8,0,:b7,SALES_CHARGES_ITEMS.F_DATE) AND DECODE(:b8,0,:b7,SALES_CHARGES_ITEMS.T_DATE) AND IAS_RT_BILL_DTL_RQ.RT_BILL_SER = :b12 AND NVL(SALES_CHARGES.INV_ITEM,0) = 1 AND NVL(SALES_CHARGES.ALLOW_RETURN,0) = 1 AND IAS_V_OTHER_CHARGES_ITEMS_PY.BILL_TYPE = 1 AND SALES_CHARGES.SC_NO = IAS_V_OTHER_CHARGES_ITEMS_PY.SC_NO AND SALES_CHARGES_ITEMS.I_CODE = DECODE(IAS_RT_BILL_DTL_RQ.P_SIZE,IAS_V_OTHER_CHARGES_ITEMS_PY.P_SIZE,IAS_V_OTHER_CHARGES_ITEMS_PY.I_CODE,IAS_RT_BILL_DTL_RQ.I_CODE) AND SALES_CHARGES_ITEMS.ITM_UNT = DECODE(IAS_RT_BILL_DTL_RQ.P_SIZE,IAS_V_OTHER_CHARGES_ITEMS_PY.P_SIZE,IAS_V_OTHER_CHARGES_ITEMS_PY.ITM_UNT,IAS_RT_BILL_DTL_RQ.ITM_UNT) AND DECODE(:b1,2,IAS_RT_BILL_DTL_RQ.W_CODE,1) = DECODE(:b1,2,IAS_V_OTHER_CHARGES_ITEMS_PY.W_CODE,1) AND SALES_CHARGES_ITEMS.SC_NO = IAS_V_OTHER_CHARGES_ITEMS_PY.SC_NO AND IAS_RT_BILL_DTL_RQ.I_CODE = IAS_V_OTHER_CHARGES_ITEMS_PY.I_CODE AND IAS_RT_BILL_DTL_RQ.SI_RCRD_NO = IAS_V_OTHER_CHARGES_ITEMS_PY.RCRD_NO AND IAS_RT_BILL_DTL_RQ.BILL_NO = IAS_V_OTHER_CHARGES_ITEMS_PY.BILL_NO AND IAS_RT_BILL_DTL_RQ.BILL_SER = IAS_V_OTHER_CHARGES_ITEMS_PY.BILL_SER"INSERT INTO OTHER_CHARGES_ITEMS_OTHRS ( BILL_TYPE,BILL_DOC_TYPE,BILL_NO,BILL_SER,SC_NO,A_CODE,DOC_DATE,A_CY,AC_RATE,PER,AMT,UNIT_AMT,RCRD_NO,I_CODE,ITM_UNT,POST_CODE,P_SIZE,W_CODE,CC_CODE ) VALUES ( 136,:b1,:b2,:b3,:b4,:b5,:b6,:b7,:b8,:b9,ROUND((:b10 * DECODE(:b11,2,(NVL(:b12,0) + NVL(:b13,0) ),NVL(:b12,0)) ),:b15),ROUND(DECODE(:b16,:b17,:b18,(:b18 / :b17 )),:b15),:b22,DECODE(:b16,:b17,:b25,:b26),DECODE(:b16,:b17,:b29,:b30),:b31,:b16,:b33,:b34 )"DELETE FROM OTHER_CHARGES_OTHRS WHERE BILL_TYPE = 136 AND BILL_NO = :b1 AND BILL_SER = :b2 AND NVL(INV_ITEM,0) = 1"SELECT BILL_TYPE,BILL_DOC_TYPE,BILL_NO,BILL_SER,SC_NO,A_CODE,A_CY,AC_RATE,ROUND(NVL(SUM(AMT),0),:b1) AMT,BRN_NO,BRN_YEAR,CMP_NO,BRN_USR FROM OTHER_CHARGES_ITEMS_OTHRS WHERE BILL_TYPE = 136 AND BILL_NO = :b2 AND BILL_SER = :b3 GROUP BY BILL_TYPE,BILL_DOC_TYPE,BILL_NO,BILL_SER,SC_NO,A_CODE,A_CY,AC_RATE,BRN_NO,BRN_YEAR,CMP_NO,BRN_USR"INSERT INTO OTHER_CHARGES_OTHRS ( BILL_TYPE,BILL_DOC_TYPE,BILL_NO,BILL_SER,SC_NO,A_CODE,DOC_DATE,A_CY,AC_RATE,AMT,RCRD_NO,INV_ITEM,BRN_NO,BRN_YEAR,CMP_NO,BRN_USR ) VALUES ( 136,:b1,:b2,:b3,:b4,:b5,:b6,:b7,:b8,:b9, NULL ,1,:b10,:b11,:b12,:b13 )"
```
```sql
SELECT RT_BILL_SER,BILL_NO,BILL_SER,I_CODE,NVL(P_SIZE,1) P_SIZE,W_CODE,NVL(SUM(P_QTY + (NVL(FREE_QTY,0) * NVL(P_SIZE,1) ) ),0) P_QTY FROM IAS_RT_BILL_DTL_RQ WHERE RT_BILL_NO = :b1 AND RT_BILL_SER = :b2 GROUP BY RT_BILL_SER,BILL_NO,BILL_SER,I_CODE,P_SIZE,W_CODE"SELECT NVL(SUM(B.P_QTY + (NVL(B.FREE_QTY,0) * NVL(B.P_SIZE,1) ) ),0) FROM IAS_RT_BILL_MST A,IAS_RT_BILL_DTL B WHERE A.RT_BILL_SER = B.RT_BILL_SER AND B.BILL_NO = :b1 AND B.BILL_SER = :b2 AND B.I_CODE = :b3 AND A.P_YEAR IN ( 0,3 )"SELECT NVL(SUM(P_QTY + (NVL(FREE_QTY,0) * NVL(P_SIZE,1) ) ),0) FROM DETAIL_OUT_BILLS WHERE BILL_SER = :b1 AND I_CODE = :b2"SELECT NVL(SUM(P_QTY + (NVL(FREE_QTY,0) * NVL(P_SIZE,1) ) ),0) FROM IAS_BILL_DTL WHERE BILL_NO = :b1 AND BILL_SER = :b2 AND I_CODE = :b3"
```
```sql
Select nvl(sum(D.p_qty),0) From
```
```sql
SELECT NVL(SUM(I_QTY * NVL(P_SIZE,1) ),0) FROM IAS_V_SI_DET_PY WHERE BILL_SER = :b1 AND I_CODE = :b2 AND NVL(RCRD_NO,0) = NVL(:b3,0)"SELECT NVL(SUM(D.P_QTY),0) FROM IAS_RT_BILL_MST M,IAS_RT_BILL_DTL D WHERE M.RT_BILL_SER = D.RT_BILL_SER AND M.P_YEAR = 1 AND D.BILL_SER = :b1 AND D.I_CODE = :b2 AND NVL(D.SI_RCRD_NO,0) = NVL(:b3,0)"SELECT NVL(SUM(P_QTY),0) FROM IAS_RT_BILL_DTL_RQ WHERE RT_BILL_SER = :b1 AND BILL_DOC_TYPE = :b2 AND BILL_NO = :b3 AND BILL_SER = :b4 AND I_CODE = :b5 AND NVL(SI_RCRD_NO,0) = NVL(:b6,0) AND NVL(RCRD_NO,0) = NVL(:b7,0)"SELECT NVL(SUM(I_QTY * NVL(P_SIZE,1) ),0) FROM IAS_V_SI_DET_PY WHERE BILL_DOC_TYPE = :b1 AND BILL_NO = :b2 AND BILL_SER = :b3 AND I_CODE = :b4 AND NVL(RCRD_NO,0) = NVL(:b5,0)"SELECT NVL(SUM(D.P_QTY),0) FROM IAS_RT_BILL_MST M,IAS_RT_BILL_DTL D WHERE M.RT_BILL_SER = D.RT_BILL_SER AND M.P_YEAR = 1 AND D.BILL_SER = :b1 AND M.RT_BILL_SER != :b2 AND D.I_CODE = :b3 AND NVL(D.SI_RCRD_NO,0) = NVL(:b4,0)"SELECT NVL(SUM(P_QTY),0) FROM IAS_RT_BILL_DTL_RQ WHERE RT_BILL_SER = :b1 AND BILL_DOC_TYPE = :b2 AND BILL_NO = :b3 AND BILL_SER = :b4 AND I_CODE = :b5 AND NVL(SI_RCRD_NO,0) = NVL(:b6,0) AND NVL(RCRD_NO,0) != NVL(:b7,0)"
```
```sql
UPDATE IAS_RT_BILL_MST_RQ SET BILL_AMT=(SELECT ROUND(NVL(SUM(I_QTY * I_PRICE ),0),:b1) FROM IAS_RT_BILL_DTL_RQ WHERE RT_BILL_SER = :b2 ),INSRNCE_CSH_AMT=(SELECT ROUND(NVL(SUM(I_QTY * (NVL(INSRNCE_LOAD_AMT,0) + NVL(INSRNCE_ADD_LMT,0) ) ),0),:b1) FROM IAS_RT_BILL_DTL_RQ WHERE RT_BILL_SER = :b2 ),DISC_AMT=ROUND((NVL(DISC_AMT_MST,0) + NVL(DISC_AMT_DTL,0) ),:b5) WHERE RT_BILL_SER = :b2"
```
```sql
SELECT PRM_CODE,
```
```sql
Select nvl(sum(nvl(D.Free_qty,0)*nvl(D.p_size,1)),0) From
```
```sql
SELECT 1 FROM IAS_V_SI_PY WHERE BILL_DOC_TYPE = :b1 AND BILL_NO = :b2 AND ROWNUM <= 1"SELECT 1 FROM IAS_V_SI_DET_PY WHERE BILL_DOC_TYPE = :b1 AND BILL_NO = :b2 AND I_CODE = :b3 AND ROWNUM <= 1"SELECT 1 FROM IAS_V_SI_DET_PY WHERE BILL_DOC_TYPE = :b1 AND BILL_NO = :b2 AND I_CODE = :b3 AND W_CODE = :b4 AND ROWNUM <= 1"SELECT 1 FROM IAS_V_SI_DET_PY WHERE BILL_DOC_TYPE = :b1 AND BILL_NO = :b2 AND I_CODE = :b3 AND CC_CODE = :b4 AND ROWNUM <= 1"SELECT BILL_SER,RCRD_NO,ROUND(((I_PRICE_VAT / P_SIZE ) * NVL(:b1,1) ),:b2),ROUND(((I_PRICE / P_SIZE ) * NVL(:b1,1) ),:b2),((STK_COST / P_SIZE ) * NVL(:b1,1) ),DECODE(NVL(IAS_V_SI_DET_PY.I_PRICE,0),0,0,((NVL(IAS_V_SI_DET_PY.DIS_AMT_DTL,0) + NVL(IAS_V_SI_DET_PY.ADD_DIS_AMT_DTL,0) ) / NVL(:b6,0) ) * 100 ) DIS_PER,((NVL(IAS_V_SI_DET_PY.DIS_AMT_DTL,0) + NVL(IAS_V_SI_DET_PY.ADD_DIS_AMT_DTL,0) ) / NVL(P_SIZE,0) * NVL(:b1,1) ) DIS_AMT_DTL,INSRNCE_LOAD_PER,INSRNCE_LOAD_AMT,INSRNCE_ADD_LMT FROM IAS_V_SI_DET_PY WHERE BILL_DOC_TYPE = :b8 AND BILL_NO = :b9 AND I_CODE = :b10 AND NVL(EXPIRE_DATE,''01/01/1900'') = NVL(TO_CHAR(:b11,''dd/mm/yyyy''),''01/01/1900'') AND NVL(BATCH_NO,''0'') = NVL(:b12,''0'') AND BILL_SER = :b13"SELECT (NVL(I_QTY,0) * P_SIZE ) / :b1 FROM IAS_V_SI_DET_PY WHERE BILL_DOC_TYPE = :b2 AND BILL_NO = :b3 AND I_CODE = :b4 AND RCRD_NO = NVL(:b5,0) AND BILL_SER = NVL(:b6,0)"SELECT BILL_CURRENCY,C_CODE,(NVL(DISC_AMT_MST,0) + NVL(ADD_DISC_AMT_MST,0) ) DISC_AMT_MST,BILL_AMT FROM IAS_V_SI_PY WHERE BILL_DOC_TYPE = :b1 AND BILL_NO = :b2 AND BILL_SER = :b3"SELECT CC_CODE FROM IAS_V_SI_DET_PY WHERE BILL_NO = :b1 AND BILL_SER = :b2 AND I_CODE = :b3 AND RCRD_NO = :b4"SELECT TO_DATE(EXPIRE_DATE,''DD/MM/RRRR'') FROM IAS_V_SI_DET_PY WHERE BILL_DOC_TYPE = :b1 AND BILL_NO = :b2 AND BILL_SER = :b3 AND I_CODE = :b4 AND RCRD_NO = :b5"SELECT BATCH_NO FROM IAS_V_SI_DET_PY WHERE BILL_NO = :b1 AND BILL_SER = :b2 AND I_CODE = :b3 AND RCRD_NO = :b4"SELECT NVL(SUM(I_QTY * NVL(P_SIZE,1) ),0),NVL(SUM(FREE_QTY * NVL(P_SIZE,1) ),0) FROM IAS_V_SI_DET_PY WHERE BILL_DOC_TYPE = :b1 AND BILL_NO = :b2 AND BILL_SER = NVL(:b3,0) AND I_CODE = :b4 AND RCRD_NO = NVL(:b5,0)"SELECT NVL(SUM(D.P_QTY),0),NVL(SUM(D.FREE_QTY * NVL(D.P_SIZE,1) ),0) FROM IAS_RT_BILL_DTL_RQ M,IAS_RT_BILL_DTL_RQ D WHERE M.RT_BILL_SER = D.RT_BILL_SER AND D.BILL_DOC_TYPE = :b1 AND D.BILL_NO = :b2 AND D.BILL_SER = NVL(:b3,0) AND D.I_CODE = :b4 AND D.SI_RCRD_NO = NVL(:b5,0)"SELECT (NVL(DIS_AMT,0) / NVL(P_SIZE,0) * NVL(:b1,1) ) FROM IAS_V_SI_DET_PY WHERE BILL_NO = :b2 AND BILL_SER = NVL(:b3,0) AND I_CODE = :b4 AND RCRD_NO = NVL(:b5,0)"SELECT DECODE(NVL(IAS_V_SI_DET_PY.I_PRICE,0),0,0,((NVL(IAS_V_SI_DET_PY.DIS_AMT_DTL,0) + NVL(IAS_V_SI_DET_PY.ADD_DIS_AMT_DTL,0) ) / NVL(:b1,0) ) * 100 ),(NVL(DIS_AMT,0) / NVL(P_SIZE,0) * NVL(:b2,1) ),((NVL(DIS_AMT_DTL,0) + NVL(ADD_DIS_AMT_DTL,0) ) / NVL(P_SIZE,0) * NVL(:b2,1) ),NVL(ADD_DIS_AMT_DTL,0),VAT_PER FROM IAS_V_SI_DET_PY WHERE BILL_NO = :b4 AND BILL_SER = NVL(:b5,0) AND I_CODE = :b6 AND RCRD_NO = NVL(:b7,0)"SELECT COUNT(1) FROM WAREHOUSE_DETAILS"SELECT W_CODE FROM WAREHOUSE_DETAILS"
```
```sql
SELECT I_CODE FROM (SELECT I_CODE,W_CODE,NVL(EXPIRE_DATE,''01/01/1900''),NVL(BATCH_NO,''0''),RCRD_NO,NVL(I_QTY,0) + NVL(FREE_QTY,0) FROM IAS_RT_BILL_DTL_RQ WHERE BILL_SER = :b1 AND NVL(IAS_RT_BILL_DTL_RQ.USE_SERIALNO,0) = 1 MINUS SELECT I_CODE,W_CODE,NVL(EXPIRE_DATE,''01/01/1900''),NVL(BATCH_NO,''0''),RCRD_NO_DOC,COUNT(1) FROM IAS_ITEM_SERIALNO_OTHRS WHERE DOC_TYPE = 3 AND DOC_SER = :b1 GROUP BY I_CODE,W_CODE,NVL(EXPIRE_DATE,''01/01/1900''),NVL(BATCH_NO,''0''),RCRD_NO_DOC ) WHERE ROWNUM <= 1"SELECT I_CODE FROM (SELECT I_CODE,W_CODE,NVL(EXPIRE_DATE,''01/01/1900''),NVL(BATCH_NO,''0''),RCRD_NO_DOC,COUNT(1) FROM IAS_ITEM_SERIALNO_OTHRS WHERE DOC_TYPE = 3 AND DOC_SER = :b1 GROUP BY I_CODE,W_CODE,NVL(EXPIRE_DATE,''01/01/1900''),NVL(BATCH_NO,''0''),RCRD_NO_DOC MINUS SELECT I_CODE,W_CODE,NVL(EXPIRE_DATE,''01/01/1900''),NVL(BATCH_NO,''0''),RCRD_NO,NVL(I_QTY,0) + NVL(FREE_QTY,0) FROM IAS_RT_BILL_DTL_RQ WHERE BILL_SER = :b1 AND NVL(IAS_RT_BILL_DTL_RQ.USE_SERIALNO,0) = 1 ) WHERE ROWNUM <= 1"
```
```sql
Select 1 From All_Users Where UserName='
```
```sql
Create Or Replace View Ias_V_Si_Py as SELECT Bill_date,
```
```sql
Select 1 From Ias_V_Si_Py Where RowNum<=1
```
```sql
SELECT A_CODE FROM CASH_IN_HAND WHERE CASH_NO = :b1"SELECT A_CODE FROM CASH_AT_BANK WHERE BANK_NO = :b1"SELECT C_A_CODE FROM CUSTOMER WHERE C_CODE = :b1"SELECT A_CODE FROM CASH_IN_HAND WHERE CASH_NO = :b1 AND ROWNUM <= 1"SELECT 1 FROM IAS_CASH_IN_HAND_DTL WHERE CASH_NO = :b1 AND A_CY = :b2 AND A_CODE = :b3 AND ROWNUM <= 1"
```
```sql
Create Or Replace View Ias_V_Si_Det_Py as SELECT i_code,
```
```sql
Where Exists(Select 1 From Ias_V_Si_Py Where Bill_Ser=a.Bill_Ser And RowNum<=1)
```
```sql
And Exists(Select 1 From Ias_Itm_Mst Where I_Code=a.I_Code And Nvl(No_Return_Sale,0)=0 And RowNum<=1)
```
```sql
Create Or Replace View Ias_V_Si_Det_Py_List as SELECT i_code,
```
```sql
Create Or Replace View Ias_V_Other_Charges_Py as SELECT *
```
```sql
And Exists(Select 1 From Ias_V_Si_Py Where Bill_Ser=a.Bill_Ser And RowNum<=1)
```
```sql
Create Or Replace View Ias_V_Other_Charges_Items_Py as SELECT *
```
```sql
Create Or Replace View Ias_V_Item_Serialno_Py as SELECT *
```
```sql
And Exists(Select 1 From Ias_V_Si_Py Where Bill_Ser=a.doc_Ser And RowNum<=1)
```
```sql
Select to_Char(Type_Of_Item)||'- '||Decode(
```
```sql
Select '','None' From dual Union
```
```sql
Select Ex_Rate.Cur_No||'-'||account_curr.a_cy ,account_curr.a_cy From account_curr,Ex_Rate
```
```sql
Select Ex_Rate.Cur_No||'-'||Customer_Curr.a_cy ,Customer_Curr.a_cy From Customer_Curr,Ex_Rate
```
```sql
SELECT COUNT(1) FROM ACCOUNT_CURR WHERE A_CODE = :b1"SELECT A_CY FROM ACCOUNT_CURR WHERE A_CODE = :b1"SELECT COUNT(1) FROM CUSTOMER_CURR WHERE C_CODE = :b1"SELECT A_CY FROM CUSTOMER_CURR WHERE C_CODE = :b1"SELECT DECODE(:b1,1,NVL(C_A_NAME,C_E_NAME),NVL(C_E_NAME,C_A_NAME)) FROM CUSTOMER WHERE C_CODE = :b2"
```
```sql
SELECT CASH_NAME FROM CASH_IN_HAND WHERE CASH_NO = :b1"SELECT DECODE(:b1,1,NVL(A_NAME,A_NAME_ENG),NVL(A_NAME_ENG,A_NAME)) FROM ACCOUNT WHERE A_CODE = :b2"SELECT BANK_NAME FROM CASH_AT_BANK WHERE BANK_NO = :b1"SELECT DECODE(:b1,1,NVL(A_NAME,A_NAME_ENG),NVL(A_NAME_ENG,A_NAME)) FROM ACCOUNT WHERE A_CODE = :b2"SELECT DECODE(:b1,1,NVL(A_NAME,A_NAME_ENG),NVL(A_NAME_ENG,A_NAME)) FROM ACCOUNT WHERE A_CODE = :b2"
```
```sql
SELECT BILL_SER FROM IAS_BILL_MST WHERE BILL_SER_CONN = :b1 AND ROWNUM <= 1"SELECT SUM(FREE_QTY) FROM IAS_BILL_DTL WHERE BILL_SER = :b1 AND I_CODE = :b2 AND NVL(I_QTY,0) = 0"SELECT (((NVL(FREE_QTY,0) + NVL(:b1,0) ) / NVL(I_QTY,0) ) * 100 ) FREE_QTY_PER FROM IAS_BILL_DTL WHERE BILL_SER = :b2 AND RCRD_NO = :b3"
```
```sql
Select 1 From Ias_Itm_Mst
```
```sql
SELECT MAX(RCRD_NO) FROM IAS_RT_BILL_DTL_RQ WHERE RT_BILL_SER = :b1"
```
```sql
SELECT NVL(MAX(TO_NUMBER(RT_BILL_NO)),0) + 1 FROM IAS_RT_BILL_MST_RQ WHERE BRN_NO = :b1"SELECT NVL(MAX(TO_NUMBER(RT_BILL_NO)),0) + 1 FROM IAS_RT_BILL_MST_RQ WHERE RT_BILL_DOC_TYPE = :b1 AND BRN_NO = :b2"SELECT NVL(MAX(TO_NUMBER(RT_BILL_NO)),0) + 1 FROM IAS_RT_BILL_MST_RQ WHERE CC_CODE IN (SELECT CC_CODE FROM COST_CENTERS WHERE NVL(C_SR,0) = (SELECT NVL(C_SR,0) FROM COST_CENTERS WHERE CC_CODE = :b1 ) ) AND BRN_NO = :b2"SELECT NVL(MAX(TO_NUMBER(RT_BILL_NO)),0) + 1 FROM IAS_RT_BILL_MST_RQ WHERE W_CODE IN (SELECT W_CODE FROM WAREHOUSE_DETAILS WHERE NVL(W_SER,0) = (SELECT NVL(W_SER,0) FROM WAREHOUSE_DETAILS WHERE W_CODE = :b1 ) ) AND BRN_NO = :b2"SELECT NVL(MAX(TO_NUMBER(RT_BILL_NO)),0) + 1 FROM IAS_RT_BILL_MST_RQ WHERE RT_BILL_DOC_TYPE = :b1 AND W_CODE = :b2 AND BRN_NO = :b3"SELECT NVL(MAX(TO_NUMBER(RT_BILL_NO)),0) + 1 FROM IAS_RT_BILL_MST_RQ WHERE RT_BILL_DOC_TYPE = :b1 AND CC_CODE = :b2 AND BRN_NO = :b3"SELECT NVL(MAX(TO_NUMBER(RT_BILL_NO)),0) + 1 FROM IAS_RT_BILL_MST_RQ WHERE SR_TYPE = :b1 AND BRN_NO = :b2"SELECT NVL(MAX(TO_NUMBER(RT_BILL_NO)),0) + 1 FROM IAS_RT_BILL_MST_RQ WHERE SR_TYPE = :b1 AND W_CODE = :b2 AND BRN_NO = :b3"SELECT NVL(MAX(TO_NUMBER(RT_BILL_NO)),0) + 1 FROM IAS_RT_BILL_MST_RQ WHERE SR_TYPE = :b1 AND W_CODE = :b2 AND RT_BILL_DOC_TYPE = :b3 AND BRN_NO = :b4"
```
```sql
SELECT DECODE(:b1,1,NVL(DESC_A_NAME,DESC_E_NAME),NVL(DESC_E_NAME,DESC_A_NAME)) FROM IAS_ITEM_SERIAL_DESC WHERE DESC_NO = :b2 AND ROWNUM <= 1"
```
```sql
SELECT LEV_NO FROM CUSTOMER_CURR WHERE C_CODE = :b1 AND A_CY = :b2"
```
```sql
Select Excel(.Xls) File
```
```sql
SELECT 1 FROM IAS_ITM_MST WHERE I_CODE = LTRIM(RTRIM(UPPER(:b1))) AND ROWNUM <= 1"SELECT 1 FROM MEASUREMENT WHERE MEASURE_CODE = :b1 AND ROWNUM <= 1"SELECT USE_SERIALNO,USE_BATCH_NO,USE_EXP_DATE FROM IAS_ITM_MST WHERE I_CODE = LTRIM(RTRIM(UPPER(:b1)))"
```
```sql
SELECT 1 FROM IAS_CST_SMAN WHERE C_CODE = :b1 AND REP_CODE = :b2 AND ROWNUM <= 1"SELECT REPRS_CODE FROM SALES_MAN WHERE W_CODE = :b1 AND ROWNUM <= 1"SELECT W_CODE,CASH_NO,CC_CODE,PJ_NO,ACTV_NO FROM SALES_MAN WHERE REPRS_CODE = :b1 AND ROWNUM <= 1"SELECT 1 FROM WAREHOUSE_DETAILS A,PRIVILEGE_WH B WHERE B.U_ID = :b1 AND A.W_CODE = B.W_CODE AND A.W_CODE = :b2 AND NVL(B.ADD_FLAG,0) = 1 AND NVL(INACTIVE,0) = 0 AND ROWNUM <= 1"SELECT 1 FROM PRIV_CASH WHERE U_ID = :b1 AND CASH_NO = :b2 AND NVL(ADD_FLAG,0) = 1 AND CASH_TYPE = 1 AND ROWNUM <= 1"SELECT A_CODE,CASH_NAME FROM CASH_IN_HAND WHERE CASH_NO = :b1"SELECT W_CODE,CC_CODE,PJ_NO,ACTV_NO FROM SALES_MAN WHERE REPRS_CODE = :b1 AND ROWNUM <= 1"SELECT 1 FROM WAREHOUSE_DETAILS A,PRIVILEGE_WH B WHERE B.U_ID = :b1 AND A.W_CODE = B.W_CODE AND A.W_CODE = :b2 AND NVL(B.ADD_FLAG,0) = 1 AND NVL(INACTIVE,0) = 0 AND ROWNUM <= 1"SELECT W_CODE,CASH_NO,CC_CODE,PJ_NO,ACTV_NO FROM SALES_MAN WHERE REPRS_CODE = :b1 AND ROWNUM <= 1"SELECT CASH_NO FROM SALES_MAN WHERE REPRS_CODE = :b1 AND ROWNUM <= 1"SELECT 1 FROM PRIV_CASH WHERE U_ID = :b1 AND CASH_NO = :b2 AND NVL(ADD_FLAG,0) = 1 AND CASH_TYPE = 1 AND ROWNUM <= 1"SELECT CASH_NO FROM SALES_MAN WHERE REPRS_CODE = :b1 AND ROWNUM <= 1"SELECT 1 FROM IAS_SMAN_ITM_ACTVTY WHERE REP_CODE = :b1 AND ACTIVITY_NO = :b2 AND ROWNUM <= 1"SELECT 1 FROM SALES_MAN WHERE W_CODE = :b1 AND ROWNUM <= 1"SELECT W_CODE,REPRS_CODE FROM SALES_MAN WHERE W_CODE = :b1 AND ROWNUM <= 1"SELECT I_CODE FROM IAS_RT_BILL_DTL_RQ WHERE RT_BILL_SER = :b1"SELECT 1 FROM IAS_SMAN_ITM_ACTVTY WHERE REP_CODE = :b1 AND ACTIVITY_NO = :b2 AND ROWNUM <= 1"
```
```sql
UPDATE IAS_RT_BILL_DTL_RQ SET VAT_AMT=ROUND(((NVL(VAT_PER,0) * NVL(I_PRICE,0) ) / 100 ),:b1) WHERE RT_BILL_SER = :b2 AND NVL(I_QTY,0) > 0"UPDATE IAS_RT_BILL_DTL_RQ SET I_PRICE=NVL(I_PRICE_VAT,0) - NVL(VAT_AMT,0) WHERE RT_BILL_SER = :b1 AND NVL(I_QTY,0) > 0"UPDATE IAS_RT_BILL_DTL_RQ SET VAT_AMT=ROUND((NVL(VAT_PER,0) * (NVL(I_PRICE,0) - NVL(DIS_AMT,0) ) ) / 100 ,:b1) WHERE RT_BILL_SER = :b2 AND NVL(I_QTY,0) > 0"UPDATE IAS_RT_BILL_DTL_RQ SET I_PRICE=NVL(I_PRICE_VAT,0) - NVL(VAT_AMT,0) WHERE RT_BILL_SER = :b1 AND NVL(I_QTY,0) > 0"UPDATE IAS_RT_BILL_MST_RQ SET VAT_AMT=(SELECT ROUND(SUM(NVL(I_QTY,0) * NVL(VAT_AMT,0) ),:b1) FROM IAS_RT_BILL_DTL_RQ WHERE RT_BILL_SER = :b2 ) WHERE RT_BILL_SER = :b2"
```
```sql
Select 1 From Ias_Bill_Mst
```
```sql
=1) Or Exists ( Select 1 From S_Brn_Usr_Priv
```
```sql
Select Count(1) From Ias_Bill_Mst
```
```sql
Select 1 From Other_Charges
```
```sql
SELECT BILL_SER FROM IAS_BILL_MST WHERE BILL_NO = :b1 AND ((:b2 = 0 ) OR EXISTS (SELECT 1 FROM IAS_SHW_DOC_PRIV WHERE U_ID = :b3 AND T_U_ID = IAS_BILL_MST.AD_U_ID AND NVL(PRIV_FLAG,0) = 1 AND ROWNUM <= 1 ) )"SELECT BILL_DOC_TYPE,BILL_SER FROM IAS_BILL_MST WHERE BILL_NO = :b1 AND BILL_SER = :b2 AND NVL(STAND_BY,0) = 0 AND ((:b3 = 1 ) OR EXISTS (SELECT 1 FROM S_BRN_USR_PRIV WHERE U_ID = :b3 AND S_BRN_USR_PRIV.BRN_NO = IAS_BILL_MST.BRN_NO AND NVL(ADD_FLAG,1) = 1 AND ROWNUM <= 1 ) )"SELECT BILL_CURRENCY,C_CODE,CASH_NO,DECODE(BILL_DOC_TYPE,5,1,BILL_DOC_TYPE),C_NAME,A_CODE,DISC_AMT_MST,BILL_AMT,DECODE(:b1,1,W_CODE),DECODE(:b2,1,CC_CODE),BILL_NO_CONN,BILL_SER_CONN,BILL_DATE,PJ_NO,ACTV_NO,R_CODE,REP_CODE,PRM_CODE,DOC_BRN_NO,EMP_NO,FIELD1,FIELD2,FIELD3,FIELD4,FIELD5,FIELD6,FIELD7,FIELD8,FIELD9,FIELD10 FROM IAS_BILL_MST WHERE BILL_NO = :b3 AND BILL_SER = :b4 AND NVL(STAND_BY,0) = 0"SELECT BILL_CURRENCY,C_CODE,C_NAME FROM IAS_BILL_MST WHERE BILL_NO = :b1 AND BILL_SER = :b2 AND DECODE(:b3,1,NVL(CC_CODE,0),0) = DECODE(:b3,1,NVL(:b5,0),0)"SELECT CC_CODE FROM IAS_BILL_MST WHERE BILL_SER = :b1"SELECT I_CODE,ITM_UNT,NVL(P_SIZE,1) P_SIZE,W_CODE,CC_CODE,NVL(I_QTY,0) I_QTY,NVL(FREE_QTY,0) FREE_QTY,NVL(I_PRICE_VAT,0) I_PRICE_VAT,NVL(I_PRICE,0) I_PRICE,NVL(STK_COST,0) STK_COST,DECODE(EXPIRE_DATE,''01/01/1900'',TO_DATE( NULL ),TO_CHAR(EXPIRE_DATE,''DD/MM/YYYY'')) EXPIRE_DATE,DECODE(BATCH_NO,''0'', NULL ,BATCH_NO) BATCH_NO,RCRD_NO,BILL_NO,DECODE(BILL_DOC_TYPE,5,1,BILL_DOC_TYPE) BILL_DOC_TYPE,NVL(VAT_AMT,0) VAT_AMT,NVL(DIS_PER,0) DIS_PER,NVL(DIS_PER2,0) DIS_PER2,NVL(DIS_PER3,0) DIS_PER3,NVL(DIS_AMT,0) DIS_AMT,NVL(DIS_AMT_MST,0) DIS_AMT_MST,NVL(DIS_AMT_DTL,0) DIS_AMT_DTL,NVL(DIS_AMT_DTL2,0) DIS_AMT_DTL2,NVL(DIS_AMT_DTL3,0) DIS_AMT_DTL3,NVL(ADD_DIS_AMT_DTL,0) ADD_DIS_AMT_DTL,NVL(ADD_DIS_AMT_MST,0) ADD_DIS_AMT_MST,BILL_SER,NVL(SERVICE_ITEM,0) SERVICE_ITEM,BARCODE,DOC_SEQUENCE,REC_ATTCH,USE_ATTCH,PJ_NO,ACTV_NO,I_LENGTH,I_WIDTH,I_HEIGHT,I_NUMBER,WT_QTY,WT_UNT,ARGMNT_NO,FIELD_DTL1,FIELD_DTL2,FIELD_DTL3,INSRNCE_LOAD_PER,INSRNCE_LOAD_AMT,INSRNCE_ADD_LMT,EMP_NO FROM IAS_BILL_DTL WHERE BILL_NO = :b1 AND BILL_SER = :b2 AND EXISTS (SELECT 1 FROM IAS_ITM_MST WHERE I_CODE = IAS_BILL_DTL.I_CODE AND NVL(NO_RETURN_SALE,0) = 0 AND ROWNUM <= 1 ) ORDER BY RCRD_NO"SELECT NVL(SUM(B.I_QTY),0),NVL(SUM(B.FREE_QTY),0) FROM IAS_RT_BILL_MST A,IAS_RT_BILL_DTL B WHERE A.RT_BILL_SER = B.RT_BILL_SER AND B.BILL_SER = :b1 AND B.I_CODE = :b2 AND NVL(B.SI_RCRD_NO,0) = NVL(:b3,0) AND A.P_YEAR IN ( 0,3 )"SELECT NVL(SUM(I_QTY),0),NVL(SUM(FREE_QTY),0) FROM DETAIL_OUT_BILLS WHERE BILL_NO = :b1 AND BILL_SER = :b2 AND I_CODE = :b3 AND NVL(RCRD_NO,0) = NVL(:b4,0)"SELECT USE_SERIALNO FROM IAS_ITM_MST WHERE I_CODE = :b1"SELECT OTHER_CHARGES.SC_NO,SC_NAME,OTHER_CHARGES.A_CODE,PER,OTHER_CHARGES.AMT FROM OTHER_CHARGES,SALES_CHARGES WHERE SALES_CHARGES.SC_NO = OTHER_CHARGES.SC_NO AND NVL(SALES_CHARGES.ALLOW_RETURN,0) = 1 AND OTHER_CHARGES.BILL_TYPE = 1 AND BILL_NO = :b1 AND BILL_SER = :b2"
```
```sql
SELECT I_CODE,ITM_UNT,NVL(P_SIZE,1) P_SIZE,W_CODE,CC_CODE,NVL(I_QTY,0) I_QTY,NVL(FREE_QTY,0) FREE_QTY,NVL(I_PRICE_VAT,0) I_PRICE_VAT,NVL(I_PRICE,0) I_PRICE,NVL(STK_COST,0) STK_COST,DECODE(EXPIRE_DATE,''01/01/1900'',TO_DATE( NULL ),TO_CHAR(EXPIRE_DATE,''DD/MM/YYYY'')) EXPIRE_DATE,DECODE(BATCH_NO,''0'', NULL ,BATCH_NO) BATCH_NO,RCRD_NO,BILL_NO,BILL_DOC_TYPE,NVL(VAT_AMT,0) VAT_AMT,NVL(DIS_PER,0) DIS_PER,NVL(DIS_AMT,0) DIS_AMT,NVL(DIS_AMT_MST,0) DIS_AMT_MST,NVL(DIS_AMT_DTL,0) DIS_AMT_DTL,BILL_SER,NVL(SERVICE_ITEM,0) SERVICE_ITEM,BARCODE,DOC_SEQUENCE,REC_ATTCH,USE_ATTCH,PJ_NO,ACTV_NO,FIELD_DTL1,FIELD_DTL2,FIELD_DTL3,INSRNCE_LOAD_PER,INSRNCE_LOAD_AMT,INSRNCE_ADD_LMT,EMP_NO FROM IAS_BILL_DTL WHERE BILL_NO = :b1 AND BILL_SER = :b2 AND EXISTS (SELECT 1 FROM IAS_ITM_MST WHERE I_CODE = IAS_BILL_DTL.I_CODE AND NVL(NO_RETURN_SALE,0) = 0 AND ROWNUM <= 1 ) ORDER BY RCRD_NO"SELECT NVL(SUM(B.I_QTY),0),NVL(SUM(B.FREE_QTY),0) FROM IAS_RT_BILL_MST A,IAS_RT_BILL_DTL B WHERE A.RT_BILL_SER = B.RT_BILL_SER AND B.BILL_SER = :b1 AND B.I_CODE = :b2 AND NVL(B.SI_RCRD_NO,0) = NVL(:b3,0) AND A.P_YEAR IN ( 0,3 )"SELECT USE_SERIALNO FROM IAS_ITM_MST WHERE I_CODE = :b1"SELECT OTHER_CHARGES.SC_NO,SC_NAME,OTHER_CHARGES.A_CODE,PER,OTHER_CHARGES.AMT FROM OTHER_CHARGES,SALES_CHARGES WHERE SALES_CHARGES.SC_NO = OTHER_CHARGES.SC_NO AND NVL(SALES_CHARGES.ALLOW_RETURN,0) = 1 AND OTHER_CHARGES.BILL_TYPE = 1 AND BILL_NO = :b1 AND BILL_SER = :b2"
```
```sql
Select Paper_No
```
```sql
Select Count(1)
```
```sql
SELECT COUNT(1) FROM WAREHOUSE_DETAILS"SELECT W_CODE FROM WAREHOUSE_DETAILS"SELECT I_CODE,I_QTY FROM GATE_YS.GR WHERE DOC_NO = :b1 AND PAPER_SERIAL = :b2 AND BRANCH = :b3 AND YEAR = :b4 ORDER BY SER_NO FOR UPDATE NOWAIT"SELECT I_CODE,SUM(NVL(I_QTY,0)) I_QTY FROM GATE_YS.GR WHERE DOC_NO = :b1 AND PAPER_SERIAL = :b2 AND DOC_TYPE IN ( 3,5,6 ) AND BRANCH = :b3 AND YEAR = :b4 GROUP BY I_CODE"
```
```sql
SELECT R_code,
```
```sql
Or Exists ( Select 1 From S_Brn_Usr_Priv
```
```sql
SELECT BILL_DOC_TYPE,BILL_SER FROM IAS_BILL_MST WHERE BILL_NO = :b1 AND BILL_SER = :b2 AND ((:b3 = 1 ) OR EXISTS (SELECT 1 FROM S_BRN_USR_PRIV WHERE U_ID = :b3 AND S_BRN_USR_PRIV.BRN_NO = IAS_BILL_MST.BRN_NO AND NVL(ADD_FLAG,1) = 1 AND ROWNUM <= 1 ) )"SELECT BILL_CURRENCY,C_CODE,CASH_NO,BILL_DOC_TYPE,C_NAME,A_CODE,DISC_AMT_MST,BILL_AMT,DECODE(:b1,1,W_CODE),DECODE(:b2,1,CC_CODE),BILL_NO_CONN,BILL_SER_CONN,PJ_NO,ACTV_NO,FIELD1,FIELD2,FIELD3,FIELD4,FIELD5,FIELD6,FIELD7,FIELD8,FIELD9,FIELD10,EMP_NO FROM IAS_BILL_MST WHERE BILL_NO = :b3 AND BILL_SER = :b4"SELECT BILL_CURRENCY,C_CODE,C_NAME FROM IAS_BILL_MST WHERE BILL_NO = :b1 AND BILL_SER = :b2 AND DECODE(:b3,1,NVL(CC_CODE,0),0) = DECODE(:b3,1,NVL(:b5,0),0)"SELECT CC_CODE FROM IAS_BILL_MST WHERE BILL_SER = :b1"SELECT I_CODE,ITM_UNT,NVL(P_SIZE,1) P_SIZE,W_CODE,CC_CODE,NVL(I_QTY,0) I_QTY,NVL(FREE_QTY,0) FREE_QTY,NVL(I_PRICE_VAT,0) I_PRICE_VAT,NVL(I_PRICE,0) I_PRICE,NVL(STK_COST,0) STK_COST,DECODE(EXPIRE_DATE,''01/01/1900'',TO_DATE( NULL ),TO_CHAR(EXPIRE_DATE,''DD/MM/YYYY'')) EXPIRE_DATE,DECODE(BATCH_NO,''0'', NULL ,BATCH_NO) BATCH_NO,RCRD_NO,BILL_NO,BILL_DOC_TYPE,NVL(VAT_AMT,0) VAT_AMT,NVL(DIS_PER,0) DIS_PER,NVL(DIS_AMT,0) DIS_AMT,NVL(DIS_AMT_MST,0) DIS_AMT_MST,NVL(DIS_AMT_DTL,0) DIS_AMT_DTL,NVL(ADD_DIS_AMT_DTL,0) ADD_DIS_AMT_DTL,NVL(ADD_DIS_AMT_MST,0) ADD_DIS_AMT_MST,BILL_SER,NVL(SERVICE_ITEM,0) SERVICE_ITEM,BARCODE,DOC_SEQUENCE,REC_ATTCH,USE_ATTCH,PJ_NO,ACTV_NO,FIELD_DTL1,FIELD_DTL2,FIELD_DTL3,INSRNCE_LOAD_PER,INSRNCE_LOAD_AMT,INSRNCE_ADD_LMT,EMP_NO,I_LENGTH,I_WIDTH,I_HEIGHT,I_NUMBER,WT_QTY,WT_UNT,ARGMNT_NO FROM IAS_BILL_DTL WHERE BILL_NO = :b1 AND BILL_DOC_TYPE = :b2 AND BILL_SER = :b3 AND EXISTS (SELECT 1 FROM IAS_ITM_MST WHERE I_CODE = IAS_BILL_DTL.I_CODE AND NVL(NO_RETURN_SALE,0) = 0 AND ROWNUM <= 1 ) ORDER BY RCRD_NO"SELECT NVL(SUM(I_QTY),0),NVL(SUM(FREE_QTY),0) FROM IAS_RT_BILL_DTL WHERE BILL_SER = :b1 AND I_CODE = :b2 AND NVL(SI_RCRD_NO,0) = NVL(:b3,0)"SELECT NVL(SUM(I_QTY),0),NVL(SUM(FREE_QTY),0) FROM DETAIL_OUT_BILLS WHERE BILL_SER = :b1 AND I_CODE = :b2 AND NVL(RCRD_NO,0) = NVL(:b3,0)"SELECT USE_SERIALNO FROM IAS_ITM_MST WHERE I_CODE = :b1"SELECT OTHER_CHARGES.SC_NO,SC_NAME,OTHER_CHARGES.A_CODE,PER,OTHER_CHARGES.AMT FROM OTHER_CHARGES,SALES_CHARGES WHERE SALES_CHARGES.SC_NO = OTHER_CHARGES.SC_NO AND NVL(SALES_CHARGES.ALLOW_RETURN,0) = 1 AND OTHER_CHARGES.BILL_TYPE = 1 AND OTHER_CHARGES.BILL_DOC_TYPE = :b1 AND BILL_NO = :b2 AND BILL_SER = :b3"
```
```sql
Select Count(1) From Ias_V_Si_Py
```
```sql
Select nvl(sum(Nvl(D.free_qty,0)*Nvl(P_Size,1)),0) From
```
```sql
Select 1 From Ias_V_Other_Charges_Py
```
```sql
SELECT BILL_SER FROM IAS_V_SI_PY WHERE BILL_NO = :b1 AND BILL_SER = :b2"SELECT BILL_CURRENCY,C_CODE,CASH_NO,BILL_DOC_TYPE,C_NAME,DECODE(:b1,1,W_CODE),DECODE(:b2,1,CC_CODE),R_CODE,REP_CODE,PJ_NO,ACTV_NO FROM IAS_V_SI_PY WHERE BILL_NO = :b3 AND BILL_SER = :b4"SELECT BILL_CURRENCY,C_CODE,C_NAME FROM IAS_V_SI_PY WHERE BILL_NO = :b1 AND BILL_SER = :b2 AND DECODE(:b3,1,NVL(CC_CODE,0),0) = DECODE(:b3,1,NVL(:b5,0),0)"SELECT CC_CODE FROM IAS_V_SI_PY WHERE BILL_SER = :b1"SELECT I_CODE,ITM_UNT,P_SIZE,W_CODE,CC_CODE,PJ_NO,ACTV_NO,I_QTY,FREE_QTY,I_PRICE_VAT,I_PRICE,STK_COST,EXPIRE_DATE,BATCH_NO,RCRD_NO,BILL_NO,BILL_DOC_TYPE,VAT_PER,VAT_AMT,DIS_PER,DIS_AMT,DIS_AMT_MST,BILL_SER,SERVICE_ITEM,BARCODE,DIS_AMT_DTL,ADD_DIS_AMT_DTL,ADD_DIS_AMT_MST,INSRNCE_LOAD_PER,INSRNCE_LOAD_AMT,INSRNCE_ADD_LMT,WT_UNT,WT_QTY,ARGMNT_NO FROM IAS_V_SI_DET_PY WHERE BILL_NO = :b1 AND BILL_SER = :b2 ORDER BY RCRD_NO"SELECT NVL(SUM(B.I_QTY),0),NVL(SUM(B.FREE_QTY),0) FROM IAS_RT_BILL_MST_RQ A,IAS_RT_BILL_DTL_RQ B WHERE A.RT_BILL_SER = B.RT_BILL_SER AND B.BILL_SER = :b1 AND B.I_CODE = :b2 AND NVL(B.SI_RCRD_NO,0) = NVL(:b3,0) AND A.P_YEAR = 1"SELECT USE_SERIALNO FROM IAS_ITM_MST WHERE I_CODE = :b1"SELECT A.SC_NO,SC_NAME,A.A_CODE,PER,A.AMT FROM IAS_V_OTHER_CHARGES_PY A,SALES_CHARGES WHERE SALES_CHARGES.SC_NO = A.SC_NO AND NVL(SALES_CHARGES.ALLOW_RETURN,0) = 1 AND A.BILL_TYPE = 1 AND A.BILL_DOC_TYPE = :b1 AND BILL_NO = :b2 AND BILL_SER = :b3"
```
```sql
UPDATE IAS_RT_BILL_DTL_RQ SET OTHR_AMT=0 WHERE RT_BILL_SER = :b1"SELECT 1 FROM OTHER_CHARGES_OTHRS WHERE BILL_SER = :b1 AND BILL_TYPE = 3 AND NVL(INV_ITEM,0) = 1 AND ROWNUM <= 1"SELECT 1 FROM SALES_CHARGES WHERE NVL(INV_ITEM,0) = 1 AND NVL(SC_QTY_TYPE,0) = 2 AND ROWNUM <= 1"UPDATE OTHER_CHARGES_OTHRS A SET AMT=(SELECT ROUND(NVL(SUM(AMT),0),:b1) AMT FROM OTHER_CHARGES_ITEMS_OTHRS WHERE BILL_TYPE = 3 AND BILL_NO = A.BILL_NO AND BILL_SER = A.BILL_SER AND SC_NO = A.SC_NO ) WHERE BILL_TYPE = 3 AND BILL_SER = :b2 AND NVL(INV_ITEM,0) = 1"UPDATE IAS_RT_BILL_DTL_RQ A SET OTHR_AMT=(SELECT SUM(DECODE(NVL(UNIT_AMT,0),0,(NVL(OTHER_CHARGES_ITEMS_OTHRS.AMT,0) / DECODE(NVL(A.I_QTY,0),0,DECODE(NVL(A.FREE_QTY,0),0,1,A.FREE_QTY),A.I_QTY) ),UNIT_AMT)) FROM OTHER_CHARGES_ITEMS_OTHRS WHERE OTHER_CHARGES_ITEMS_OTHRS.BILL_SER = A.RT_BILL_SER AND OTHER_CHARGES_ITEMS_OTHRS.BILL_SER = :b1 AND OTHER_CHARGES_ITEMS_OTHRS.I_CODE = A.I_CODE AND OTHER_CHARGES_ITEMS_OTHRS.RCRD_NO = A.RCRD_NO ) WHERE RT_BILL_SER = :b1"UPDATE IAS_RT_BILL_DTL_RQ A SET OTHR_AMT=(SELECT SUM(NVL(OTHER_CHARGES_ITEMS_OTHRS.AMT,0) / DECODE(NVL(A.I_QTY,0),0,DECODE(NVL(A.FREE_QTY,0),0,1,A.FREE_QTY),A.I_QTY) ) FROM OTHER_CHARGES_ITEMS_OTHRS WHERE OTHER_CHARGES_ITEMS_OTHRS.BILL_SER = A.RT_BILL_SER AND OTHER_CHARGES_ITEMS_OTHRS.BILL_SER = :b1 AND OTHER_CHARGES_ITEMS_OTHRS.I_CODE = A.I_CODE AND OTHER_CHARGES_ITEMS_OTHRS.RCRD_NO = A.RCRD_NO ) WHERE RT_BILL_SER = :b1"SELECT 1 FROM OTHER_CHARGES_OTHRS WHERE BILL_SER = :b1 AND BILL_TYPE = 3 AND NVL(INV_ITEM,0) = 0 AND ROWNUM <= 1"UPDATE IAS_RT_BILL_DTL_RQ A SET OTHR_AMT=(SELECT NVL(A.OTHR_AMT,0) + (SUM(NVL(OTHER_CHARGES_OTHRS.AMT,0)) / NVL(:b1,0) ) * NVL(A.I_PRICE,0) FROM OTHER_CHARGES_OTHRS WHERE OTHER_CHARGES_OTHRS.BILL_SER = A.RT_BILL_SER AND NVL(OTHER_CHARGES_OTHRS.INV_ITEM,0) = 0 AND OTHER_CHARGES_OTHRS.BILL_SER = :b2 ) WHERE RT_BILL_SER = :b2 AND NVL(I_QTY,0) > 0"UPDATE IAS_RT_BILL_MST_RQ SET OTHR_AMT=(SELECT ROUND(NVL(SUM(AMT),0),:b1) FROM OTHER_CHARGES_OTHRS WHERE BILL_TYPE = 3 AND BILL_SER = :b2 ) WHERE RT_BILL_SER = :b2"
```
```sql
SELECT NVL(SHOW_ITEM_DESC,0),NVL(ITM_DISC_TYPE,1),NVL(DISC_COUNT,1) FROM IAS_PARA_AR"
```
```sql
And Not Exists (Select I_Code From Ias_Itm_Wcode Where I_Code = A.I_Code And RowNum<=1)
```
```sql
And Exists (Select I_Code From Ias_Itm_Wcode Where I_Code = A.I_Code And RowNum<=1)
```
```sql
And A.I_Code = C.I_Code(+) And Not Exists (Select I_Code From Ias_Item_Price Where I_Code = A.I_Code And RowNum<=1)
```
```sql
And Exists (Select I_Code From Item_Bin Where B_Code =
```
```sql
And Exists (Select I_Code From Ias_Itm_Wcode Where I_Code = A.I_Code And Nvl(Avl_Qty,0) > 0 And RowNum<=1)
```
```sql
And Exists (Select I_Code From Ias_Itm_Wcode Where I_Code = A.I_Code And Nvl(Avl_Qty,0) = 0 And RowNum<=1)
```
```sql
And Exists( Select 1 From Ias_V_Vndr_Itm Where I_Code=A.I_Code And V_Code =
```
```sql
And Exists(Select I_Code From Ias_Bill_Dtl Where Bill_Ser=
```
```sql
And Exists (Select 1 From Privilege_Gc Where U_id =
```
```sql
And Exists (Select 1
```
```sql
Select nvl(sum(nvl(D.Free_qty,0)*Nvl(p_size,1)),0) From
```
```sql
SELECT NVL(SUM(NVL(FREE_QTY,0) * NVL(P_SIZE,1) ),0) FROM IAS_V_SI_DET_PY WHERE BILL_SER = :b1 AND I_CODE = :b2 AND NVL(RCRD_NO,0) = NVL(:b3,0)"SELECT NVL(SUM(NVL(D.FREE_QTY,0) * NVL(D.P_SIZE,1) ),0) FROM IAS_RT_BILL_MST M,IAS_RT_BILL_DTL D WHERE M.RT_BILL_SER = D.RT_BILL_SER AND M.P_YEAR = 1 AND NVL(D.BILL_SER,0) = NVL(:b1,0) AND D.I_CODE = :b2 AND NVL(D.SI_RCRD_NO,0) = NVL(:b3,0)"SELECT NVL(SUM(NVL(D.FREE_QTY,0) * NVL(D.P_SIZE,1) ),0) FROM IAS_RT_BILL_MST M,IAS_RT_BILL_DTL D WHERE M.RT_BILL_SER = D.RT_BILL_SER AND M.P_YEAR = 1 AND NVL(D.RT_BILL_SER,0) = NVL(:b1,0) AND NVL(D.BILL_SER,0) = NVL(:b2,0) AND D.I_CODE = :b3 AND NVL(D.SI_RCRD_NO,0) = NVL(:b4,0)"
```
```sql
SELECT I_CODE,TO_CHAR(W_CODE,''000'') W_CODE,TO_CHAR(EXPIRE_DATE,''DD/MM/YYYY'') EXPIRE_DATE,NVL(BATCH_NO,''0'') BATCH_NO,I_QTY,USE_SERIALNO,I_HEIGHT,I_WIDTH,I_LENGTH,NVL(BILL_SER,0) BILL_SER FROM IAS_RT_BILL_DTL_RQ WHERE RT_BILL_NO = :b1 AND RT_BILL_SER = :b2 ORDER BY RCRD_NO"
```
```sql
SELECT cash_in_hand.cash_no,
```
```sql
And (:parameter.user_no=1 Or Exists(Select cash_no From priv_cash
```
```sql
SELECT NVL(BATCH_DESC_A_NAME,BATCH_DESC_E_NAME) FROM IAS_BATCH_NO_CONTENTS WHERE COL_NO = 1 AND BATCH_DESC_NO = LPAD(SUBSTR(:b1,:b2,:b3),:b3,''0'')"SELECT NVL(BATCH_DESC_A_NAME,BATCH_DESC_E_NAME) FROM IAS_BATCH_NO_CONTENTS WHERE COL_NO = 2 AND BATCH_DESC_NO = LPAD(SUBSTR(:b1,:b2,:b3),:b3,''0'')"SELECT NVL(BATCH_DESC_A_NAME,BATCH_DESC_E_NAME) FROM IAS_BATCH_NO_CONTENTS WHERE COL_NO = 3 AND BATCH_DESC_NO = LPAD(SUBSTR(:b1,:b2,:b3),:b3,''0'')"SELECT NVL(BATCH_DESC_A_NAME,BATCH_DESC_E_NAME) FROM IAS_BATCH_NO_CONTENTS WHERE COL_NO = 4 AND BATCH_DESC_NO = LPAD(SUBSTR(:b1,:b2,:b3),:b3,''0'')"SELECT NVL(BATCH_DESC_A_NAME,BATCH_DESC_E_NAME) FROM IAS_BATCH_NO_CONTENTS WHERE COL_NO = 5 AND BATCH_DESC_NO = LPAD(SUBSTR(:b1,:b2,:b3),:b3,''0'')"
```
```sql
SELECT 1 FROM IAS_BNFCRS WHERE BNF_NO = :b1 AND NVL(INACTIVE,0) = 1 AND ROWNUM <= 1"SELECT 1 FROM IAS_INSRNCE_CMP WHERE INS_CMP_NO = :b1 AND NVL(INACTIVE,0) = 1 AND ROWNUM <= 1"SELECT 1 FROM IAS_INSRNCE_CMP_CLSS WHERE INS_CMP_NO = :b1 AND BNF_NO = :b2 AND CLSS_NO = :b3 AND NVL(INACTIVE,0) = 1 AND ROWNUM <= 1"
```
```sql
And Exists(Select 1
```
```sql
and Exists(Select cash_no From priv_cash
```
```sql
WHERE (:parameter.user_no=1 Or Exists(Select cash_no From priv_cash
```
```sql
SELECT cash_at_bank.Bank_no,
```
```sql
Select D.Itm_Unt,
```
```sql
SELECT ias_item_price.lev_no,
```
```sql
SELECT lev_no
```
```sql
Select Customer.C_Code,
```
```sql
Select Cc_Code,
```
```sql
Where C_S_M = (Select Cc_Type
```
```sql
And ((:Parameter.User_no = 1) Or (Exists ( Select Cc_Code
```
```sql
Select M.I_Code, Decode(:Parameter.Lang_No,1,Nvl(M.I_Name,M.I_E_Name),Nvl(M.I_E_Name,M.I_Name)) I_Name,
```
```sql
And (:Parameter.User_No=1 Or Exists(Select 1
```
```sql
SELECT w_code,
```
```sql
And ((:parameter.User_no = 1 ) Or (Exists (Select 1
```
```sql
SELECT reprs_code,
```
```sql
(Exists (Select 1 From IAS_PRIV_SMAN
```
```sql
Select v_details.v_code,
```
```sql
SELECT rt_bill_date, flg_desc t_name, rt_bill_no, c_name, rt_bill_doc_type,
```
```sql
(Select 1
```
```sql
SELECT RT_BILL_DATE, FLG_DESC T_NAME, RT_BILL_NO, C_NAME, W_CODE, CC_CODE, RT_BILL_DOC_TYPE,
```
```sql
ELSE (SELECT 1
```
```sql
SELECT 1
```
```sql
SELECT RT_BILL_DATE, FLG_DESC T_NAME, RT_BILL_NO, C_NAME, CC_CODE, BILL_AMT, RT_BILL_DOC_TYPE,
```
```sql
SELECT bill_date, flg_desc t_name, bill_no, c_code, c_name, bill_currency,
```
```sql
SELECT Bill_date,
```
```sql
SELECT BILL_DATE, FLG_DESC T_NAME, BILL_NO, C_CODE, C_NAME, BILL_CURRENCY,
```
```sql
SELECT Ias_Bill_Dtl.bill_no bill_no, Ias_Bill_Mst.c_name c_name,
```
```sql
AND Exists(Select 1 From Ias_Itm_Mst
```
```sql
Select Ias_Item_Serialno.Serialno,
```
```sql
And Exists(Select 1 From Ias_Itm_Mst
```
```sql
SELECT Ias_V_Si_Det_Py.bill_no bill_no,
```
```sql
select sales_charges.sc_no,sc_name,sales_charges.a_code
```
```sql
select sales_charges.a_code,nvl(a_name,a_name_eng) a_name ,sc_no
```
```sql
SELECT Distinct Gate_Ys.gt_paper_ms.paper_no,
```
```sql
Select M.I_Code,
```
```sql
And (:Parameter.User_No = 1 Or Exists (Select 1 From Privilege_Gc
```
```sql
Select B_Code ,Decode(:Parameter.Lang_No,1,nvl(B_A_Name,B_E_Name), Nvl(B_E_Name,B_A_Name)) B_Name
```
```sql
Select Manf_Code
```
```sql
Select Group_No,
```
```sql
Select G_Code,
```
```sql
Exists (Select G_Code From Privilege_Gc Where add_Flag=1 And U_Id=:Parameter.User_No And G_Code = Group_Details.G_Code)
```
```sql
SELECT SUBG_CODE,
```
```sql
OR EXISTS (SELECT G_CODE
```
```sql
SELECT MNG_CODE,
```
```sql
Select Measure, Measure_Code From Measurement
```
```sql
select ROUND(Flex_No,4)Flex_No,
```
```sql
Select Ias_Attach_Dtl.Attch_Desc_No Attch_Desc_No,
```
```sql
And Exists(Select 1 From Ias_Itm_Attach
```
```sql
select ROUND(Flex_No,4) Flex_No,
```
```sql
Select Pj_No,Decode(:Parameter.Lang_no,1,pj_a_name,nvl(pj_e_name,pj_a_name)) pj_name ,
```
```sql
and ((:Parameter.User_No=1) Or Exists (select 1 from IAS_PRIV_PROJECTS
```
```sql
SELECT desc_no,
```
```sql
SELECT Actv_no,
```
```sql
Select Brn_No,
```
```sql
SELECT DECODE(batch_no,'0','', batch_no) batch_no,
```
```sql
Select Msur_Qty_Code , Argmnt_No From Ias_Conn_Itm_Msur_Unt
```
```sql
Select Res_Typ,
```
```sql
Select C.Ins_Cmp_No,
```
```sql
and (:Parameter.User_No=1 Or Exists(Select cash_no From priv_cash
```
```sql
AND cash_in_hand.cash_no In (Select cash_no from ias_cash_in_hand_dtl where a_cy=:ias_rt_bill_mst_rq.rt_bill_currency)
```
```sql
select BNF_No,Decode(:Parameter.Lang_No,1,Nvl(BNF_L_NM, BNF_F_NM),Nvl(BNF_F_NM, BNF_L_NM)) BNF_NM from IAS_BNFCRS
```
```sql
Where bnf_no in (select bnf_no from IAS_INSRNCE_CMP_CLSS where ins_cmp_no=:ias_bill_mst_rq.insrnce_cmp_no)
```
```sql
Select Emp_No,Decode(:Parameter.Lang_No,1,Nvl(Emp_l_Nm,Emp_F_Nm),Nvl(Emp_F_Nm,Emp_L_Nm)) Emp_Name
```
```sql
KEY-COMMIT
```
```sql
SELECT R_CODE,CC_CODE FROM WAREHOUSE_DETAILS WHERE W_CODE = :b1 AND ROWNUM <= 1"
```
```sql
SELECT COUNT(1) FROM IAS_PROMOTERS WHERE PRM_CODE = :b1 AND NVL(INACTIVE,0) = 1"
```
```sql
SELECT COUNT(1) FROM IAS_CASH_IN_HAND_DTL A,PRIV_CASH B WHERE B.U_ID = :b1 AND A.CASH_NO = B.CASH_NO AND B.CASH_TYPE = 1 AND NVL(B.ADD_FLAG,0) = 1"SELECT A.CASH_NO,A.CASH_NAME,B.A_CY,A.A_CODE FROM CASH_IN_HAND A,IAS_CASH_IN_HAND_DTL B,PRIV_CASH C WHERE A.CASH_NO = B.CASH_NO AND C.U_ID = :b1 AND B.CASH_NO = C.CASH_NO AND C.CASH_TYPE = 1 AND C.ADD_FLAG = 1 AND ROWNUM <= 1"
```
```sql
SELECT COUNT(1) FROM IAS_RT_BILL_MST_RQ WHERE RT_BILL_NO = :b1 AND BRN_NO = :b2"SELECT COUNT(1) FROM IAS_RT_BILL_MST_RQ WHERE RT_BILL_DOC_TYPE = :b1 AND RT_BILL_NO = :b2 AND BRN_NO = :b3"SELECT COUNT(1) FROM IAS_RT_BILL_MST_RQ WHERE RT_BILL_NO = :b1 AND CC_CODE = :b2 AND BRN_NO = :b3"SELECT COUNT(1) FROM IAS_RT_BILL_MST_RQ WHERE RT_BILL_NO = :b1 AND CC_CODE IN (SELECT CC_CODE FROM COST_CENTERS WHERE C_SR = :b2 ) AND BRN_NO = :b3"SELECT COUNT(1) FROM IAS_RT_BILL_MST_RQ WHERE RT_BILL_NO = :b1 AND W_CODE = :b2 AND BRN_NO = :b3"SELECT COUNT(1) FROM IAS_RT_BILL_MST_RQ WHERE RT_BILL_DOC_TYPE = :b1 AND RT_BILL_NO = :b2 AND W_CODE = :b3 AND BRN_NO = :b4"SELECT COUNT(1) FROM IAS_RT_BILL_MST_RQ WHERE RT_BILL_DOC_TYPE = :b1 AND RT_BILL_NO = :b2 AND CC_CODE = :b3 AND BRN_NO = :b4 AND ROWNUM <= 1"SELECT COUNT(1) FROM IAS_RT_BILL_MST_RQ WHERE SR_TYPE = :b1 AND RT_BILL_NO = :b2 AND BRN_NO = :b3"SELECT COUNT(1) FROM IAS_RT_BILL_MST_RQ WHERE SR_TYPE = :b1 AND W_CODE = :b2 AND RT_BILL_NO = :b3 AND BRN_NO = :b4"SELECT COUNT(1) FROM IAS_RT_BILL_MST_RQ WHERE SR_TYPE = :b1 AND W_CODE = :b2 AND RT_BILL_DOC_TYPE = :b3 AND RT_BILL_NO = :b4 AND BRN_NO = :b5"
```
```sql
Select 1 From Cash_In_HAnd
```
```sql
Select 1 From Ias_Cash_In_HAnd_Dtl
```
```sql
Select 1 From Ias_Cash_At_Bank_Dtl
```
```sql
SELECT A_CODE FROM CASH_IN_HAND WHERE CASH_NO = :b1"SELECT REPRS_CODE FROM SALES_MAN WHERE CASH_NO = :b1"SELECT A_CODE FROM CASH_AT_BANK WHERE BANK_NO = :b1"SELECT REPRS_CODE,W_CODE FROM SALES_MAN WHERE CASH_NO = :b1"SELECT 1 FROM SALES_MAN WHERE REPRS_CODE = :b1 AND CASH_NO = :b2 AND ROWNUM <= 1"
```
```sql
SELECT C_A_CODE,R_CODE,REP_CODE,INACTIVE,C_CLASS,DECODE(:b1,1,NVL(C_A_NAME,C_E_NAME),NVL(C_E_NAME,C_A_NAME)),BLK_LST,CONN_BRN_NO,NVL(INSRNCE_CST,0),INSRNCE_CMP_NO FROM CUSTOMER WHERE C_CODE = :b2"SELECT DIS_PER FROM IAS_INSRNCE_CMP WHERE INS_CMP_NO = :b1 AND ROWNUM <= 1"SELECT R_CODE,REP_CODE,DECODE(:b1,1,NVL(C_A_NAME,C_E_NAME),NVL(C_E_NAME,C_A_NAME)),BLK_LST FROM CUSTOMER WHERE C_CODE = :b2"SELECT 1 FROM CUSTOMER_CURR WHERE C_CODE = :b1 AND A_CY = :b2 AND ROWNUM <= 1"SELECT 1 FROM IAS_CST_SMAN WHERE C_CODE = :b1 AND REP_CODE = :b2 AND ROWNUM <= 1"
```
```sql
SELECT CC_CODE FROM CUSTOMER WHERE C_CODE = :b1"SELECT CC_CODE FROM CUSTOMER WHERE C_CODE = :b1 AND EXISTS (SELECT 1 FROM PRIVILEGE_CC WHERE U_ID = :b2 AND CC_CODE = CUSTOMER.CC_CODE AND ADD_FLAG = 1 AND ROWNUM <= 1 )"
```
```sql
SELECT 1 FROM PRIV_ACC WHERE A_CODE = :b1 AND A_CY = :b2 AND NVL(ADD_FLAG,0) = 1 AND U_ID = :b3 AND ROWNUM <= 1"SELECT 1 FROM PRIV_ACC WHERE A_CODE = :b1 AND A_CY = :b2 AND NVL(ADD_FLAG,0) = 1 AND U_ID = :b3 AND ROWNUM <= 1"
```
```sql
SELECT C_CODE FROM CUSTOMER WHERE (LTRIM(RTRIM(C_A_NAME)) = LTRIM(RTRIM(:b1)) OR LTRIM(RTRIM(C_E_NAME)) = LTRIM(RTRIM(:b1)) )"SELECT C_A_CODE,R_CODE,REP_CODE,INACTIVE,C_CLASS FROM CUSTOMER WHERE C_CODE = :b1"
```
```sql
SELECT BILL_CURRENCY,C_CODE,REP_CODE,COMM_PER,NVL(STAND_BY,0) FROM IAS_BILL_MST WHERE BILL_SER = :b1 AND DECODE(:b2,1,NVL(CC_CODE,0),0) = DECODE(:b2,1,NVL(:b4,0),0)"
```
```sql
SELECT COUNT(1) FROM SALES_MAN WHERE REPRS_CODE = :b1 AND NVL(INACTIVE,0) = 1"SELECT COMM_PER FROM SALES_MAN WHERE REPRS_CODE = :b1 AND ROWNUM <= 1"
```
```sql
SELECT DECODE(:b1,1,NVL(RES_L_NM,RES_F_NM),NVL(RES_F_NM,RES_L_NM)) RES_NM FROM IAS_RT_SALES_REASON WHERE RES_TYP = :b2 AND ROWNUM <= 1"
```
```sql
SELECT NVL(INV_DIS_LMT,0) FROM PRIVILEGE_FIXED WHERE U_ID = :b1"
```
```sql
SELECT DECODE(:b1,1,NVL(CC_A_NAME,CC_E_NAME),NVL(CC_E_NAME,CC_A_NAME)) FROM COST_CENTERS WHERE CC_CODE = :b2"
```
```sql
SELECT A_CODE FROM CASH_IN_HAND WHERE CASH_NO = :b1"
```
```sql
SELECT COUNT(1) FROM ACCOUNT WHERE A_CODE = :b1 AND A_S_M = (SELECT ACCOUNT_TYPE FROM ACCOUNT_TYPES WHERE NVL(AFFECTED_BY_TRANS,0) = 1 ) AND A_CODE IN (SELECT A_CODE FROM CASH_IN_HAND )"SELECT COUNT(1) FROM ACCOUNT_CURR WHERE A_CODE = :b1 AND A_CY = :b2"
```
```sql
Select Excel.exe File
```
```sql
Create User
```
```sql
Create Date
```
```sql
Last Update User
```
```sql
Last Update Date
```
```sql
Update Count
```
```sql
SELECT 1 FROM IAS_BNFCRS WHERE BNF_NO = :b1 AND NVL(INACTIVE,0) = 1 AND ROWNUM <= 1"
```
```sql
SELECT BNF_NO,PRSON_CARD_NO_FMLY,DECODE(:b1,1,NVL(PRSON_L_NM,PRSON_F_NM),NVL(PRSON_F_NM,PRSON_L_NM)),RLTN_TYP,NVL(INACTIVE,0),CLSS_NO,CARD_EXPIRE_DATE,CARD_ISSUE_DATE FROM IAS_INSRNCE_CARD WHERE INS_CMP_NO = :b2 AND BNF_NO = NVL(:b3,BNF_NO) AND PRSON_CARD_NO = :b4 AND ROWNUM <= 1"
```
```sql
SELECT A_CODE FROM CASH_IN_HAND WHERE CASH_NO = :b1 AND ROWNUM <= 1"
```
```sql
SELECT 1 FROM CASH_IN_HAND WHERE CASH_NO = :b1 AND ROWNUM <= 1"SELECT A_CODE,CASH_NAME FROM CASH_IN_HAND WHERE CASH_NO = :b1"SELECT 1 FROM IAS_CASH_IN_HAND_DTL WHERE CASH_NO = :b1 AND A_CY = :b2 AND ROWNUM <= 1"
```
```sql
UPDATE IAS_RT_BILL_DTL_RQ SET CC_CODE=:b1 WHERE RT_BILL_NO = :b2 AND RT_BILL_SER = :b3"UPDATE IAS_ITM_ATTACH_OTHRS SET CC_CODE=:b1 WHERE DOC_SER = :b2"UPDATE IAS_ITEM_SERIALNO_OTHRS SET CC_CODE=:b1 WHERE DOC_TYPE = 3 AND DOC_SER = :b2"UPDATE IAS_ITM_ATTACH_OTHRS SET C_CODE=:b1,REP_CODE=:b2 WHERE DOC_TYPE = 3 AND DOC_SER = :b3"UPDATE IAS_RT_BILL_DTL_RQ SET PJ_NO=:b1 WHERE RT_BILL_SER = :b2"UPDATE IAS_RT_BILL_DTL_RQ SET ACTV_NO=:b1 WHERE RT_BILL_SER = :b2"UPDATE IAS_RT_BILL_DTL_RQ SET EMP_NO=:b1 WHERE RT_BILL_SER = :b2"
```
```sql
DELETE FROM IAS_RT_BILL_DTL_RQ WHERE RT_BILL_NO = :b1 AND RT_BILL_DOC_TYPE = :b2 AND RT_BILL_SER = :b3"DELETE FROM OTHER_CHARGES_OTHRS WHERE BILL_TYPE = 3 AND BILL_NO = :b1 AND BILL_DOC_TYPE = :b2 AND BILL_SER = :b3"DELETE FROM OTHER_CHARGES_ITEMS_OTHRS WHERE BILL_TYPE = 3 AND BILL_NO = :b1 AND BILL_DOC_TYPE = :b2 AND BILL_SER = :b3"DELETE FROM IAS_ITEM_SERIALNO_OTHRS I WHERE DOC_TYPE = 3 AND I.DOC_NO = :b1 AND I.DOC_SER = :b2"DELETE FROM IAS_ITM_ATTACH_OTHRS I WHERE I.DOC_SER = :b1"
```
```sql
SELECT NVL(A_NAME,A_NAME_ENG) FROM ACCOUNT WHERE A_CODE = :b1"SELECT NVL(CASH_NAME,CASH_E_NAME) FROM CASH_IN_HAND WHERE CASH_NO = :b1"SELECT NVL(A_NAME,A_NAME_ENG) FROM ACCOUNT WHERE A_CODE = :b1"SELECT NVL(BANK_NAME,BANK_E_NAME) FROM CASH_AT_BANK WHERE BANK_NO = :b1"SELECT NVL(A_NAME,A_NAME_ENG) FROM ACCOUNT WHERE A_CODE = :b1"SELECT C_CLASS,NVL(INSRNCE_CST,0) FROM CUSTOMER WHERE C_CODE = :b1"SELECT DECODE(:b1,1,NVL(CASH_NAME,CASH_E_NAME),NVL(CASH_E_NAME,CASH_NAME)) FROM CASH_IN_HAND WHERE CASH_NO = :b2 AND A_CODE = :b3 AND ROWNUM <= 1"SELECT DIS_PER FROM IAS_INSRNCE_CMP WHERE INS_CMP_NO = :b1 AND ROWNUM <= 1"
```
```sql
Cannot delete master record when matching detail records exist.
```
```sql
SELECT 1 FROM IAS_RT_BILL_DTL I WHERE I.RT_BILL_NO = :b1 AND I.RT_BILL_DOC_TYPE = :b2 AND I.RT_BILL_SER = :b3"SELECT 1 FROM OTHER_CHARGES_OTHRS O WHERE O.BILL_NO = :b1 AND O.BILL_DOC_TYPE = :b2 AND O.BILL_SER = :b3"
```
```sql
SELECT 1 FROM IAS_ITM_MST WHERE I_CODE = :b1 AND ROWNUM <= 1"
```
```sql
SELECT FLEX_NO FROM IAS_ITM_UNT_BARCODE WHERE BARCODE = :b1"SELECT 1 FROM IAS_ITM_MST WHERE I_CODE = :b1 AND ROWNUM <= 1"SELECT DECODE(:b1,1,NVL(I_NAME,I_E_NAME),NVL(I_E_NAME,I_NAME)),I_DESC,NVL(VAT_PER,0),NVL(INACTIVE,0),NVL(:b2,0),NVL(USE_QTY_FRACTION,0),NVL(SERVICE_ITM,0),NVL(ALLOW_FREE_QTY,0),NVL(USE_SERIALNO,0),NVL(NO_RETURN_SALE,0),RETURN_PERIOD,RET_ITM_BEFOR_EXP_PRD,NVL(USE_ATTCH,0),NVL(UNDER_SELLING,0),NVL(USE_GRANT,0),NVL(USE_WEIGHT,0) FROM IAS_ITM_MST WHERE I_CODE = :b3 AND ROWNUM <= 1"SELECT DECODE(:b1,1,NVL(I_NAME,I_E_NAME),NVL(I_E_NAME,I_NAME)),I_DESC,NVL(VAT_PER,0),NVL(INACTIVE,0),NVL(:b2,0),NVL(USE_QTY_FRACTION,0),NVL(SERVICE_ITM,0),NVL(ALLOW_FREE_QTY,0),NVL(USE_SERIALNO,0),NVL(NO_RETURN_SALE,0),RETURN_PERIOD,RET_ITM_BEFOR_EXP_PRD,NVL(USE_ATTCH,0),NVL(UNDER_SELLING,0),NVL(USE_GRANT,0),NVL(USE_WEIGHT,0) FROM IAS_ITM_MST WHERE I_CODE = :b3 AND ROWNUM <= 1"SELECT NVL(VAT_TYPE,0) FROM IAS_ITM_MST WHERE I_CODE = :b1"SELECT NVL(USE_EXP_DATE,0),NVL(USE_BATCH_NO,0) FROM IAS_ITM_MST WHERE I_CODE = :b1"SELECT V_CODE FROM IAS_V_VNDR_ITM WHERE I_CODE = :b1"
```
```sql
SELECT COUNT(1) FROM IAS_BILL_MST WHERE BILL_NO = :b1 AND BILL_SER = NVL(:b2,0) AND NVL(STAND_BY,0) = 0"SELECT BILL_SER FROM IAS_BILL_MST WHERE BILL_NO = :b1"SELECT 1 FROM IAS_BILL_MST WHERE BILL_SER = :b1 AND NVL(STAND_BY,0) = 0 AND ROWNUM <= 1"SELECT 1 FROM IAS_BILL_DTL WHERE BILL_SER = :b1 AND I_CODE = :b2 AND ROWNUM <= 1"SELECT 1 FROM IAS_BILL_DTL WHERE BILL_SER = :b1 AND I_CODE = :b2 AND W_CODE = :b3 AND ROWNUM <= 1"SELECT 1 FROM IAS_BILL_DTL WHERE BILL_SER = :b1 AND I_CODE = :b2 AND CC_CODE = :b3 AND ROWNUM <= 1"SELECT BILL_DATE,BILL_NO_CONN FROM IAS_BILL_MST WHERE BILL_SER = :b1 AND ROWNUM <= 1"SELECT BILL_SER,RCRD_NO,DOC_SEQUENCE,ROUND(((I_PRICE_VAT / P_SIZE ) * NVL(:b1,1) ),:b2),ROUND(((I_PRICE / P_SIZE ) * NVL(:b1,1) ),:b2),((STK_COST / P_SIZE ) * NVL(:b1,1) ),DIS_PER,VAT_PER,VAT_AMT,((NVL(FREE_QTY,0) / NVL(I_QTY,0) ) * 100 ) FREE_QTY_PER,INSRNCE_LOAD_PER,INSRNCE_LOAD_AMT,INSRNCE_ADD_LMT,EMP_NO,WT_UNT,WT_QTY,ARGMNT_NO FROM IAS_BILL_DTL WHERE BILL_SER = :b6 AND I_CODE = :b7 AND NVL(EXPIRE_DATE,''01/01/1900'') = NVL(TO_CHAR(:b8,''dd/mm/yyyy''),''01/01/1900'') AND NVL(BATCH_NO,''0'') = NVL(:b9,''0'')"SELECT TO_DATE(:b1,''dd/mm/yyyy'') - TO_DATE(BILL_DATE,''dd/mm/yyyy'') FROM IAS_BILL_MST WHERE BILL_SER = :b2"SELECT NVL(I_QTY,0),REC_ATTCH FROM IAS_BILL_DTL WHERE BILL_SER = NVL(:b1,0) AND I_CODE = :b2 AND RCRD_NO = NVL(:b3,0)"SELECT NVL(I_QTY,0) FROM DETAIL_OUT_BILLS WHERE BILL_SER = NVL(:b1,0) AND I_CODE = :b2 AND RCRD_NO = NVL(:b3,0)"SELECT BILL_CURRENCY,C_CODE,(NVL(DISC_AMT_MST,0) + NVL(ADD_DISC_AMT_MST,0) ),BILL_AMT FROM IAS_BILL_MST WHERE BILL_SER = :b1"SELECT CC_CODE FROM IAS_BILL_DTL WHERE BILL_SER = :b1 AND I_CODE = :b2 AND RCRD_NO = :b3"SELECT TO_DATE(EXPIRE_DATE,''DD/MM/RRRR'') FROM IAS_BILL_DTL WHERE BILL_SER = :b1 AND I_CODE = :b2 AND RCRD_NO = :b3"SELECT BATCH_NO FROM IAS_BILL_DTL WHERE BILL_SER = :b1 AND I_CODE = :b2 AND RCRD_NO = :b3"SELECT NVL(SUM(P_QTY),0),NVL(SUM(FREE_QTY * NVL(P_SIZE,1) ),0) FROM IAS_BILL_DTL WHERE BILL_SER = NVL(:b1,0) AND I_CODE = :b2 AND RCRD_NO = NVL(:b3,0)"SELECT NVL(SUM(B.P_QTY),0),NVL(SUM(NVL(B.FREE_QTY,0) * NVL(P_SIZE,1) ),0) FROM IAS_RT_BILL_MST_RQ A,IAS_RT_BILL_DTL_RQ B WHERE A.RT_BILL_SER = B.RT_BILL_SER AND A.P_YEAR IN ( 0,3 ) AND NVL(B.BILL_SER,0) = NVL(:b1,0) AND B.I_CODE = :b2 AND B.SI_RCRD_NO = NVL(:b3,0)"SELECT NVL(SUM(P_QTY),0),NVL(SUM(FREE_QTY * NVL(P_SIZE,1) ),0) FROM DETAIL_OUT_BILLS WHERE BILL_SER = NVL(:b1,0) AND I_CODE = :b2 AND RCRD_NO = NVL(:b3,0)"SELECT (NVL(DIS_AMT,0) / NVL(P_SIZE,0) * NVL(:b1,1) ) FROM IAS_BILL_DTL WHERE BILL_SER = NVL(:b2,0) AND I_CODE = :b3 AND RCRD_NO = NVL(:b4,0)"SELECT NVL(DIS_PER,0),(NVL(DIS_AMT,0) / NVL(P_SIZE,0) * NVL(:b1,1) ),((NVL(DIS_AMT_DTL,0) + NVL(ADD_DIS_AMT_DTL,0) ) / NVL(P_SIZE,0) * NVL(:b1,1) ),NVL(ADD_DIS_AMT_DTL,0) FROM IAS_BILL_DTL WHERE BILL_SER = NVL(:b3,0) AND I_CODE = :b4 AND RCRD_NO = NVL(:b5,0)"SELECT COUNT(1) FROM WAREHOUSE_DETAILS"SELECT W_CODE FROM WAREHOUSE_DETAILS"SELECT V_CODE FROM SALE_COST WHERE DOC_SER = :b1 AND I_CODE = :b2 AND RCRD_NO = :b3"SELECT V_CODE FROM IAS_V_VNDR_ITM WHERE I_CODE = :b1"
```
```sql
SELECT COUNT(1) FROM IAS_BILL_DTL WHERE BILL_DOC_TYPE = :b1 AND BILL_NO = :b2 AND I_CODE = :b3 AND BILL_SER = :b4 AND W_CODE = :b5"
```
```sql
SELECT NVL(SUM(P_QTY),0) FROM IAS_BILL_DTL WHERE BILL_SER = :b1 AND I_CODE = :b2 AND NVL(RCRD_NO,0) = NVL(:b3,0)"SELECT NVL(SUM(B.P_QTY),0) FROM IAS_RT_BILL_MST_RQ A,IAS_RT_BILL_DTL_RQ B WHERE A.RT_BILL_SER = B.RT_BILL_SER AND B.BILL_SER = :b1 AND B.I_CODE = :b2 AND NVL(B.RCRD_NO,0) = NVL(:b3,0) AND A.P_YEAR IN ( 0,3 ) AND ( B.RT_BILL_SER,B.I_CODE,B.SI_RCRD_NO,B.DOC_SEQUENCE_SI ) NOT IN (SELECT NVL(DOC_SER_REF,0),I_CODE,NVL(SI_RCRD_NO,0),NVL(DOC_SEQUENCE_SI,0) FROM IAS_RT_BILL_DTL )"SELECT NVL(SUM(B.P_QTY),0) FROM IAS_RT_BILL_MST A,IAS_RT_BILL_DTL B WHERE A.RT_BILL_SER = B.RT_BILL_SER AND B.BILL_SER = :b1 AND B.I_CODE = :b2 AND NVL(B.RCRD_NO,0) = NVL(:b3,0) AND A.P_YEAR IN ( 0,3 )"SELECT NVL(SUM(P_QTY),0) FROM DETAIL_OUT_BILLS WHERE BILL_SER = :b1 AND I_CODE = :b2 AND NVL(RCRD_NO,0) = NVL(:b3,0)"SELECT NVL(SUM(B.P_QTY),0) FROM IAS_RT_BILL_MST_RQ A,IAS_RT_BILL_DTL_RQ B WHERE A.RT_BILL_SER = B.RT_BILL_SER AND B.RT_BILL_SER = :b1 AND B.BILL_SER = :b2 AND B.I_CODE = :b3 AND NVL(B.SI_RCRD_NO,0) = NVL(:b4,0) AND NVL(B.RCRD_NO,0) = NVL(:b5,0) AND A.P_YEAR IN ( 0,3 )"SELECT NVL(SUM(P_QTY),0) FROM IAS_BILL_DTL WHERE BILL_SER = :b1 AND I_CODE = :b2 AND NVL(RCRD_NO,0) = NVL(:b3,0)"SELECT NVL(SUM(B.P_QTY),0) FROM IAS_RT_BILL_MST_RQ A,IAS_RT_BILL_DTL_RQ B WHERE A.RT_BILL_SER = B.RT_BILL_SER AND B.RT_BILL_SER != :b1 AND B.BILL_SER = :b2 AND B.I_CODE = :b3 AND NVL(B.RCRD_NO,0) = NVL(:b4,0) AND A.P_YEAR IN ( 0,3 ) AND ( B.RT_BILL_SER,B.I_CODE,B.SI_RCRD_NO,B.DOC_SEQUENCE_SI ) NOT IN (SELECT NVL(DOC_SER_REF,0),I_CODE,NVL(SI_RCRD_NO,0),NVL(DOC_SEQUENCE_SI,0) FROM IAS_RT_BILL_DTL )"SELECT NVL(SUM(B.P_QTY),0) FROM IAS_RT_BILL_MST A,IAS_RT_BILL_DTL B WHERE A.RT_BILL_SER = B.RT_BILL_SER AND B.BILL_SER = :b1 AND B.I_CODE = :b2 AND NVL(B.RCRD_NO,0) = NVL(:b3,0) AND A.P_YEAR IN ( 0,3 )"SELECT NVL(SUM(P_QTY),0) FROM DETAIL_OUT_BILLS WHERE BILL_SER = :b1 AND I_CODE = :b2 AND NVL(RCRD_NO,0) = NVL(:b3,0)"SELECT NVL(SUM(B.P_QTY),0) FROM IAS_RT_BILL_MST_RQ A,IAS_RT_BILL_DTL_RQ B WHERE A.RT_BILL_SER = B.RT_BILL_SER AND B.RT_BILL_SER = :b1 AND B.BILL_SER = :b2 AND B.I_CODE = :b3 AND NVL(B.SI_RCRD_NO,0) = NVL(:b4,0) AND NVL(B.RCRD_NO,0) != NVL(:b5,0) AND A.P_YEAR IN ( 0,3 )"
```
```sql
SELECT NVL(ALLOW_FREE_QTY,0) FROM IAS_ITM_MST WHERE I_CODE = :b1"
```
```sql
SELECT NVL(SUM(NVL(FREE_QTY,0) * NVL(P_SIZE,1) ),0) FROM IAS_BILL_DTL WHERE BILL_SER = :b1 AND I_CODE = :b2 AND NVL(RCRD_NO,0) = NVL(:b3,0)"SELECT NVL(SUM(NVL(FREE_QTY,0) * NVL(P_SIZE,1) ),0) FROM DETAIL_OUT_BILLS WHERE NVL(BILL_SER,0) = NVL(:b1,0) AND I_CODE = :b2 AND NVL(RCRD_NO,0) = NVL(:b3,0)"SELECT NVL(SUM(NVL(B.FREE_QTY,0) * NVL(B.P_SIZE,1) ),0) FROM IAS_RT_BILL_MST_RQ A,IAS_RT_BILL_DTL_RQ B WHERE A.RT_BILL_SER = B.RT_BILL_SER AND A.RT_BILL_SER != NVL(:b1,0) AND NVL(B.BILL_SER,0) = NVL(:b2,0) AND B.I_CODE = :b3 AND NVL(B.SI_RCRD_NO,0) = NVL(:b4,0) AND A.P_YEAR IN ( 0,3 )"SELECT NVL(SUM(NVL(B.FREE_QTY,0) * NVL(B.P_SIZE,1) ),0) FROM IAS_RT_BILL_MST A,IAS_RT_BILL_DTL B WHERE A.RT_BILL_SER = B.RT_BILL_SER AND NVL(B.BILL_SER,0) = NVL(:b1,0) AND B.I_CODE = :b2 AND NVL(B.SI_RCRD_NO,0) = NVL(:b3,0) AND A.P_YEAR IN ( 0,3 ) AND ( B.RT_BILL_SER,B.I_CODE,B.SI_RCRD_NO,B.DOC_SEQUENCE_SI ) NOT IN (SELECT NVL(DOC_SER_REF,0),I_CODE,NVL(SI_RCRD_NO,0),NVL(DOC_SEQUENCE_SI,0) FROM IAS_RT_BILL_DTL )"SELECT NVL(SUM(NVL(B.FREE_QTY,0) * NVL(B.P_SIZE,1) ),0) FROM IAS_RT_BILL_MST_RQ A,IAS_RT_BILL_DTL_RQ B WHERE A.RT_BILL_SER = B.RT_BILL_SER AND B.RT_BILL_SER = :b1 AND NVL(B.BILL_SER,0) = NVL(:b2,0) AND B.I_CODE = :b3 AND NVL(B.SI_RCRD_NO,0) = NVL(:b4,0) AND NVL(B.RCRD_NO,0) != NVL(:b5,0) AND A.P_YEAR IN ( 0,3 )"
```
```sql
(select * from item_serialno where 1=2)
```
```sql
SELECT DECODE(:b1,1,NVL(V_A_NAME,V_E_NAME),NVL(V_E_NAME,V_A_NAME)),INACTIVE,V_A_CODE,BLK_LST FROM V_DETAILS WHERE V_CODE = :b2"
```
```sql
DELETE FROM OTHER_CHARGES_ITEMS_OTHRS WHERE BILL_TYPE = 3 AND BILL_NO = :b1 AND BILL_DOC_TYPE = :b2 AND BILL_SER = :b3 AND I_CODE = :b4 AND ITM_UNT = :b5"
```

## Other extracted strings (first 180)

- `ARST023`
- `YSERP_MNU.MMX`
- `AMERICAN_AMERICA.AR8MSWIN1256`
- `P7_19_NOV_201408_39_34`
- `SQLFORMS`
- `"PKG INIT"<anonymous>""`
- `P6_19_NOV_201408_39_34`
- `P0_10_SEP_201409_07_04`
- `P6_19_MAR_201410_50_09`
- `/LYSERP_LIB`
- `GEN_PKG`
- `P30_16_JAN_201412_58_41`
- `/NSPC17/PRE_FORM_PRC`
- `P29_16_JAN_201412_58_41`
- `"/NSPC17/WHEN_NEW_FORM_INSTANCE_PRC`
- `P28_16_JAN_201412_58_41`
- `/NSPC17/POST_FORMS_COMMIT_PRC`
- `P27_16_JAN_201412_58_41`
- `P26_16_JAN_201412_58_41`
- `P25_16_JAN_201412_58_41`
- `P24_16_JAN_201412_58_41`
- `P23_16_JAN_201412_58_41`
- `P22_16_JAN_201412_58_41`
- `P21_16_JAN_201412_58_41`
- `/NSPC17/EXIT_PROC`
- `P20_16_JAN_201412_58_41`
- `P19_16_JAN_201412_58_41`
- `STANDARD`
- `FORMS40`
- `VIEW_BTN`
- `SCREEN_BTN`
- `PRINT_DOC_BTN`
- `CANCEL_BTN`
- `PRINT_ST`
- `P18_16_JAN_201412_58_41`
- `Create -`
- `P17_16_JAN_201412_58_41`
- `FORMS4C`
- `/NSPC17/ADD_PROC`
- `BAR.ADD_Btn`
- `TRUE`
- `Delete -`
- `P16_16_JAN_201412_58_41`
- `/NSPC17/DELETE_PROC`
- `BAR.Del_Btn`
- `Accept -`
- `P15_16_JAN_201412_58_41`
- `/NSPC17/SAVE_PROC`
- `BAR.SAVE_Btn`
- `List -`
- `P14_16_JAN_201412_58_41`
- `/NSPC17/LIST_PROC`
- `BAR.LIST_BTN`
- `Search_`
- `P13_16_JAN_201412_58_41`
- `/NSPC17/KEY_ENTQRY`
- `BAR.SeeK_Btn`
- `Exceute -`
- `P12_16_JAN_201412_58_41`
- `/NSPC17/KEY_EXEQRY`
- `BAR.SEEK_BTN`
- `Print -`
- `P11_16_JAN_201412_58_41`
- `/NSPC17/CALL_SCR_PRINT`
- `BAR.Print_Btn`
- `P10_16_JAN_201412_58_41`
- `/NSPC17/WHEN_TIMER_EXPIRED_PRC`
- `P9_16_JAN_201412_58_41`
- `FORMS4W`
- `WIN7`
- `Ias_Rt_Bill_Mst_Rq.Dummy`
- `WIN9`
- `Bar.Dummy`
- `WIN10`
- `IAS_RT_BILL_DTL_RQ`
- `WIN11`
- `Ias_Rt_Bill_Dtl_Rq.I_Qty`
- `Win_Wt`
- `.I_Qty`
- `.Dummy`
- `WIN13`
- `P8_16_JAN_201412_58_41`
- `P4_12_FEB_201417_30_42`
- `!/NSPC17/WHEN_TAB_PAGE_CHANGED_PRC`
- `P5_16_JAN_201412_58_41`
- `P9_31_OCT_201505_19_12`
- `/NSPC17/CLEAR_ALL_MASTER_DETAILS`
- `ADD_PROC2`
- `"PKG INIT"ADD_PROC2"V_PK_VAL""`
- `ADD_F_DOC`
- `CALL_BTN_SCR`
- `CALL_BAR_BTN_SCR`
- `IAS_USR_PKG`
- `IAS20142`
- `YS_GEN_PKG`
- `"PKG INIT"CALL_BTN_SCR"P_SCR_TYP"V_APPROVED_PRV"V_VRFY_PRV"V_INACTIVE_PRV""`
- `CALL_OBJ.DUMMY`
- `Bar.Save_Btn`
- `FALSE`
- `IAS_USR_PKGIAS20142CHK_USR_FRM_PRVIA`
- `IAS_USR_PKGIAS20142GET_USR_APPRV`
- `Parameter.Vrfy_Flag`
- `Parameter.Blk`
- `INACTIVE`
- `YS_GEN_PKGIAS20142GET_USR_FLD_PRVYS_`
- `CALL_SCR_PRINT`
- `IAS_RT_BILL_MST_RQ`
- `"PKG INIT"CALL_SCR_PRINT"V_STAND_BY"<cursor ptr>"<SQL statement. Line 8>""`
- `Ftr.Err`
- `Prnt.rep_smple`
- `Prnt.screen_btn`
- `Prnt.view_btn`
- `Prnt.print_doc_btn`
- `SELECT NVL(STAND_BY,0) FROM IAS_RT_BILL_MST_RQ WHERE RT_BILL_SER = :b1 AND ROWNUM <= 1"`
- `VA_ITM_MOVE`
- `VA_FLD`
- `MS Sans Serif`
- `VA_PRMPT`
- `ADD_PROC`
- `/NSPC17/IAS_WT_PKG`
- `IAS_CHECK_SYS_PKG`
- `IAS_GEN_PKG`
- `/NSPC17/RT_BILL_DOC_TYPE_CHANGE`
- `/NSPC17/GET_RT_BILL_NO`
- `WAREHOUSE_DETAILS`
- `/NSPC17/MAKEARRAY`
- `"PKG INIT"ADD_PROC"CNT"A"B"D"CC"F_WC"I"<cursor ptr>"<SQL statement. Line 258>"<SQL statement. Line 263>""`
- `RollBack`
- `Ias_Rt_Bill_Mst_Rq.Brn_No`
- `Ias_Rt_Bill_Dtl_Rq.bill_no`
- `Ias_Rt_Bill_Mst_Rq.fsi`
- `Ias_Rt_Bill_Mst_Rq.rt_bill_doc_type`
- `Ias_Rt_Bill_Mst_Rq.rt_bill_no`
- `Ias_Rt_Bill_Mst_Rq.rt_bill_date`
- `Ias_Rt_Bill_Mst_Rq.rt_bill_currency`
- `Ias_Rt_Bill_Mst_Rq.rt_bill_rate`
- `Ias_Rt_Bill_Mst_Rq.C_Name`
- `Ias_Rt_Bill_Mst_Rq.a_desc`
- `Ias_Rt_Bill_Mst_Rq.ref_no`
- `Ias_Rt_Bill_Mst_Rq.Attach_cnt`
- `Ias_Rt_Bill_Mst_Rq.p_year`
- `Ias_Rt_Bill_Mst_Rq.r_code`
- `Ias_Rt_Bill_Mst_Rq.rep_code`
- `Ias_Rt_Bill_Mst_Rq.comm_per`
- `Ias_Rt_Bill_Mst_Rq.Sr_Type`
- `Ias_Rt_Bill_Mst_Rq.PRM_CODE`
- `Ias_Rt_Bill_Mst_Rq.Cc_Code`
- `Ias_Rt_Bill_Mst_Rq.Pj_No`
- `Ias_Rt_Bill_Dtl_Rq.Pj_No`
- `Ias_Rt_Bill_Mst_Rq.Actv_No`
- `Ias_Rt_Bill_Dtl_Rq.Actv_No`
- `Ias_Rt_Bill_Mst_Rq.Emp_No`
- `Ias_Rt_Bill_Dtl_Rq.Emp_No`
- `Ias_Rt_Bill_Mst_Rq.stock_rate`
- `Ias_Rt_Bill_Mst_Rq.classIfy_no`
- `Ias_Rt_Bill_Mst_Rq.Stand_By`
- `Other_Charges_Othrs.sc_no`
- `Other_Charges_Othrs.A_Code`
- `Other_Charges_Othrs.per`
- `Other_Charges_Othrs.amt`
- `Other_Charges_Othrs.clear`
- `IAS_CHECK_SYS_PKGIAS20142CHECK_INV_CLOSE`
- `Ias_Rt_Bill_Dtl_Rq.i_code`
- `Ias_Rt_Bill_Dtl_Rq.Itm_Unt`
- `Ias_Rt_Bill_Dtl_Rq.item_desc`
- `Ias_Rt_Bill_Dtl_Rq.w_code`
- `Ias_Rt_Bill_Mst_Rq.W_Code`
- `Ias_Rt_Bill_Dtl_Rq.i_qty`
- `Ias_Rt_Bill_Dtl_Rq.dis_per`
- `Ias_Rt_Bill_Dtl_Rq.dis_amt_dtl`
- `Ias_Rt_Bill_Mst_Rq.disc_amt_mst`
- `Ias_Rt_Bill_Dtl_Rq.dis_per2`
- `Ias_Rt_Bill_Dtl_Rq.dis_amt_dtl2`
- `Ias_Rt_Bill_Dtl_Rq.dis_per3`
- `Ias_Rt_Bill_Dtl_Rq.dis_amt_dtl3`
- `Ias_Rt_Bill_Mst_Rq.Field`
- `Ias_Rt_Bill_Dtl_Rq.FIELD_DTL`
- `Ias_Rt_Bill_Mst_Rq.discount`
- `Ias_Rt_Bill_Dtl_Rq.Cc_Code`
- `Ias_Rt_Bill_Dtl_Rq.V_Code`