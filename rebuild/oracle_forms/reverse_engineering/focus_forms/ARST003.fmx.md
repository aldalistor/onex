# ARST003.fmx

Total extracted strings: 4547

## Procedure-like names

- `ADD_PROC`
- `ATTACH_ITEM_PKG`
- `AUDIT_OTHR_PROC`
- `B4SAVE_PRC`
- `CHECK_DOC_DEL_PRC`
- `CHK_B4SAVE_DTL_PRC`
- `CHK_B4SAVE_MST_PRC`
- `DELETE_PROC`
- `DEL_DET_REC_PRC`
- `ENA_DIS_ITM_PRC`
- `EXIT_PROC`
- `FILL_ALL_LIST_PRC`
- `GEN_PKG`
- `GET_BL_CST_VND_FNC`
- `GET_BL_CST_VND_FNCIAS20142GET_BL_CST_VND_FNC`
- `IAS_ACTV_PKG`
- `IAS_ATTACH_ITM_PKG`
- `IAS_AUDIT_OTHR_PKG`
- `IAS_AUDIT_OTHR_PKGIAS20142AUDIT_OTHR_PRC`
- `IAS_AUD_SYS_PKG`
- `IAS_CHECK_SYS_PKG`
- `IAS_CST_PKG`
- `IAS_FETCH_DATA_PKG`
- `IAS_GEN_PKG`
- `IAS_GET_DOC_DEL_FNC`
- `IAS_GET_ENC_PASS_FNC`
- `IAS_GET_ENC_PASS_FNCIAS20142IAS_GET_ENC_PASS_FNC`
- `IAS_GET_ICODE_RESERVE_QTY_PRC`
- `IAS_GET_SALESDISC_PRC`
- `IAS_GET_SALESFREEQTY_PRC`
- `IAS_GET_SALESFREEQTY_PRCIAS20142IAS_GET_SALESFREEQTY_PRC`
- `IAS_INSRNCE_SCR_PKG`
- `IAS_ITM_PKG`
- `IAS_LAST_SALE_PRICE_PRC`
- `IAS_PJ_PKG`
- `IAS_PRMTR_PKG`
- `IAS_SMS_MAIL_PKG`
- `IAS_TRNS_PKG`
- `IAS_USR_PKG`
- `IAS_WCODE_PKG`
- `IAS_WEIGHT_SYS_PKG`
- `IAS_WEIGHT_SYS_PKGIAS20142CALC_AREA_SIZE_PRC`
- `IAS_WT_PKG`
- `INSRT_APPRVD_LVL_PRC`
- `KEY_LISTVAL_PRC`
- `LIST_PROC`
- `LOV_PKG`
- `LOV_TRG`
- `LYSERP_LIB`
- `POST_FORMS_COMMIT_PRC`
- `PRE_FORM_PRC`
- `PRINT_PROC`
- `SAVE_PROC`
- `SET_POS_PRC`
- `SHOW_BAT_COL1_IN_SCREEN`
- `SHOW_BAT_COL2_IN_SCREEN`
- `SHOW_BAT_COL3_IN_SCREEN`
- `SHOW_BAT_COL4_IN_SCREEN`
- `SHOW_BAT_COL5_IN_SCREEN`
- `SRCH_DTL_PKG`
- `SYS_SCREEN`
- `UPDATE_PROC`
- `WHEN_NEW_FORM_INSTANCE_PRC`
- `WHEN_TIMER_EXPIRED_PRC`
- `WIN_API`
- `YSERP_LIB`
- `YSERP_MNU`
- `YS_APPRVD_PKG`
- `YS_EMP_PKG`
- `YS_GEN_PKG`

## Referenced components

- `ARSI008.fmx`
- `ARSI021.FMX`
- `ARST024.FMX`
- `EXCEL.EXE`
- `Excel.exe`
- `GLST004.fmx`
- `YSERP_MNU.MMX`

## SQL-like strings

```sql
Create -
```
```sql
Delete -
```
```sql
Select 1 from Sales_Order
```
```sql
SELECT SO_TYPE,ORDER_CUR,C_CODE,C_NAME,A_DESC,CC_CODE,REF_DOC_NO,REF_DOC_DATE,DISC_AMT,DELIVERY_DATE,REP_CODE,PREPARE_DATE,REC_DEALER_DATE,W_CODE,BILL_DOC_TYPE,INSRNCE_CMP_NO,INSRNCE_BNF_NO,INSRNCE_PRSON_NM,INSRNCE_CARD_NO,INSRNCE_CARD_NO_FMLY,INSRNCE_RLTN_TYP,INSRNCE_CLSS_NO,INSRNCE_LOAD_PER,INSRNCE_DIAGNOSIS,INSRNCE_MDCL_NO,INSRNCE_CSH_AMT FROM SALES_ORDER WHERE ORDER_SER = :b1 AND ROWNUM <= 1"SELECT I_CODE,NVL(I_QTY,0) I_QTY,NVL(P_QTY,0) P_QTY,NVL(P_SIZE,0) P_SIZE,NVL(FREE_QTY,0) FREE_QTY,I_PRICE,I_PRICE_VAT,W_CODE,I_LENGTH,I_WIDTH,I_HEIGHT,I_NUMBER,DIS_PER,DIS_AMT_DTL,DIS_PER2,DIS_AMT_DTL2,DIS_PER3,DIS_AMT_DTL3,INSRNCE_LOAD_PER,INSRNCE_LOAD_AMT,INSRNCE_ADD_LMT FROM ORDER_DETAIL WHERE ORDER_SER = :b1 ORDER BY RCRD_NO FOR UPDATE NOWAIT"SELECT DISC_AMT FROM SALES_ORDER WHERE ORDER_SER = :b1"
```
```sql
Select 1 From Ias_Chk_Doc_Inout_Qty_Mst
```
```sql
ROLLBACK
```
```sql
Select 1 From Sales_Order where ORDER_SER =
```
```sql
Select 1 From Ias_Bill_Dtl
```
```sql
SELECT CREDIT_PERIOD FROM CUSTOMER WHERE C_CODE = :b1"
```
```sql
Select 1 From Ias_Reserve_Mst Where Doc_Type_Ref = 53 And Doc_Ser_Ref=
```
```sql
UPDATE QUOTATION SET PROCESSED=1 WHERE QUOT_SER = :b1"UPDATE IAS_ITM_ATTACH_OTHRS SET C_CODE=:b1,CC_CODE=:b2,REP_CODE=:b3 WHERE DOC_TYPE = 2 AND DOC_SER = :b4"SELECT NVL(MAX(DOC_NO),0) + 1 FROM IAS_RESERVE_MST WHERE DOC_TYPE_REF = 53"INSERT INTO IAS_RESERVE_MST ( DOC_TYPE,DOC_NO,DOC_SER,DOC_NO_REF,DOC_TYPE_REF,DOC_SER_REF,C_CODE,BRN_NO,CMP_NO,BRN_USR,BRN_YEAR ) VALUES ( 2,:b1,:b2,:b3,53,:b4,:b5,:b6,:b7,:b8,:b9 )"UPDATE IAS_RESERVE_MST SET C_CODE=:b1 WHERE DOC_TYPE_REF = 53 AND DOC_SER_REF = :b2"
```
```sql
Select 1 From Gr_Detail
```
```sql
Select 1 From Ias_Bill_Dtl where Doc_Type_Ref=53 And Doc_Ser_Ref=
```
```sql
UPDATE QUOTATION SET PROCESSED=0 WHERE QUOT_SER = :b1 AND QUOT_SER NOT IN (SELECT QUOT_SER FROM SALES_ORDER WHERE ORDER_SER != :b2 AND QUOT_SER IS NOT NULL )"DELETE FROM IAS_RESERVE_MST WHERE DOC_TYPE_REF = 53 AND DOC_SER_REF = :b1"
```
```sql
INSERT
```
```sql
SELECT IAS_DOC_SEQ_OTHR.NEXTVAL FROM DUAL"UPDATE IAS_RESERVE_DTL SET W_CODE=NVL(:b1,:b2),I_QTY=NVL(:b3,0) + NVL(:b4,0) ,P_QTY=NVL(:b5,0) + (NVL(:b6,0) * NVL(:b7,1) ) ,CP_QTY=:b5,T_DATE=:b9,RESERVED=NVL(:b10,0),EXPIRE_DATE=NVL(:b11,''01/01/1900''),BATCH_NO=NVL(:b12,''0''),UP_U_ID=:b13,UP_DATE=:b14 WHERE DOC_TYPE_REF = 53 AND DOC_SER_REF = :b15 AND I_CODE = :b16 AND RCRD_NO = :b17"
```
```sql
Exists(Select 1 From Ias_Shw_Doc_Priv
```
```sql
UPDATE ORDER_DETAIL SET SO_TYPE=:b1,SI_TYPE=:b2 WHERE ORDER_SER = :b3"UPDATE OTHER_CHARGES_OTHRS SET BILL_DOC_TYPE=:b1 WHERE BILL_SER = :b2"UPDATE OTHER_CHARGES_ITEMS_OTHRS SET BILL_DOC_TYPE=:b1 WHERE BILL_SER = :b2"
```
```sql
Select Brn_no||'-'||Decode(
```
```sql
Or Exists (select 1 From S_Brn_Usr_Priv
```
```sql
Select To_Char(Pj_No)||'- '||Decode(
```
```sql
Exists(Select Pj_No From Ias_Priv_Projects
```
```sql
Select To_Char(Actv_No)||'- '||Decode(
```
```sql
Exists(Select Actv_No From Ias_Priv_Actvty
```
```sql
Select To_Char(PRM_CODE)||'-'||Decode(
```
```sql
Select To_Char(SI_Type)||'- '||Decode(
```
```sql
Where Exists(Select S_Type From Ias_Priv_Ar
```
```sql
Select To_Char(Driver_No)||'-'||Decode(
```
```sql
Select '','None' from dual Union Select Cur_No||'- '||cur_code,cur_code from ex_rate
```
```sql
Select To_Char(So_Type)||'- '||Decode(
```
```sql
=1) Or Exists(Select S_Type From Ias_Priv_Ar
```
```sql
SELECT TO_CHAR(W_CODE)||'- '||DECODE(
```
```sql
=1) OR EXISTS (SELECT W_CODE FROM PRIVILEGE_WH
```
```sql
Select to_char(lev_no)||'- '||
```
```sql
Or (EXISTS (SELECT lev_no
```
```sql
Select To_Char(Type_Of_Item)||'-'||Decode(
```
```sql
Select measure, To_Char(measure_code)
```
```sql
Select R_code||'- '||Decode(
```
```sql
Select Reprs_Code||'- '||Decode (
```
```sql
(Exists (Select 1 From IAS_PRIV_SMAN
```
```sql
Select to_Char(Emp_No)||'- '||Decode(
```
```sql
Select Ins_Cmp_No||'-'||Decode(
```
```sql
Select Bnf_No||'-'||Decode(
```
```sql
Select MDCL_NO||'-'||Decode(
```
```sql
RollBack
```
```sql
Select 1 From order_detail where Order_Ser =
```
```sql
Select 1 From Sales_Charges Where RowNum<=1
```
```sql
Select 1 From Sales_Charges Where Nvl(Inv_Item,0)=1 And RowNum<=1
```
```sql
SELECT DOC_GEN_AR,DATE_GEN_AR,DATE_SETTING_AR,NVL(CUST_PRICE_FIXED,0),NVL(PRICE_TYPE,1),AR_WC_TYPE,CONN_AC_CC,NVL(CHECK_LMT_SORDER,1),NVL(SHOW_I_SIZE,0),NVL(SALES_DUPLICATE_ITEM,1),NVL(CONN_ITM_ACT_BY_USR_PRIV,0),NVL(SHOW_SUM_QTY_IN_SALES,0),NVL(YS_GEN_PKG.CHK_ACTV_SYSTEM(130,:b1),0),NVL(IAS_PARA_AR.ALLOW_DOC_DEL_NO,1),NVL(USE_STAND_BY,0),NVL(USE_WT_SYS,0),DFLT_WT_ARGMNT,NVL(ADD_PORDER_QTY,0),NVL(USE_AUDIT_DOC,0),NVL(USE_PROJECTS,0),NVL(IAS_PARA_AR.SI_DUE_DATE_MANDTRY,0),NVL(IAS_PARA_AR.PAID_INSTLLMNT_MAN,0),NVL(ALLOW_SO_WITHOUT_STORE,0),NVL(MAX_RESERVE_SO_ITM_DAY,0),NVL(SALES_FREE_QTY_TYPE,0),NVL(SALES_FREE_QTY_WCODE,0),NVL(SI_HIDE_FREE_PER,0),NVL(SHOW_SUM_QTY_IN_SALES,0),NVL(AR_USE_FREE_QTY,0),NVL(SO_EXP_DATE_MANDTRY,0),NVL(DATE_GEN_AR,1),NVL(MAX_PRD_EXP_DATE_SO_DAY,0),NVL(P_RATE_TYPE,0),NVL(USE_ITM_ATTACH,0),NVL(IAS_PARA_GEN.USE_ACTVTY,0),NVL(CONN_SO_INC,0),NVL(MAX_PRD_RESERVE_SO_DAY,0),NVL(YS_GEN_PKG.CHK_ACTV_SYSTEM(160,:b1),0),NVL(YS_GEN_PKG.CHK_ACTV_SYSTEM(85,:b1),0),NVL(USE_OUT_BILLS,0),NVL(USE_EXPIRE_DATE,0),NVL(USE_BATCH_NO,0),NVL(USE_VAT,0),NVL(SHOW_DISC_PER_ITEMS_AR,0),NVL(CALC_VAT_AMT_TYPE,0),TO_DATE(ACTIVE_VAT_DATE,''DD/MM/YYYY''),COSTING_TYPE,CREDIT_LIMIT_TYPE,AR_SORDER_SERIAL,USE_ALT_CODE,WTAVG_TYPE,REQUEST_REFNO_AR,REQUEST_DESC_AR,NVL(USE_SALE_INVOICE_AS_POS,0),NVL(USE_COSTING,0),NVL(SHW_ADD_FLD_IN_DTL,0),USE_KIMB,INPT_UNT_FACTOR_WT,1 INV_SYS_TYPE,NVL(USE_APPRV_SLS_MNG_SO,0),NVL(CONN_ITEM_BY_ATTACH_AUTO,0),NVL(SHOW_ITM_BARCODE_AR,0),BATCHNO_COL_NO,SIZE_COL1,SIZE_COL2,SIZE_COL3,SIZE_COL4,SIZE_COL5,BATCH_NM_COL1,BATCH_NM_COL2,BATCH_NM_COL3,BATCH_NM_COL4,BATCH_NM_COL5,SHOW_BAT_COL1_IN_SCREEN,SHOW_BAT_COL2_IN_SCREEN,SHOW_BAT_COL3_IN_SCREEN,SHOW_BAT_COL4_IN_SCREEN,SHOW_BAT_COL5_IN_SCREEN,SALES_DISC_TYPE,SALES_DISC_PERIODIC,SALES_DISC_WCODE,NVL(SI_HIDE_DISC_PER,0),NVL(CHK_LMT_PRICE_MINUS_DISC_SI,1),NVL(IT
```
```sql
M_DISC_TYPE,1),NVL(DISC_COUNT,1),NVL(AR_EMP_TYPE,0),NVL(USE_SUB_CUSTOMER,0),NVL(SC_ITEM_TYPE,0),NVL(SC_WCODE_TYPE,1),NVL(IAS_PARA_AR.USE_SC_PRD,0),NVL(NO_OF_DECIMAL_AR,2),NVL(USE_PRICE_INCLUDE_VAT,0),NVL(SHW_ALL_ITM_UNT_TRNS,0),NVL(USE_INSRNCE_SYS,0),NVL(USE_SO_PRPRTON_QTY,0),NVL(CALC_MEASUR_PRICE,0),NVL(SHOW_WT_UNT_ARGMNT_IN_TRNS,0) FROM IAS_PARA_AR,IAS_PARA_INV,IAS_PARA_GL,IAS_PARA_GEN"SELECT NVL(ALLOW_PRNTDOC_BFR_AUDIT_REF,0),NVL(ALLOW_CANCEL_AUDIT_REF,0),NVL(PMANUAL,1),NVL(HIDE_COST,1),NVL(PLIMIT_TYPE,2),NVL(L_PRICE,2),NVL(H_PRICE,2),NVL(USER_VIEW_DOC_ENTR,0),NVL(CHECK_CST_BLK_LST,0),NVL(CHECK_CREDIT_PERIOD,0),NVL(AR_UPDATE_FREE_QTY,1),NVL(ALLOW_BILL_FQTY_ONLY,1),PLIMIT_PER,NVL(AR_UPDATE_ITM_ALLOW_DISC,0),NVL(AR_UPDATE_ITM_NOT_ALLOW_DISC,0),NVL(SHOW_STKCOST_INQTY_DISC_FQTY,0),NVL(FILL_STKCOST_INTO_PRICE_SI,0),ALLOW_UPD_QTY_WT,NVL(MAX_DISC_ITM_PER,0),NVL(MOD_ITM_DISC,0) FROM PRIVILEGE_FIXED WHERE U_ID = :b1"SELECT NVL(PRINT_FLAG,0),NVL(VWREP_FLAG,0) FROM PRIVILEGE WHERE FORM_NO = :b1 AND U_ID = :b2 AND ROWNUM <= 1"SELECT NVL(VRFY_FLAG,0) FROM PRIVILEGE WHERE FORM_NO = :b1 AND U_ID = :b2 AND ROWNUM <= 1"SELECT NVL(HEAD_OFFICE,0),NVL(LGHT_SHOW_QTY_ALL_WCODE,0) FROM IAS_PARA_LGHT,IAS_PARA_AR"SELECT NVL(PRINT_FLAG,0),NVL(VWREP_FLAG,0) FROM PRIVILEGE WHERE FORM_NO = :b1 AND U_ID = :b2 AND ROWNUM <= 1"SELECT PATH_EXCEL FROM IAS_PARA_GEN"
```
```sql
SELECT NVL(MAX(TO_NUMBER(ORDER_NO)),0) + 1 FROM SALES_ORDER WHERE BRN_NO = :b1"SELECT NVL(MAX(TO_NUMBER(ORDER_NO)),0) + 1 FROM SALES_ORDER WHERE SO_TYPE = :b1 AND BRN_NO = :b2"
```
```sql
SELECT CRLIMIT_TYPE,NVL(INV_DIS_LMT,0),CRLIMIT_PER FROM PRIVILEGE_FIXED WHERE U_ID = :b1"
```
```sql
SELECT NVL(AR_ALLOW_SALES_PRV_DR,0),NVL(AR_CHK_PRD_AFTR_DUE,0) FROM PRIVILEGE_FIXED WHERE U_ID = :b1"SELECT CREDIT_PERIOD,DECODE(NVL(:b1,0),1,ALLOW_PRD_AFTR_DUE,0) FROM CUSTOMER WHERE C_CODE = :b2"SELECT MIN(I_DATE) FROM INSTALLMENT WHERE C_CODE = :b1 AND BILL_SER != NVL(:b2,-1) AND TRUNC(NVL(I_AMT,0)) > (TRUNC(NVL(PAID_AMT,0) + NVL(ADJ_AMT,0) ) + 1 ) AND DR_NO IS NULL"SELECT MIN(DOC_DATE) FROM IAS_DR_DET_TMP WHERE C_CODE = :b1 AND DOC_SER != NVL(:b2,-1) AND TRUNC(NVL(I_AMT,0)) > (TRUNC(NVL(PAID_AMT,0)) + 1 )"
```
```sql
SELECT 1 FROM ORDER_DETAIL WHERE ORDER_SER = :b1 AND I_CODE = :b2 AND ITM_UNT = :b3 AND W_CODE = NVL(:b4,:b5) AND NVL(EXPIRE_DATE,''01/01/1900'') = NVL(:b6,''01/01/1900'') AND NVL(BATCH_NO,''0'') = NVL(:b7,''0'') AND NVL(I_LENGTH,0) = NVL(:b8,0) AND NVL(I_WIDTH,0) = NVL(:b9,0) AND NVL(I_HEIGHT,0) = NVL(:b10,0) AND NVL(I_NUMBER,0) = NVL(:b11,0) AND BRN_NO = :b12 AND ROWNUM <= 1"
```
```sql
Select count(1) from Sales_Order,Order_DETAIL
```
```sql
SELECT I_CODE FROM ORDER_DETAIL WHERE ORDER_SER = :b1 ORDER BY RCRD_NO"
```
```sql
Select Count(1) From Ias_Attach_Mst
```
```sql
Select 1 From Ias_Attach_Mst
```
```sql
Select To_Char(Ias_Attach_Dtl.Attch_Desc_No)||' -'||Decode(
```
```sql
And Exists(Select 1 From Ias_Itm_Attach Where i_code='
```
```sql
Select Distinct To_Char(Attch_Desc_No
```
```sql
SELECT IAS_RCRD_ATTCH.NEXTVAL FROM DUAL"SELECT FLEX_NO,FLEX_FIELD,ATTCH_DESC_NO1,ATTCH_DESC_NO2,ATTCH_DESC_NO3,ATTCH_DESC_NO4,ATTCH_DESC_NO5 FROM IAS_ITM_ATTACH WHERE FLEX_NO = :b1 AND ROWNUM <= 1"SELECT IAS_ATTCH_SEQ.NEXTVAL FROM DUAL"SELECT FLEX_FIELD FROM IAS_ITM_ATTACH WHERE FLEX_NO = :b1"SELECT DECODE(:b1,1,NVL(ATTCH_A_NAME,ATTCH_E_NAME),NVL(ATTCH_E_NAME,ATTCH_A_NAME)) ATTCH_NAME FROM IAS_ATTACH_MST WHERE ATTCH_NO = :b2"SELECT I_CODE,W_CODE,FLEX_NO,ATTCH_DESC_NO1,ATTCH_DESC_NO2,ATTCH_DESC_NO3,ATTCH_DESC_NO4,ATTCH_DESC_NO5,SUM(QTY) QTY,SUM(RSRV_QTY) RSRV_QTY FROM (SELECT I_CODE,W_CODE,FLEX_NO,ATTCH_DESC_NO1,ATTCH_DESC_NO2,ATTCH_DESC_NO3,ATTCH_DESC_NO4,ATTCH_DESC_NO5,0 QTY,NVL(SUM(NVL(CP_QTY,0)),0) / NVL(P_SIZE,1) RSRV_QTY FROM IAS_ITM_ATTACH_OTHRS A WHERE EXISTS (SELECT I_CODE FROM IAS_ITM_ATTACH_OTHRS WHERE DOC_TYPE = 2 AND DOC_SER = :b1 AND I_CODE = A.I_CODE AND W_CODE = A.W_CODE AND FLEX_NO = A.FLEX_NO AND NVL(RESERVED,0) = 1 AND ROWNUM <= 1 ) GROUP BY I_CODE,W_CODE,FLEX_NO,ATTCH_DESC_NO1,ATTCH_DESC_NO2,ATTCH_DESC_NO3,ATTCH_DESC_NO4,ATTCH_DESC_NO5,EXPIRE_DATE,BATCH_NO,P_SIZE UNION ALL SELECT I_CODE,W_CODE,FLEX_NO,ATTCH_DESC_NO1,ATTCH_DESC_NO2,ATTCH_DESC_NO3,ATTCH_DESC_NO4,ATTCH_DESC_NO5,NVL(SUM(IN_OUT * (NVL(P_QTY,0) + NVL(PF_QTY,0) ) ),0) QTY,0 RSRV_QTY FROM IAS_ITM_ATTACH_MOVEMENT A WHERE EXISTS (SELECT I_CODE FROM IAS_ITM_ATTACH_OTHRS WHERE DOC_TYPE = 2 AND DOC_SER = :b1 AND I_CODE = A.I_CODE AND W_CODE = A.W_CODE AND FLEX_NO = A.FLEX_NO AND ROWNUM <= 1 ) AND EXISTS (SELECT I_CODE,W_CODE FROM ORDER_DETAIL WHERE ORDER_SER = :b1 AND I_CODE = A.I_CODE AND W_CODE = A.W_CODE AND NVL(RESERVED,0) = 1 AND ROWNUM <= 1 ) GROUP BY I_CODE,W_CODE,FLEX_NO,ATTCH_DESC_NO1,ATTCH_DESC_NO2,ATTCH_DESC_NO3,ATTCH_DESC_NO4,ATTCH_DESC_NO5,EXPIRE_DATE,BATCH_NO ) GROUP BY I_CODE,W_CODE,FLEX_NO,ATTCH_DESC_NO1,ATTCH_DESC_NO2,ATTCH_DESC_NO3,ATTCH_DESC_NO4,ATTCH_DESC_NO5 HAVING SUM(QTY) - SUM(RSRV_QTY) < 0"SELECT ROUND(NVL(SUM((NVL(P_QTY,0) + NVL(PF_QTY,0) )),0),4) FROM IAS_ITM_ATTACH_OTHRS A WHERE DOC_SER = :b1 AND FLEX_NO = :b2 AND W_CODE = :b3"SELECT I_CODE FROM (SELECT I_CODE,W_CODE,REC_ATTCH,SUM(NVL(I_QTY,0)) QTY,SUM(NVL(FREE_QTY,0)) FQTY FROM ORDER_DETAIL WHERE ORDER_SER = :b1 AND NVL(ORDER_DETAIL.USE_ATTCH,0) = 1 GROUP BY I_CODE,W_CODE,REC_ATTCH MINUS SELECT I_CODE,W_CODE,REC_ATTCH,SUM(NVL(I_QTY,0)) QTY,SUM(NVL(FREE_QTY,0)) FQTY FROM IAS_ITM_ATTACH_OTHRS WHERE DOC_TYPE = 2 AND DOC_SER = :b1 GROUP BY I_CODE,W_CODE,REC_ATTCH ) WHERE ROWNUM <= 1"SELECT I_CODE FROM (SELECT I_CODE,W_CODE,REC_ATTCH,SUM(NVL(I_QTY,0)) QTY,SUM(NVL(FREE_QTY,0)) FQTY FROM IAS_ITM_ATTACH_OTHRS WHERE DOC_TYPE = 2 AND DOC_SER = :b1 GROUP BY I_CODE,W_CODE,REC_ATTCH MINUS SELECT I_CODE,W_CODE,REC_ATTCH,SUM(NVL(I_QTY,0)) QTY,SUM(NVL(FREE_QTY,0)) FQTY FROM ORDER_DETAIL WHERE ORDER_SER = :b1 AND NVL(ORDER_DETAIL.USE_ATTCH,0) = 1 GROUP BY I_CODE,W_CODE,REC_ATTCH ) WHERE ROWNUM <= 1"SELECT DECODE(:b1,1,NVL(ATTCH_A_NAME,ATTCH_E_NAME),NVL(ATTCH_E_NAME,ATTCH_A_NAME)) ATTCH_NAME FROM IAS_ATTACH_MST WHERE ATTCH_NO = :b2"SELECT FLEX_NO,FLEX_FIELD,ATTCH_NO1,ATTCH_NO2,ATTCH_NO3,ATTCH_NO4,ATTCH_NO5 FROM IAS_ITM_ATTACH WHERE I_CODE = :b1 AND NVL(ATTCH_DESC_NO1,0) = NVL(:b2,0) AND NVL(ATTCH_DESC_NO2,0) = NVL(:b3,0) AND NVL(ATTCH_DESC_NO3,0) = NVL(:b4,0) AND NVL(ATTCH_DESC_NO4,0) = NVL(:b5,0) AND NVL(ATTCH_DESC_NO5,0) = NVL(:b6,0) AND ROWNUM <= 1"SELECT I_CODE,P_SIZE,ATTCH_NO1,ATTCH_DESC_NO1,ATTCH_NO2,ATTCH_DESC_NO2,ATTCH_NO3,ATTCH_DESC_NO3,ATTCH_NO4,ATTCH_DESC_NO4,ATTCH_NO5,ATTCH_DESC_NO5,FLEX_NO,ATTCH_NOTE,EXPIRE_DATE,BATCH_NO,W_CODE,I_QTY,FREE_QTY,FLEX_FIELD,C_CODE,RCRD_NO,PREPARE_DATE,DELIVERY_DATE FROM IAS_ITM_ATTACH_OTHRS WHERE DOC_TYPE = 1 AND DOC_SER = :b1 AND REC_ATTCH = :b2 ORDER BY RCRD_NO"UPDATE IAS_ITM_ATTACH_OTHRS A SET ( RESERVED,T_DATE )=(SELECT RESERVED,RES_DATE FROM ORDER_DETAIL WHERE ORDER_SER = A.DOC_SER AND A.REC_ATTCH = A.REC_ATTCH ) WHERE DOC_TYPE = 2 AND DOC_SER = :b1"SELECT FLEX_NO FROM IAS_ITM_ATTACH WHERE FLEX_FIELD = :b1"SELECT FLEX_FIELD FROM IAS_ITM_ATTACH WHERE FLEX_NO = :b1"SELECT 1 FROM IAS_ITM_ATTACH WHERE FLEX_FIELD = :b1 AND FLEX_NO = :b2"SELECT IAS_ATTCH_SEQ.NEXTVAL FROM DUAL"INSERT INTO IAS_ITM_ATTACH ( I_CODE,ATTCH_NO1,ATTCH_DESC_NO1,ATTCH_NO2,ATTCH_DESC_NO2,ATTCH_NO3,ATTCH_DESC_NO3,ATTCH_NO4,ATTCH_DESC_NO4,ATTCH_NO5,ATTCH_DESC_NO5,FLEX_FIELD,FLEX_NO,AD_U_ID,AD_DATE,UP_U_ID,UP_DATE ) VALUES ( :b1,:b2,:b3,:b4,:b5,:b6,:b7,:b8,:b9,:b10,:b11,:b12,:b13,:b14,:b15,:b16,:b17 )"SELECT ATTCH_NO1,ATTCH_DESC_NO1,ATTCH_NO2,ATTCH_DESC_NO2,ATTCH_NO3,ATTCH_DESC_NO3,ATTCH_NO4,ATTCH_DESC_NO4,ATTCH_NO5,ATTCH_DESC_NO5 FROM IAS_ITM_ATTACH WHERE FLEX_FIELD = :b1"
```
```sql
SELECT SUM(((NVL(I_QTY,0) - NVL(OUT_QTY,0) ) + (NVL(FREE_QTY,0) + NVL(OUT_FREE_QTY,0) ) ) * NVL(P_SIZE,1) ) FROM IAS_BILL_MST,IAS_BILL_DTL WHERE IAS_BILL_MST.BILL_SER = IAS_BILL_DTL.BILL_SER AND I_CODE = :b1 AND IAS_BILL_DTL.W_CODE = :b2 AND NVL(EXPIRE_DATE,''01/01/1900'') = NVL(:b3,''01/01/1900'') AND NVL(BATCH_NO,''0'') = NVL(:b4,''0'') AND ((NVL(I_QTY,0) > NVL(OUT_QTY,0) ) OR (NVL(FREE_QTY,0) > NVL(OUT_FREE_QTY,0) ))"SELECT SUM(((NVL(I_QTY,0) - NVL(RET_QTY,0) ) + (NVL(FREE_QTY,0) + NVL(RET_FREE_QTY,0) ) ) * NVL(P_SIZE,1) ) FROM IAS_RT_BILL_MST,IAS_RT_BILL_DTL WHERE IAS_RT_BILL_MST.RT_BILL_SER = IAS_RT_BILL_DTL.RT_BILL_SER AND I_CODE = :b1 AND IAS_RT_BILL_DTL.W_CODE = :b2 AND NVL(EXPIRE_DATE,''01/01/1900'') = NVL(:b3,''01/01/1900'') AND NVL(BATCH_NO,''0'') = NVL(:b4,''0'') AND ((NVL(I_QTY,0) > NVL(RET_QTY,0) ) OR (NVL(FREE_QTY,0) > NVL(RET_FREE_QTY,0) ))"SELECT NVL(SUM(NVL(CP_QTY,0)),0) / NVL(:b1,1) FROM IAS_RESERVE_DTL WHERE I_CODE = :b2 AND NVL(W_CODE,0) = DECODE(:b3, NULL ,NVL(W_CODE,0),:b3) AND NVL(EXPIRE_DATE,''01/01/1900'') = :b5 AND NVL(BATCH_NO,''0'') = :b6 AND NVL(DOC_SER_REF,0) = :b7 AND RCRD_NO != :b8 AND T_DATE >= :b9 AND NVL(RESERVED,0) = 1 AND NVL(CP_QTY,0) >= 0"SELECT NVL(SUM(NVL(CP_QTY,0)),0) FROM IAS_RESERVE_DTL WHERE I_CODE = :b1 AND NVL(W_CODE,0) = NVL(:b2,NVL(W_CODE,0)) AND NVL(EXPIRE_DATE,''01/01/1900'') = NVL(:b3,''01/01/1900'') AND NVL(BATCH_NO,''0'') = NVL(:b4,''0'') AND NVL(C_CODE,''0'') != DECODE(:b5, NULL ,NVL(C_CODE,''0''),:b5) AND NVL(DOC_TYPE_REF,0) = 53 AND NVL(DOC_SER_REF,0) = :b7 AND NVL(RCRD_NO,0) = :b8 AND NVL(RESERVED,0) = 1"
```
```sql
Select to_Char(Type_Of_Item)||'- '||Decode(
```
```sql
SELECT (SUM(NVL(P_QTY,0) + (NVL(FREE_QTY,0) * NVL(P_SIZE,1) ) ) - SUM(NVL(PI_QTY,0) / NVL(P_SIZE,1) ) ) FROM P_ORDER A,P_ORDER_DETAIL B WHERE A.PO_SER = B.PO_SER AND NVL(A.PO_CLOSED,0) = 0 AND B.I_CODE = :b1"
```
```sql
SELECT MAX(RCRD_NO) FROM ORDER_DETAIL WHERE ORDER_SER = :b1"
```
```sql
SELECT B.C_CODE,B.A_CY,B.BILL_NO,B.DOC_TYPE,B.BILL_DOC_TYPE,B.BILL_SER,B.I_NO,B.I_DATE,B.I_AMT,(NVL(B.I_AMT,0) * NVL(B.AC_RATE,1) ) I_AMT_LOC,B.AC_RATE,B.BRN_NO,B.BRN_YEAR,B.CMP_NO,B.BRN_USR FROM CUSTOMER A,INSTALLMENT B WHERE A.C_CODE = B.C_CODE AND B.C_CODE = :b1 AND B.I_DATE <= :b2 AND B.DR_NO IS NULL AND ((:b3 = 1 ) OR (((:b4 = 1 ) AND EXISTS (SELECT 1 FROM PRIV_ACC WHERE U_ID = :b3 AND A_CODE = A.C_A_CODE AND A_CY = B.A_CY AND NVL(ADD_FLAG,0) = 1 AND ROWNUM <= 1 ) ) OR ((:b4 = 2 ) AND EXISTS (SELECT 1 FROM IAS_PRIV_CUSTOMER WHERE U_ID = :b3 AND C_CODE = A.C_CODE AND A_CY = B.A_CY AND NVL(ADD_FLAG,0) = 1 AND ROWNUM <= 1 ) )))ORDER BY I_DATE,DOC_TYPE,BILL_NO"SELECT B.C_CODE,B.DOC_DATE,B.CR_AMT CR_AMT FROM CUSTOMER A,IAS_POST_DTL B WHERE A.C_CODE = B.C_CODE AND NVL(CR_AMT,0) > 0 AND B.C_CODE = :b1 AND B.DOC_DATE <= :b2 AND ((:b3 = 1 ) OR (((:b4 = 1 ) AND EXISTS (SELECT 1 FROM PRIV_ACC WHERE U_ID = :b3 AND A_CODE = A.C_A_CODE AND A_CY = B.A_CY AND NVL(ADD_FLAG,0) = 1 AND ROWNUM <= 1 ) ) OR ((:b4 = 2 ) AND EXISTS (SELECT 1 FROM IAS_PRIV_CUSTOMER WHERE U_ID = :b3 AND C_CODE = A.C_CODE AND A_CY = B.A_CY AND NVL(ADD_FLAG,0) = 1 AND ROWNUM <= 1 ) )))ORDER BY B.C_CODE,B.DOC_DATE"INSERT INTO IAS_DR_DET_TMP ( C_CODE,A_CY,DOC_NO,DOC_TYPE,DOC_JV_TYPE,DOC_SER,I_NO,DOC_DATE,I_AMT,I_AMT_LOC,BILL_RATE,PAID_AMT,PAID_AMT_LOC,PAID_DATE,PAID,TRMNL_NAME,BRN_NO,BRN_YEAR,CMP_NO,BRN_USR ) VALUES ( :b1,:b2,:b3,:b4,:b5,:b6,:b7,:b8,:b9,:b10,:b11, NULL , NULL , NULL ,0,:b12,:b13,:b14,:b15,:b16 )"SELECT DOC_SER,I_NO,DOC_DATE,I_AMT_LOC,PAID_AMT_LOC,PAID_DATE,PAID FROM IAS_DR_DET_TMP WHERE NVL(PAID,0) = 0 AND C_CODE = :b1 ORDER BY DOC_DATE,DOC_SER"UPDATE IAS_DR_DET_TMP SET PAID_AMT_LOC=I_AMT_LOC,PAID_AMT=ROUND((I_AMT_LOC / BILL_RATE ),:b1),PAID=1,PAID_DATE=:b2 WHERE DOC_SER = :b3"UPDATE IAS_DR_DET_TMP SET PAID_AMT_LOC=I_AMT_LOC,PAID_AMT=ROUND((I_AMT_LOC / BILL_RATE ),:b1),PAID=1,PAID_DATE=:b2 WHERE DOC_SER = :b3"UPDATE IAS_DR_DET_TMP SET PAID_AMT_LOC=:b1,PAID_AMT=ROUND((:b1 / BILL_RATE ),:b3),PAID_DATE=:b4 WHERE DOC_SER = :b5"
```
```sql
SELECT LEV_NO FROM CUSTOMER_CURR WHERE C_CODE = :b1 AND A_CY = :b2"
```
```sql
Select Excel(.Xls) File
```
```sql
SELECT 1,USE_BATCH_NO,USE_EXP_DATE FROM IAS_ITM_MST WHERE I_CODE = LTRIM(RTRIM(UPPER(:b1))) AND ROWNUM <= 1"SELECT BATCH_NO,BATCH_NO FROM IAS_SUB_CST_MEASUR_MST WHERE C_CODE = :b1 AND SUB_C_CODE = :b2 AND I_CODE = :b3"SELECT EXPIRE_DATE,BATCH_NO,SUM(TRUNC(CP_QTY / NVL(:b1,1) )) CP_QTY FROM GR_DETAIL WHERE I_CODE = LTRIM(RTRIM(UPPER(:b2))) AND W_CODE = NVL(NVL(:b3,NVL(:b4,:b5)),W_CODE) AND NVL(CP_QTY,0) > 0 AND (EXPIRE_DATE != ''01/01/1900'' OR BATCH_NO != ''0'' ) AND TO_DATE(EXPIRE_DATE,''DD/MM/YYYY'') >= TO_DATE(SYSDATE,''DD/MM/YYYY'') + NVL(:b6,0) GROUP BY EXPIRE_DATE,BATCH_NO ORDER BY EXPIRE_DATE,BATCH_NO"
```
```sql
SELECT SUM(NVL(I_QTY,0) + NVL(FREE_QTY,0) ) FROM ORDER_DETAIL WHERE I_CODE = :b1 AND W_CODE = :b2 AND RES_DATE >= :b3 AND NVL(RESERVED,0) = 1"
```
```sql
SELECT NVL(CR_LIMIT,0) FROM CUSTOMER_CURR WHERE C_CODE = :b1 AND A_CY = :b2"SELECT 1 FROM PRIV_ACC WHERE U_ID = :b1 AND A_CODE = IAS_CST_PKG.GET_ACODE(:b2) AND NVL(VIEW_FLAG,0) = 1 AND ROWNUM <= 1"SELECT 1 FROM IAS_PRIV_CUSTOMER WHERE U_ID = :b1 AND C_CODE = :b2 AND NVL(VIEW_FLAG,0) = 1 AND ROWNUM <= 1"SELECT TO_CHAR(MAX(DOC_DATE),''dd/mm/yyyy'') FROM IAS_POST_DTL WHERE C_CODE = :b1 AND DOC_TYPE IN ( 1,2 ) AND NVL(CR_AMT,0) > 0"
```
```sql
Select 1 From QUOTATION
```
```sql
Select 1 From other_charges_Othrs
```
```sql
SELECT 1 FROM QUOTATION WHERE QUOT_NO = :b1 AND ((:b2 = 1 ) OR EXISTS (SELECT 1 FROM S_BRN_USR_PRIV WHERE U_ID = :b2 AND S_BRN_USR_PRIV.BRN_NO = QUOTATION.BRN_NO AND NVL(FILL_FLAG,1) = 1 AND ROWNUM <= 1 ) ) AND ROWNUM <= 1"SELECT QUOT_SER FROM QUOTATION WHERE QUOT_NO = :b1"SELECT QUOT_SER FROM QUOTATION WHERE QUOT_NO = :b1 AND QUOT_SER = :b2 AND ROWNUM <= 1"SELECT 1 FROM QUOTATION WHERE QUOT_NO = :b1 AND QUOT_SER = :b2 AND INACTIVE = 1 AND ROWNUM <= 1"SELECT EXPORT FROM QUOTATION WHERE QUOT_NO = :b1 AND QUOT_SER = :b2 AND ROWNUM <= 1"SELECT QUOTATION_DETAIL.QUOT_NO,QUOTATION_DETAIL.I_CODE,IAS_ITM_DTL.ITM_UNT,DECODE(:b1,1,NVL(I_NAME,I_E_NAME),I_E_NAME) I_NAME,I_QTY,FREE_QTY,NVL(QUOTATION_DETAIL.P_SIZE,1) P_SIZE,W_CODE,P_QTY,I_PRICE_VAT,I_PRICE,DIS_AMT_MST,DIS_PER1,DIS_AMT_DTL1,DIS_PER2,DIS_AMT_DTL2,DIS_PER3,DIS_AMT_DTL3,DIS_AMT,RCRD_NO,QUOTATION_DETAIL.REC_ATTCH,I_LENGTH,I_WIDTH,I_HEIGHT,I_NUMBER,FIELD_DTL1,FIELD_DTL2,FIELD_DTL3,ITEM_DESC,EXPIRE_DATE,BATCH_NO,WT_UNT,WT_QTY,ARGMNT_NO FROM QUOTATION_DETAIL,IAS_ITM_MST,IAS_ITM_DTL WHERE IAS_ITM_MST.I_CODE = IAS_ITM_DTL.I_CODE AND QUOTATION_DETAIL.I_CODE = IAS_ITM_MST.I_CODE AND QUOTATION_DETAIL.ITM_UNT = IAS_ITM_DTL.ITM_UNT AND QUOTATION_DETAIL.QUOT_NO = :b2 AND QUOTATION_DETAIL.QUOT_SER = :b3 ORDER BY RCRD_NO"SELECT C_CODE,C_NAME,QUOT_CUR,DISC_AMT_MST,DISC_AMT_DTL,DISC_AMT,CC_CODE,PJ_NO,ACTV_NO,A_DESC,REF_NO,REF_DATE,FIELD1,FIELD2,FIELD3,FIELD4,FIELD5,FIELD6,FIELD7,FIELD8,FIELD9,FIELD10 FROM QUOTATION WHERE QUOT_NO = :b1 AND QUOT_SER = :b2"SELECT NVL(SUM(I_QTY),0),NVL(SUM(FREE_QTY),0) FROM SALES_ORDER A,ORDER_DETAIL B WHERE A.ORDER_SER = B.ORDER_SER AND A.QUOT_SER = :b1 AND B.I_CODE = :b2"SELECT OTHER_CHARGES_OTHRS.SC_NO,SALES_CHARGES.SC_NAME,OTHER_CHARGES_OTHRS.A_CODE,OTHER_CHARGES_OTHRS.PER,OTHER_CHARGES_OTHRS.AMT,NVL(SALES_CHARGES.INV_ITEM,0) INV_ITEM FROM OTHER_CHARGES_OTHRS,SALES_CHARGES WHERE SALES_CHARGES.SC_NO = OTHER_CHARGES_OTHRS.SC_NO AND OTHER_CHARGES_OTHRS.BILL_TYPE = 52 AND OTHER_CHARGES_OTHRS.BILL_NO = :b1 AND OTHER_CHARGES_OTHRS.BILL_SER = :b2"
```
```sql
SELECT NVL(NO_OF_DECIMAL,2),NVL(USE_DEL_GL,0),NVL(USE_MOD_GL,0),NVL(CONN_AC_CC,0),NVL(FOR_CURRENCY,0),NVL(HANDLY_LOCAL_AMT,0),NVL(J_STAND_BY,0),POST_CHEQUE_TYPE,DATE_SETTING_GL FROM IAS_PARA_GL,IAS_PARA_GEN"SELECT ACCOUNT_TYPE FROM ACCOUNT_TYPES WHERE AFFECTED_BY_TRANS = 1"
```
```sql
And Not Exists (Select I_Code From Ias_Itm_Wcode Where I_Code = A.I_Code And RowNum<=1)
```
```sql
And Exists (Select I_Code From Ias_Itm_Wcode Where I_Code = A.I_Code And RowNum<=1)
```
```sql
And A.I_Code = C.I_Code(+) And Not Exists (Select I_Code From Ias_Item_Price Where I_Code = A.I_Code And RowNum<=1)
```
```sql
And Exists (Select I_Code From Item_Bin Where B_Code =
```
```sql
And Exists (Select I_Code From Ias_Itm_Wcode Where I_Code = A.I_Code And Nvl(Avl_Qty,0) > 0 And RowNum<=1)
```
```sql
And Exists (Select I_Code From Ias_Itm_Wcode Where I_Code = A.I_Code And Nvl(Avl_Qty,0) = 0 And RowNum<=1)
```
```sql
And Exists( Select 1 From Ias_V_Vndr_Itm Where I_Code=A.I_Code And V_Code =
```
```sql
And Exists (Select 1 From Privilege_Gc Where U_id =
```
```sql
And Exists (Select 1
```
```sql
SELECT NVL(BATCH_DESC_A_NAME,BATCH_DESC_E_NAME) FROM IAS_BATCH_NO_CONTENTS WHERE COL_NO = 1 AND BATCH_DESC_NO = LPAD(SUBSTR(:b1,:b2,:b3),:b3,''0'')"SELECT NVL(BATCH_DESC_A_NAME,BATCH_DESC_E_NAME) FROM IAS_BATCH_NO_CONTENTS WHERE COL_NO = 2 AND BATCH_DESC_NO = LPAD(SUBSTR(:b1,:b2,:b3),:b3,''0'')"SELECT NVL(BATCH_DESC_A_NAME,BATCH_DESC_E_NAME) FROM IAS_BATCH_NO_CONTENTS WHERE COL_NO = 3 AND BATCH_DESC_NO = LPAD(SUBSTR(:b1,:b2,:b3),:b3,''0'')"SELECT NVL(BATCH_DESC_A_NAME,BATCH_DESC_E_NAME) FROM IAS_BATCH_NO_CONTENTS WHERE COL_NO = 4 AND BATCH_DESC_NO = LPAD(SUBSTR(:b1,:b2,:b3),:b3,''0'')"SELECT NVL(BATCH_DESC_A_NAME,BATCH_DESC_E_NAME) FROM IAS_BATCH_NO_CONTENTS WHERE COL_NO = 5 AND BATCH_DESC_NO = LPAD(SUBSTR(:b1,:b2,:b3),:b3,''0'')"
```
```sql
SELECT NVL(PLIMIT_TYPE,2),PLIMIT_PER,NVL(L_PRICE,2),NVL(H_PRICE,2) FROM PRIVILEGE_FIXED WHERE U_ID = :b1"
```
```sql
SELECT LEV_NO FROM IAS_ITEM_PRICE WHERE I_CODE = :b1 AND ROUND(IAS_GEN_PKG.GET_ITM_PRICE_DOCACY(:b2,:b3,:b4,:b5,:b6,:b7,:b8,LEV_NO,NVL(:b9,:b10),NVL(:b11,0),:b12,:b13),:b14) = :b15"
```
```sql
SELECT EXPIRE_DATE,BATCH_NO,CP_QTY FROM GR_DETAIL WHERE I_CODE = :b1 AND W_CODE = NVL(:b2,W_CODE) AND NVL(CP_QTY,0) > 0 AND (EXPIRE_DATE != ''01/01/1900'' OR BATCH_NO != ''0'' )ORDER BY REC_NO"SELECT EXPIRE_DATE,BATCH_NO,CP_QTY FROM GR_DETAIL WHERE I_CODE = :b1 AND W_CODE = NVL(:b2,W_CODE) AND NVL(CP_QTY,0) > 0 AND (EXPIRE_DATE != ''01/01/1900'' OR BATCH_NO != ''0'' )ORDER BY REC_NO"
```
```sql
UPDATE SALES_ORDER SET ORDER_AMT=(SELECT ROUND(NVL(SUM(I_QTY * I_PRICE ),0),:b1) FROM ORDER_DETAIL WHERE ORDER_SER = :b2 ),INSRNCE_CSH_AMT=(SELECT ROUND(NVL(SUM(I_QTY * (NVL(INSRNCE_LOAD_AMT,0) + NVL(INSRNCE_ADD_LMT,0) ) ),0),:b1) FROM ORDER_DETAIL WHERE ORDER_SER = :b2 ),DISC_AMT=ROUND((NVL(DISC_AMT_MST,0) + NVL(DISC_AMT_DTL,0) ),:b5) WHERE ORDER_SER = :b2"
```
```sql
Select 1 From Order_Detail
```
```sql
UPDATE ORDER_DETAIL SET DIS_AMT_MST=0,DIS_AMT=DIS_AMT_DTL WHERE ORDER_SER = :b1"
```
```sql
UPDATE ORDER_DETAIL SET VAT_AMT=ROUND(((NVL(VAT_PER,0) * NVL(I_PRICE,0) ) / 100 ),:b1) WHERE ORDER_SER = :b2 AND NVL(I_QTY,0) > 0"UPDATE ORDER_DETAIL SET I_PRICE=NVL(I_PRICE_VAT,0) - NVL(VAT_AMT,0) WHERE ORDER_SER = :b1 AND NVL(I_QTY,0) > 0"UPDATE ORDER_DETAIL SET VAT_AMT=ROUND((NVL(VAT_PER,0) * (NVL(I_PRICE,0) - (NVL(:b1,0) / NVL(:b2,0) ) * NVL(I_PRICE,0) ) ) / 100 ,:b3) WHERE ORDER_SER = :b4 AND NVL(I_QTY,0) > 0"UPDATE ORDER_DETAIL SET I_PRICE=NVL(I_PRICE_VAT,0) - NVL(VAT_AMT,0) WHERE ORDER_SER = :b1 AND NVL(I_QTY,0) > 0"UPDATE ORDER_DETAIL SET VAT_PER=0,VAT_AMT=0 WHERE ORDER_SER = :b1"UPDATE ORDER_DETAIL SET I_PRICE=NVL(I_PRICE_VAT,0) WHERE ORDER_SER = :b1 AND NVL(I_QTY,0) > 0"
```
```sql
Select 1 From Other_Charges_Othrs
```
```sql
Select 1 From Sales_Charges
```
```sql
UPDATE ORDER_DETAIL SET OTHR_AMT=0 WHERE ORDER_SER = :b1"UPDATE ORDER_DETAIL A SET OTHR_AMT=(SELECT SUM(DECODE(NVL(UNIT_AMT,0),0,(NVL(OTHER_CHARGES_ITEMS_OTHRS.AMT,0) / DECODE(NVL(A.I_QTY,0),0,DECODE(NVL(A.FREE_QTY,0),0,1,A.FREE_QTY),A.I_QTY) ),UNIT_AMT)) FROM OTHER_CHARGES_ITEMS_OTHRS WHERE OTHER_CHARGES_ITEMS_OTHRS.BILL_SER = A.ORDER_SER AND OTHER_CHARGES_ITEMS_OTHRS.BILL_SER = :b1 AND OTHER_CHARGES_ITEMS_OTHRS.I_CODE = A.I_CODE AND OTHER_CHARGES_ITEMS_OTHRS.RCRD_NO = A.RCRD_NO ) WHERE ORDER_SER = :b1"UPDATE ORDER_DETAIL A SET OTHR_AMT=(SELECT SUM(NVL(OTHER_CHARGES_ITEMS_OTHRS.AMT,0) / DECODE(NVL(A.I_QTY,0),0,DECODE(NVL(A.FREE_QTY,0),0,1,A.FREE_QTY),A.I_QTY) ) FROM OTHER_CHARGES_ITEMS_OTHRS WHERE OTHER_CHARGES_ITEMS_OTHRS.BILL_SER = A.ORDER_SER AND OTHER_CHARGES_ITEMS_OTHRS.BILL_SER = :b1 AND OTHER_CHARGES_ITEMS_OTHRS.I_CODE = A.I_CODE AND OTHER_CHARGES_ITEMS_OTHRS.RCRD_NO = A.RCRD_NO ) WHERE ORDER_SER = :b1"SELECT ORDER_AMT FROM SALES_ORDER WHERE ORDER_SER = :b1 AND ROWNUM <= 1"SELECT 1 FROM ORDER_DETAIL WHERE ORDER_SER = :b1 AND I_QTY > 0 AND ROWNUM <= 1"SELECT SUM(NVL(FREE_QTY,0) * NVL(I_PRICE,0) ) FROM ORDER_DETAIL WHERE ORDER_SER = :b1"UPDATE ORDER_DETAIL A SET OTHR_AMT=(SELECT NVL(A.OTHR_AMT,0) + (SUM(NVL(OTHER_CHARGES_OTHRS.AMT,0)) / NVL(:b1,0) ) * NVL(A.I_PRICE,0) FROM OTHER_CHARGES_OTHRS WHERE OTHER_CHARGES_OTHRS.BILL_SER = A.ORDER_SER AND NVL(OTHER_CHARGES_OTHRS.INV_ITEM,0) = 0 AND OTHER_CHARGES_OTHRS.BILL_SER = :b2 ) WHERE ORDER_SER = :b2 AND NVL(I_QTY,0) > 0"UPDATE ORDER_DETAIL A SET OTHR_AMT=(SELECT NVL(A.OTHR_AMT,0) + (SUM(NVL(OTHER_CHARGES_OTHRS.AMT,0)) / NVL(:b1,0) ) * NVL(A.I_PRICE,0) FROM OTHER_CHARGES_OTHRS WHERE OTHER_CHARGES_OTHRS.BILL_SER = A.ORDER_SER AND NVL(OTHER_CHARGES_OTHRS.INV_ITEM,0) = 0 AND OTHER_CHARGES_OTHRS.BILL_SER = :b2 ) WHERE ORDER_SER = :b2"
```
```sql
and Exists ( Select 1 from customer cst,priv_acc pac
```
```sql
and Exists ( Select 1 from IAS_PRIV_CUSTOMER pcst
```
```sql
Select Mv_Ias_Itm_Wcode.W_Code W_Code,
```
```sql
SELECT Pj_No,
```
```sql
(SELECT 1
```
```sql
Select customer.c_code,
```
```sql
Select 1
```
```sql
Select M.I_Code,
```
```sql
And (:Parameter.User_No=1 Or Exists (Select 1 From Privilege_Gc
```
```sql
SELECT ias_item_price.lev_no,
```
```sql
SELECT lev_no
```
```sql
SELECT QT_TYPE, QUOT_NO, QUOT_DATE, C_NAME, QUOT_SER, BRN_NO
```
```sql
SELECT S_TYPE
```
```sql
SELECT 1
```
```sql
SELECT Sales_Man.reprs_code rep_code,
```
```sql
Select M.I_Code, Decode(:Parameter.Lang_No,1,Nvl(M.I_Name,M.I_E_Name),Nvl(M.I_E_Name,M.I_Name)) I_Name,
```
```sql
exists (select 1
```
```sql
Select D.Itm_Unt,
```
```sql
Select B_Code ,Decode(:Parameter.Lang_No,1,nvl(B_A_Name,B_E_Name), Nvl(B_E_Name,B_A_Name)) B_Name
```
```sql
Select Manf_Code
```
```sql
SELECT 1 FROM IAS_INSRNCE_CLSS_MDCL_NET WHERE INS_CMP_NO = :b1 AND CLSS_NO = :b2 AND MDCL_NO = :b3 AND ROWNUM <= 1"SELECT 1 FROM IAS_BNFCRS WHERE BNF_NO = :b1 AND NVL(INACTIVE,0) = 1 AND ROWNUM <= 1"SELECT 1 FROM IAS_INSRNCE_CMP WHERE INS_CMP_NO = :b1 AND NVL(INACTIVE,0) = 1 AND ROWNUM <= 1"SELECT 1 FROM IAS_INSRNCE_CMP_CLSS WHERE INS_CMP_NO = :b1 AND BNF_NO = :b2 AND CLSS_NO = :b3 AND NVL(INACTIVE,0) = 1 AND ROWNUM <= 1"SELECT OUT_TYP FROM IAS_INSRNCE_CLSS_ITM_TYP A,IAS_ITM_MST B WHERE A.ITEM_TYPE = NVL(B.ITEM_TYPE,0) AND B.I_CODE = :b1 AND A.INS_CMP_NO = :b2 AND A.CLSS_NO = :b3 AND ROWNUM <= 1"SELECT DECODE(:b1,1,LOAD_PER,LOAD_PER_OUT_NET) FROM IAS_INSRNCE_CLSS_LMT_DOC WHERE INS_CMP_NO = :b2 AND CLSS_NO = :b3 AND RLTN_TYP = :b4 AND ROWNUM <= 1"SELECT DECODE(:b1,1,LOAD_PER,LOAD_PER_OUT_NET) FROM IAS_INSRNCE_CLSS_ITM_TYP A,IAS_ITM_MST B WHERE A.ITEM_TYPE = NVL(B.ITEM_TYPE,0) AND B.I_CODE = :b2 AND A.INS_CMP_NO = :b3 AND A.CLSS_NO = :b4 AND ROWNUM <= 1"SELECT DISTINCT A.ITEM_TYPE,A.LMT_AMT_LOC_NET_DOC,A.LMT_AMT_LOC_OUT_NET_DOC FROM IAS_INSRNCE_CLSS_ITM_TYP A,IAS_ITM_MST B,ORDER_DETAIL C WHERE A.ITEM_TYPE = NVL(B.ITEM_TYPE,0) AND B.I_CODE = C.I_CODE AND C.ORDER_SER = :b1 AND A.INS_CMP_NO = :b2 AND A.CLSS_NO = :b3 AND (A.LMT_AMT_LOC_NET_DOC IS NOT NULL OR A.LMT_AMT_LOC_OUT_NET_DOC IS NOT NULL )"SELECT SUM(NVL(D.I_QTY,0) * (NVL(D.I_PRICE,0) - NVL(D.INSRNCE_ADD_LMT,0) ) * NVL(M.CUR_RATE,1) ) FROM SALES_ORDER M,ORDER_DETAIL D,IAS_ITM_MST A WHERE M.ORDER_SER = D.ORDER_SER AND D.I_CODE = A.I_CODE AND NVL(A.ITEM_TYPE,0) = :b1 AND M.ORDER_SER = :b2 AND D.INSRNCE_APPRVD_CODE IS NULL"SELECT DECODE(:b1,1,NVL(IT_A_NAME,IT_E_NAME),NVL(IT_E_NAME,IT_A_NAME)) FROM ITEM_TYPES WHERE TYPE_OF_ITEM = :b2 AND ROWNUM <= 1"SELECT DECODE(:b1,1,NVL(IT_A_NAME,IT_E_NAME),NVL(IT_E_NAME,IT_A_NAME)) FROM ITEM_TYPES WHERE TYPE_OF_ITEM = :b2 AND ROWNUM <= 1"SELECT DECODE(:b1,1,LMT_AMT_NET_LOC,LMT_AMT_OUT_NET_LOC) FROM IAS_INSRNCE_CLSS_LMT_DOC WHERE INS_CMP_NO = :b2 AND CLSS_NO = :b3 AND RLTN_TYP = :b4 AND DECODE(:b1,1,LMT_AMT_NET_LOC,LMT_AMT_OUT_NET_LOC) IS NOT NULL AND ROWNUM <= 1"SELECT RLTN_TYP1,RLTN_TYP2,RLTN_TYP3,RLTN_TYP4,LMT_AMT_NET_LOC,LMT_AMT_NET_LOC_YR FROM IAS_INSRNCE_CLSS_LMT_MNTH A WHERE A.INS_CMP_NO = :b1 AND A.CLSS_NO = :b2 AND :b3 IN ( NVL(A.RLTN_TYP1,0),NVL(A.RLTN_TYP2,0),NVL(A.RLTN_TYP3,0),NVL(A.RLTN_TYP4,0) ) AND A.ITEM_TYPE IS NULL AND A.LMT_AMT_NET_LOC IS NOT NULL AND ROWNUM <= 1"SELECT SUM(NVL(D.I_QTY,0) * (NVL(D.I_PRICE,0) - NVL(D.INSRNCE_ADD_LMT,0) ) * NVL(M.BILL_RATE,1) ) FROM IAS_BILL_MST M,IAS_BILL_DTL D WHERE M.BILL_SER = D.BILL_SER AND M.INSRNCE_CMP_NO = :b1 AND M.INSRNCE_CLSS_NO = :b2 AND M.INSRNCE_CARD_NO_FMLY = :b3 AND TO_CHAR(M.BILL_DATE,''MM'') = TO_CHAR(:b4,''MM'') AND :b5 IN ( :b6,:b7,:b8,:b9 ) AND D.INSRNCE_APPRVD_CODE IS NULL"SELECT SUM(NVL(D.I_QTY,0) * (NVL(D.I_PRICE,0) - NVL(D.INSRNCE_ADD_LMT,0) ) * NVL(M.BILL_RATE,1) ) FROM IAS_BILL_MST M,IAS_BILL_DTL D WHERE M.BILL_SER = D.BILL_SER AND M.INSRNCE_CMP_NO = :b1 AND M.INSRNCE_CLSS_NO = :b2 AND M.INSRNCE_CARD_NO_FMLY = :b3 AND :b4 IN ( :b5,:b6,:b7,:b8 ) AND D.INSRNCE_APPRVD_CODE IS NULL"SELECT DISTINCT A.RLTN_TYP1,A.RLTN_TYP2,A.RLTN_TYP3,A.RLTN_TYP4,A.ITEM_TYPE,A.LMT_AMT_NET_LOC,A.LMT_AMT_NET_LOC_YR FROM IAS_INSRNCE_CLSS_LMT_MNTH A,IAS_ITM_MST B,ORDER_DETAIL C WHERE A.ITEM_TYPE = NVL(B.ITEM_TYPE,0) AND B.I_CODE = C.I_CODE AND :b1 IN ( NVL(A.RLTN_TYP1,0),NVL(A.RLTN_TYP2,0),NVL(A.RLTN_TYP3,0),NVL(A.RLTN_TYP4,0) ) AND A.ITEM_TYPE IS NOT NULL AND A.INS_CMP_NO = :b2 AND A.CLSS_NO = :b3 AND A.LMT_AMT_NET_LOC IS NOT NULL"SELECT SUM(NVL(D.I_QTY,0) * (NVL(D.I_PRICE,0) - NVL(D.INSRNCE_ADD_LMT,0) ) * NVL(M.BILL_RATE,1) ) FROM IAS_BILL_MST M,IAS_BILL_DTL D,IAS_ITM_MST A WHERE M.BILL_SER = D.BILL_SER AND D.I_CODE = A.I_CODE AND NVL(A.ITEM_TYPE,0) = :b1 AND TO_CHAR(M.BILL_DATE,''MM'') = TO_CHAR(:b2,''MM'') AND NVL(M.INSRNCE_CMP_NO,0) = :b3 AND NVL(M.INSRNCE_CLSS_NO,''0'') = :b4 AND NVL(M.INSRNCE_CARD_NO_FMLY,''0'') = :b5 AND NVL(INSRNCE_RLTN_TYP,0) IN ( NVL(:b6,0),NVL(:b7,0),NVL(:b8,0),NVL(:b9,0) ) AND D.INSRNCE_APPRVD_CODE IS NULL"SELECT DECODE(:b1,1,NVL(IT_A_NAME,IT_E_NAME),NVL(IT_E_NAME,IT_A_NAME)) FROM ITEM_TYPES WHERE TYPE_OF_ITEM = :b2 AND ROWNUM <= 1"SELECT SUM(NVL(D.I_QTY,0) * (NVL(D.I_PRICE,0) - NVL(D.INSRNCE_ADD_LMT,0) ) * NVL(M.BILL_RATE,1) ) FROM IAS_BILL_MST M,IAS_BILL_DTL D,IAS_ITM_MST A WHERE M.BILL_SER = D.BILL_SER AND D.I_CODE = A.I_CODE AND NVL(A.ITEM_TYPE,0) = :b1 AND NVL(M.INSRNCE_CMP_NO,0) = :b2 AND NVL(M.INSRNCE_CLSS_NO,''0'') = :b3 AND NVL(M.INSRNCE_CARD_NO_FMLY,''0'') = :b4 AND NVL(INSRNCE_RLTN_TYP,0) IN ( NVL(:b5,0),NVL(:b6,0),NVL(:b7,0),NVL(:b8,0) ) AND D.INSRNCE_APPRVD_CODE IS NULL"SELECT DECODE(:b1,1,NVL(IT_A_NAME,IT_E_NAME),NVL(IT_E_NAME,IT_A_NAME)) FROM ITEM_TYPES WHERE TYPE_OF_ITEM = :b2 AND ROWNUM <= 1"SELECT I_CODE,I_QTY_MNTH,BILL_QTY FROM (SELECT A.I_CODE,NVL(A.I_QTY_MNTH,0) I_QTY_MNTH,SUM(NVL(D.P_QTY,0) / A.P_SIZE ) BILL_QTY FROM IAS_BILL_MST M,IAS_BILL_DTL D,IAS_INSRNCE_CARD_CHRONIC A WHERE M.BILL_SER = D.BILL_SER AND M.INSRNCE_CMP_NO = A.INS_CMP_NO AND M.INSRNCE_CARD_NO = A.PRSON_CARD_NO AND D.I_CODE = A.I_CODE AND TO_CHAR(BILL_DATE,''MM'') = TO_CHAR(:b1,''MM'') AND D.I_CODE IN (SELECT I_CODE FROM ORDER_DETAIL WHERE ORDER_SER = :b2 ) AND D.INSRNCE_APPRVD_CODE IS NULL GROUP BY A.I_CODE,NVL(A.I_QTY_MNTH,0) HAVING SUM(NVL(D.I_QTY,0)) > NVL(A.I_QTY_MNTH,0) ) WHERE ROWNUM <= 1"
```
```sql
SELECT w_code,
```
```sql
SELECT cc_code,
```
```sql
AND c_s_m = (SELECT cc_type
```
```sql
SELECT Regions.R_code,
```
```sql
SELECT SO_TYPE,
```
```sql
DECODE (NVL ((SELECT 1
```
```sql
OR EXISTS (SELECT S_TYPE
```
```sql
ELSE (SELECT 1
```
```sql
OR EXISTS (SELECT 1
```
```sql
SELECT DISTINCT Ias_Itm_Wcode.W_Code,
```
```sql
And (:Parameter.User_No = 1 Or Exists (Select 1 From Privilege_Gc
```
```sql
select M.i_code,
```
```sql
And (:Parameter.User_No=1 Or exists (select 1
```
```sql
SELECT Sales_Order.ORDER_NO ORDER_NO,
```
```sql
Select Group_No,
```
```sql
Select v_details.v_code,
```
```sql
Select G_Code,
```
```sql
Exists (Select G_Code From Privilege_Gc Where add_Flag=1 And U_Id=:Parameter.User_No And G_Code = Group_Details.G_Code)
```
```sql
SELECT SUBG_CODE,
```
```sql
OR EXISTS (SELECT G_CODE
```
```sql
SELECT MNG_CODE,
```
```sql
Select Measure, Measure_Code From Measurement
```
```sql
Select Ias_Attach_Dtl.Attch_Desc_No Attch_Desc_No,
```
```sql
And Exists(Select 1 From Ias_Itm_Attach
```
```sql
Select C_Code Attch_Desc_No,
```
```sql
select Flex_No ,
```
```sql
select Flex_No,
```
```sql
SELECT Driver_No,
```
```sql
SELECT PRM_CODE,
```
```sql
SELECT Actv_no,
```
```sql
Select Bill_No, Bill_Date,A_DESC, I_Qty, I_Price,bill_currency
```
```sql
From (Select A.Bill_No,Bill_Date, a.A_DESC,I_Qty, I_Price,bill_currency
```
```sql
(EXISTS (SELECT 1
```
```sql
Select Brn_No,
```
```sql
SELECT
```
```sql
SELECT Decode(to_char(expire_date,'ddmmyyyy'),'01011900',to_date(''),expire_date) Expire_Date,
```
```sql
SELECT DECODE(batch_no,'0','', batch_no) batch_no,
```
```sql
SELECT BATCH_NO FROM (
```
```sql
SELECT DISTINCT BATCH_NO
```
```sql
UNION ALL
```
```sql
Select Msur_Qty_Code , Argmnt_No From Ias_Conn_Itm_Msur_Unt
```
```sql
Select Emp_No,Decode(:Parameter.Lang_No,1,Nvl(Emp_l_Nm,Emp_F_Nm),Nvl(Emp_F_Nm,Emp_L_Nm)) Emp_Name
```
```sql
Select sub_customer.sub_c_code,
```
```sql
SELECT A_CY FROM ACCOUNT_CURR WHERE
```
```sql
select sales_charges.sc_no,sc_name,sales_charges.a_code,sales_charges.a_cy,sales_charges.ac_rate ,amt_type
```
```sql
select sales_charges.a_code,nvl(a_name,a_name_eng) a_name ,
```
```sql
Select C.Ins_Cmp_No,
```
```sql
select BNF_No,Decode(:Parameter.Lang_No,1,Nvl(BNF_L_NM, BNF_F_NM),Nvl(BNF_F_NM, BNF_L_NM)) BNF_NM from IAS_BNFCRS
```
```sql
Where bnf_no in (select bnf_no from IAS_INSRNCE_CMP_CLSS where ins_cmp_no=:Sales_Order.insrnce_cmp_no)
```
```sql
select Mdcl_No,Decode(:Parameter.Lang_No,1,Nvl(Mdcl_L_Nm,Mdcl_F_Nm),Nvl(Mdcl_F_Nm,Mdcl_L_Nm)) MDCL_NM from IAS_MEDICAL_NET order by Mdcl_No
```
```sql
select Mdcl_No,Decode(:Parameter.Lang_No,1,Nvl(Mdcl_L_Nm,Mdcl_F_Nm),Nvl(Mdcl_F_Nm,Mdcl_L_Nm)) MDCL_NM
```
```sql
Where Exists(Select 1 From Ias_Insrnce_Clss_Mdcl_Net
```
```sql
Select Distinct INSRNCE_DIAGNOSIS
```
```sql
KEY-COMMIT
```
```sql
select Count(1) from Customer_Curr where c_code=
```
```sql
Select '','None' from dual
```
```sql
union Select cur_no||'- '||Customer_curr.a_cy ,Customer_curr.a_cy
```
```sql
Select 1 From Sales_Order
```
```sql
Select 1 From Customer Where Cc_Code Is Not Null And RowNum<=1 And C_Code=
```
```sql
Select '','None' from dual union Select Cur_No||'- '||cur_code,cur_code from ex_rate
```
```sql
SELECT R_CODE,DECODE(:b1,1,NVL(C_A_NAME,C_E_NAME),NVL(C_E_NAME,C_A_NAME)),BLK_LST,DRIVER_NO,C_CLASS,REP_CODE,NVL(INSRNCE_CST,0),INSRNCE_CMP_NO FROM CUSTOMER WHERE C_CODE = :b2"SELECT C_CLASS,R_CODE,DISC_PER,DECODE(:b1,1,NVL(C_A_NAME,C_E_NAME),NVL(C_E_NAME,C_A_NAME)),REP_CODE,NVL(INACTIVE,0),INACTIVE_RES,BLK_LST,CREDIT_PERIOD,DRIVER_NO,F_ACTV_DATE,T_ACTV_DATE FROM CUSTOMER WHERE C_CODE = :b2"SELECT R_CODE,C_CLASS,DISC_PER,DRIVER_NO,REP_CODE FROM CUSTOMER WHERE C_CODE = :b1"SELECT A_CY FROM CUSTOMER_CURR WHERE C_CODE = :b1"SELECT CC_CODE FROM CUSTOMER WHERE C_CODE = :b1"SELECT CREDIT_PERIOD FROM CUSTOMER WHERE C_CODE = :b1"SELECT DIS_PER FROM IAS_INSRNCE_CMP WHERE INS_CMP_NO = :b1 AND ROWNUM <= 1"
```
```sql
SELECT COUNT(1) FROM IAS_DRIVERS WHERE DRIVER_NO = :b1 AND NVL(INACTIVE,0) = 1"
```
```sql
SELECT COUNT(1) FROM IAS_PROMOTERS WHERE PRM_CODE = :b1 AND NVL(INACTIVE,0) = 1"
```
```sql
SELECT 1 FROM IAS_BNFCRS WHERE BNF_NO = :b1 AND NVL(INACTIVE,0) = 1 AND ROWNUM <= 1"
```
```sql
SELECT MAX(BILL_DATE) FROM IAS_BILL_MST WHERE C_CODE = :b1 AND INSRNCE_CMP_NO = :b2 AND INSRNCE_CARD_NO = :b3"
```
```sql
SELECT BNF_NO,PRSON_CARD_NO_FMLY,DECODE(:b1,1,NVL(PRSON_L_NM,PRSON_F_NM),NVL(PRSON_F_NM,PRSON_L_NM)),RLTN_TYP,LOAD_PER,LMT_AMT_LOC_NET_DOC,LMT_AMT_LOC_OUT_NET_DOC,NVL(INACTIVE,0),CLSS_NO,CARD_EXPIRE_DATE,CARD_ISSUE_DATE FROM IAS_INSRNCE_CARD WHERE INS_CMP_NO = :b2 AND BNF_NO = NVL(:b3,BNF_NO) AND PRSON_CARD_NO = :b4 AND ROWNUM <= 1"
```
```sql
SELECT 1 FROM IAS_MEDICAL_NET WHERE MDCL_NO = :b1 AND NVL(INACTIVE,0) = 1 AND ROWNUM <= 1"SELECT 1 FROM IAS_INSRNCE_CLSS_MDCL_NET WHERE INS_CMP_NO = :b1 AND CLSS_NO = :b2 AND MDCL_NO = :b3 AND NVL(INACTIVE,0) = 1 AND ROWNUM <= 1"
```
```sql
Create User
```
```sql
Create Date
```
```sql
Last Update User
```
```sql
Last Update Date
```
```sql
Update Count
```
```sql
SELECT NVL(INV_DIS_LMT,0) FROM PRIVILEGE_FIXED WHERE U_ID = :b1"
```
```sql
Update Inactive Fld ,
```
```sql
Commit
```
```sql
UPDATE SALES_ORDER SET INACTIVE=NVL(:b1,0),INACTIVE_RES=:b2,INACTIVE_DATE=IAS_GEN_PKG.GET_CURDATE,INACTIVE_U_ID=:b3 WHERE ORDER_SER = :b4"
```
```sql
Update Approved Fld ,
```
```sql
UPDATE SALES_ORDER SET APPROVED=NVL(:b1,0),APRV_U_ID=:b2,APRV_DATE=IAS_GEN_PKG.GET_CURDATE WHERE ORDER_SER = :b3"
```
```sql
SELECT NVL(MAX(DOC_NO),0) + 1 FROM IAS_RESERVE_MST WHERE DOC_TYPE_REF = 53"INSERT INTO IAS_RESERVE_MST ( DOC_TYPE,DOC_NO,DOC_SER,DOC_NO_REF,DOC_TYPE_REF,DOC_SER_REF,C_CODE,BRN_NO,CMP_NO,BRN_USR,BRN_YEAR ) VALUES ( 2,:b1,:b2,:b3,53,:b4,:b5,:b6,:b7,:b8,:b9 )"
```
```sql
DELETE FROM ORDER_DETAIL O WHERE O.ORDER_NO = :b1 AND O.ORDER_SER = :b2 AND O.SO_TYPE = :b3"DELETE FROM IAS_ITM_ATTACH_OTHRS I WHERE I.DOC_SER = :b1"DELETE FROM IAS_RESERVE_MST WHERE DOC_TYPE_REF = 53 AND DOC_SER_REF = :b1"DELETE FROM OTHER_CHARGES_OTHRS O WHERE O.BILL_NO = :b1 AND O.BILL_SER = :b2"
```
```sql
SELECT DOC_NO,DOC_SER FROM IAS_RESERVE_MST WHERE DOC_TYPE_REF = 53 AND DOC_SER_REF = :b1 AND ROWNUM <= 1"SELECT SUM(NVL(I_QTY,0) * NVL(I_PRICE,0) ) FROM ORDER_DETAIL WHERE ORDER_SER = :b1"SELECT C_CLASS,DISC_PER,NVL(INSRNCE_CST,0) FROM CUSTOMER WHERE C_CODE = :b1"SELECT DIS_PER FROM IAS_INSRNCE_CMP WHERE INS_CMP_NO = :b1 AND ROWNUM <= 1"
```
```sql
UPDATE ORDER_DETAIL SET EMP_NO=:b1 WHERE ORDER_SER = :b2"UPDATE IAS_RESERVE_MST SET C_CODE=:b1 WHERE DOC_SER_REF = :b2"UPDATE IAS_RESERVE_DTL SET C_CODE=:b1 WHERE DOC_SER_REF = :b2"
```
```sql
SELECT 1 FROM IAS_ITM_MST WHERE I_CODE = :b1 AND ROWNUM <= 1"
```
```sql
SELECT SUM(CP_QTY) FROM GR_DETAIL WHERE CP_QTY > 0 AND (((TO_DATE(EXPIRE_DATE,''dd/mm/yyyy'') - :b1 ) <= :b2 ) OR TO_DATE(EXPIRE_DATE,''dd/mm/yyyy'') < :b1 ) AND I_CODE = :b4"
```
```sql
SELECT FLEX_NO,EXPIRE_DATE,BATCH_NO FROM IAS_ITM_UNT_BARCODE WHERE BARCODE = :b1"SELECT 1 FROM IAS_ITM_MST WHERE I_CODE = :b1 AND ROWNUM <= 1"SELECT DECODE(:b1,1,NVL(I_NAME,I_E_NAME),NVL(I_E_NAME,I_NAME)),I_DESC,ITEM_TYPE,NVL(INACTIVE,0),ALTER_CODE,NVL(ALLOW_FREE_QTY,0),NVL(:b2,0),NVL(USE_QTY_FRACTION,0),DAY_ITM_EXPIRE,USE_EXP_DATE,NVL(USE_ATTCH,0),NVL(ITEM_SIZE,0),NVL(SERVICE_ITM,0),G_CODE,FREE_QTY_PER,NVL(ALLOW_DISC,0),NVL(USE_WEIGHT,0),NVL(VAT_PER,0),DECODE(:b3,1,NVL(VAT_TYPE,0),0),LENGTH_ITM,WIDTH_ITM,HEIGHT_ITM FROM IAS_ITM_MST WHERE I_CODE = :b4 AND ROWNUM <= 1"SELECT DECODE(:b1,1,NVL(I_NAME,I_E_NAME),NVL(I_E_NAME,I_NAME)),I_DESC,ITEM_TYPE,NVL(INACTIVE,0),ALTER_CODE,NVL(ALLOW_FREE_QTY,0),NVL(:b2,0),NVL(USE_QTY_FRACTION,0),DAY_ITM_EXPIRE,USE_EXP_DATE,NVL(USE_ATTCH,0),NVL(ITEM_SIZE,0),NVL(SERVICE_ITM,0),G_CODE,FREE_QTY_PER,NVL(ALLOW_DISC,0),NVL(USE_WEIGHT,0),NVL(VAT_PER,0),DECODE(:b3,1,NVL(VAT_TYPE,0),0),LENGTH_ITM,WIDTH_ITM,HEIGHT_ITM FROM IAS_ITM_MST WHERE I_CODE = :b4 AND ROWNUM <= 1"SELECT NVL(USE_EXP_DATE,0),NVL(USE_BATCH_NO,0) FROM IAS_ITM_MST WHERE I_CODE = :b1"SELECT 1 FROM IAS_ITM_WCODE WHERE I_CODE = :b1 AND W_CODE = :b2 AND ROWNUM <= 1"SELECT NVL(USE_EXP_DATE,0) FROM IAS_ITM_MST WHERE I_CODE = :b1"SELECT MIN(EXPIRE_DATE) FROM (SELECT EXPIRE_DATE FROM ITEM_MOVEMENT WHERE I_CODE = :b1 AND W_CODE = :b2 AND TO_CHAR(EXPIRE_DATE,''DD/MM/YYYY'') != ''01/01/1900'' GROUP BY EXPIRE_DATE HAVING SUM(IN_OUT * (NVL(P_QTY,0) + NVL(PF_QTY,0) ) ) > 0 )"SELECT MSUR_QTY_CODE FROM IAS_CONN_ITM_MSUR_UNT WHERE I_CODE = :b1"
```
```sql
SELECT A_CY FROM IAS_PRICING_LEVELS WHERE LEV_NO = :b1"SELECT NVL(CUR_RATE,1) FROM EX_RATE WHERE CUR_CODE = :b1"
```
```sql
SELECT GROUP_NO FROM IAS_ITM_MST WHERE I_CODE = :b1"
```
```sql
SELECT DECODE(:b1,1,NVL(SUB_C_L_NM,SUB_C_F_NM),NVL(SUB_C_F_NM,SUB_C_L_NM)) FROM SUB_CUSTOMER WHERE SUB_C_CODE = :b2"SELECT DECODE(:b1,1,NVL(SUB_C_L_NM,SUB_C_F_NM),NVL(SUB_C_F_NM,SUB_C_L_NM)) FROM SUB_CUSTOMER WHERE C_CODE = :b2 AND SUB_C_CODE = :b3 AND ROWNUM <= 1"SELECT BATCH_NO,BATCH_NO FROM IAS_SUB_CST_MEASUR_MST WHERE C_CODE = :b1 AND SUB_C_CODE = :b2 AND I_CODE = :b3"
```
```sql
SELECT SUM(NVL(I_QTY,0) + NVL(FREE_QTY,0) ) FROM IAS_BILL_DTL WHERE DOC_TYPE_REF = 53 AND DOC_SER_REF = :b1 AND I_CODE = :b2 AND NVL(RCRD_NO_REF,RCRD_NO) = :b3"
```
```sql
SELECT DECODE(:b1,1,NVL(I_NAME,I_E_NAME),NVL(I_E_NAME,I_NAME)),I_DESC,NVL(ALLOW_FREE_QTY,0),NVL(USE_QTY_FRACTION,0),DAY_ITM_EXPIRE,NVL(ITEM_SIZE,0),NVL(SERVICE_ITM,0),FREE_QTY_PER,NVL(USE_EXP_DATE,0),NVL(USE_BATCH_NO,0),NVL(USE_WEIGHT,0) FROM IAS_ITM_MST WHERE I_CODE = :b2 AND ROWNUM <= 1"SELECT DECODE(:b1,1,NVL(EMP_L_NM,EMP_F_NM),NVL(EMP_F_NM,EMP_L_NM)) FROM S_EMP WHERE EMP_NO = :b2 AND ROWNUM <= 1"SELECT DECODE(:b1,1,NVL(SUB_C_L_NM,SUB_C_F_NM),NVL(SUB_C_F_NM,SUB_C_L_NM)) FROM SUB_CUSTOMER WHERE SUB_C_CODE = :b2"SELECT DECODE(:b1,1,NVL(SUB_C_L_NM,SUB_C_F_NM),NVL(SUB_C_F_NM,SUB_C_L_NM)) FROM SUB_CUSTOMER WHERE C_CODE = :b2 AND SUB_C_CODE = :b3 AND ROWNUM <= 1"
```
```sql
SELECT NVL(VAT_PER,0) FROM IAS_ITM_MST WHERE I_CODE = :b1"
```
```sql
DELETE FROM IAS_RESERVE_DTL WHERE DOC_TYPE_REF = 53 AND DOC_SER_REF = :b1 AND I_CODE = :b2 AND RCRD_NO = :b3"DELETE FROM OTHER_CHARGES_ITEMS_OTHRS O WHERE O.BILL_NO = :b1 AND O.BILL_SER = :b2 AND O.I_CODE = :b3"
```
```sql
INSERT INTO IAS_RESERVE_DTL ( DOC_TYPE,DOC_NO,DOC_SER,DOC_NO_REF,DOC_TYPE_REF,DOC_SER_REF,I_CODE,ITM_UNT,W_CODE,EXPIRE_DATE,BATCH_NO,I_QTY,P_SIZE,P_QTY,CP_QTY,RESERVED,C_CODE,T_DATE,DOC_SEQUENCE,RCRD_NO,ITEM_DESC,AD_U_ID,AD_DATE,UP_U_ID,UP_DATE,BRN_NO,BRN_YEAR,CMP_NO,BRN_USR ) VALUES ( 2,:b1,:b2,:b3,53,:b4,:b5,:b6,NVL(:b7,:b8),NVL(:b9,''01/01/1900''),NVL(:b10,''0''),:b11,:b12,NVL(:b13,0) + (NVL(:b14,0) * NVL(:b15,1) ) ,NVL(:b13,0) + (NVL(:b14,0) * NVL(:b15,1) ) ,NVL(:b19,0),:b20,:b21, NULL ,:b22,:b23,:b24,:b25,:b26,:b27,:b28,:b29,:b30,:b31 )"
```
```sql
SELECT 1 FROM SALES_CHARGES WHERE SC_NO = :b1 AND NVL(FILL_AUTO,0) = 0"SELECT A_CODE,DECODE(AMT_TYPE,0,AMT, NULL ) * DECODE(SC_TYPE,1,1,-1) AMT_PER,NVL(AMT_TYPE,0),DECODE(NVL(FILL_AUTO,0),0,1,0) FROM SALES_CHARGES WHERE SC_NO = :b1 AND AMT IS NOT NULL"SELECT SC_NAME,DECODE(A_CY,:b1,AMT,AMT * AC_RATE * 1 / :b2 ) * DECODE(SC_TYPE,1,1,-1) ,NVL(INV_ITEM,0) FROM SALES_CHARGES WHERE SC_NO = :b3 AND AMT_TYPE = 1"
```
```sql
SELECT 1 FROM SALES_CHARGES WHERE A_CODE = :b1 AND ROWNUM <= 1"SELECT SC_NAME,DECODE(A_CY,:b1,AMT,AMT * AC_RATE * 1 / :b2 ) * DECODE(SC_TYPE,1,1,-1) FROM SALES_CHARGES WHERE SC_NO = :b3 AND AMT_TYPE = 1"
```
```sql
SELECT NVL(CALC_TYPE,0),SC_TYPE FROM SALES_CHARGES WHERE A_CODE = :b1 AND SC_NO = :b2"
```
```sql
SELECT COUNT(1) FROM SALES_CHARGES WHERE A_CODE = :b1 AND SC_NO = :b2 AND NVL(AMT_TYPE,0) = 1"
```
```sql
SELECT DECODE(A_CY,:b1,AMT,AMT * AC_RATE * 1 / :b2 ) FROM SALES_CHARGES WHERE SC_NO = :b3 AND AMT_TYPE = 1"
```
```sql
SELECT SC_NAME,DECODE(NVL(FILL_AUTO,0),0,1,0) FROM SALES_CHARGES WHERE SC_NO = :b1 AND A_CODE = :b2"SELECT 1 FROM SALES_CHARGES WHERE SC_NO = :b1 AND NVL(FILL_AUTO,0) = 1 AND ROWNUM <= 1"
```
```sql
SELECT 1 FROM ACCOUNT_CURR WHERE A_CODE = :b1 AND A_CY = :b2 AND ROWNUM <= 1"SELECT DECODE(A_CY,:b1,AMT,AMT * AC_RATE * 1 / :b2 ) FROM SALES_CHARGES WHERE SC_NO = :b3 AND AMT_TYPE = 1"
```
```sql
Alter Code
```
```sql
SELECT FLEX_FIELD,ATTCH_DESC_NO1,ATTCH_DESC_NO2,ATTCH_DESC_NO3,ATTCH_DESC_NO4,ATTCH_DESC_NO5,ATTCH_NO1,ATTCH_NO2,ATTCH_NO3,ATTCH_NO4,ATTCH_NO5 FROM IAS_ITM_ATTACH WHERE I_CODE = :b1 AND FLEX_NO = :b2 AND ROWNUM <= 1"
```
```sql
SELECT IAS_ATTCH_SEQ.NEXTVAL FROM DUAL"
```
```sql
SELECT DECODE(:b1,1,NVL(C_A_NAME,C_E_NAME),NVL(C_E_NAME,C_A_NAME)) FROM CUSTOMER WHERE C_CODE = :b2"
```
```sql
Select Excel.exe File
```
```sql
SELECT PASSWORD FROM USER_R WHERE U_ID = :b1 AND ROWNUM <= 1"
```

## Other extracted strings (first 180)

- `ARST003`
- `YSERP_MNU.MMX`
- `AMERICAN_AMERICA.AR8MSWIN1256`
- `P6_20_NOV_201408_34_23`
- `SQLFORMS`
- `"PKG INIT"<anonymous>""`
- `P5_20_NOV_201408_34_23`
- `P2_09_SEP_201416_23_58`
- `P0_19_MAR_201416_21_03`
- `/LYSERP_LIB`
- `GEN_PKG`
- `P30_16_JAN_201412_58_41`
- `/NSPC11/PRE_FORM_PRC`
- `P29_16_JAN_201412_58_41`
- `"/NSPC11/WHEN_NEW_FORM_INSTANCE_PRC`
- `P28_16_JAN_201412_58_41`
- `/NSPC11/POST_FORMS_COMMIT_PRC`
- `P27_16_JAN_201412_58_41`
- `P26_16_JAN_201412_58_41`
- `P25_16_JAN_201412_58_41`
- `P24_16_JAN_201412_58_41`
- `P23_16_JAN_201412_58_41`
- `P22_16_JAN_201412_58_41`
- `P21_16_JAN_201412_58_41`
- `/NSPC11/EXIT_PROC`
- `P20_16_JAN_201412_58_41`
- `P19_16_JAN_201412_58_41`
- `STANDARD`
- `FORMS40`
- `VIEW_BTN`
- `SCREEN_BTN`
- `PRINT_DOC_BTN`
- `CANCEL_BTN`
- `PRINT_ST`
- `P18_16_JAN_201412_58_41`
- `Create -`
- `P17_16_JAN_201412_58_41`
- `FORMS4C`
- `/NSPC11/ADD_PROC`
- `BAR.ADD_Btn`
- `TRUE`
- `Delete -`
- `P16_16_JAN_201412_58_41`
- `/NSPC11/DELETE_PROC`
- `BAR.Del_Btn`
- `Accept -`
- `P15_16_JAN_201412_58_41`
- `/NSPC11/SAVE_PROC`
- `BAR.SAVE_Btn`
- `List -`
- `P14_16_JAN_201412_58_41`
- `/NSPC11/LIST_PROC`
- `BAR.LIST_BTN`
- `Search_`
- `P13_16_JAN_201412_58_41`
- `/NSPC11/KEY_ENTQRY`
- `BAR.SeeK_Btn`
- `Exceute -`
- `P12_16_JAN_201412_58_41`
- `/NSPC11/KEY_EXEQRY`
- `BAR.SEEK_BTN`
- `Print -`
- `P11_16_JAN_201412_58_41`
- `/NSPC11/CALL_SCR_PRINT`
- `BAR.Print_Btn`
- `P10_16_JAN_201412_58_41`
- `/NSPC11/WHEN_TIMER_EXPIRED_PRC`
- `P9_16_JAN_201412_58_41`
- `P8_16_JAN_201412_58_41`
- `P5_16_JAN_201412_58_41`
- `P8_02_FEB_201414_58_17`
- `IMP_F_EXCEL`
- `Bar.Save_Btn`
- `Sales_Order.Order_No`
- `Sales_Order.Order_Date`
- `Sales_Order.Order_Cur`
- `Sales_Order.w_code`
- `Imp_Excel.W_Code`
- `P9_02_FEB_201416_28_08`
- `/NSPC11/CLEAR_ALL_MASTER_DETAILS`
- `KEY_LISTVAL_PRC`
- `LOV_PKG`
- `"PKG INIT"KEY_LISTVAL_PRC"P_ITM"V_LOV_NM"V_RTRN_ITM1"V_RTRN_ITM2"V_WHR"V_GO_ITM""`
- `CALL_SCR_PRINT`
- `FORMS4W`
- `"PKG INIT"CALL_SCR_PRINT"V_AUDIT_REF"V_STAND_BY""`
- `Prnt.rep_smple`
- `Prnt.screen_btn`
- `Prnt.view_btn`
- `Prnt.print_doc_btn`
- `CHECK_DUPLICATE`
- `IAS_GEN_PKG`
- `IAS20142`
- `/NSPC11/GET_ORDER_NO`
- `/NSPC11/CHECK_DOC_DEL_PRC`
- `"PKG INIT"CHECK_DUPLICATE"CNT""`
- `Select 1 from Sales_Order`
- `where Order_No=`
- `And Brn_No =`
- `And RowNum<=1`
- `IAS_GEN_PKGIAS20142GET_CNTIA`
- `And So_Type =`
- `ENA_DIS_ITM_PRC`
- `"PKG INIT"ENA_DIS_ITM_PRC"P_TYP""`
- `WHEN_TIMER_EXPIRED_PRC`
- `"PKG INIT"WHEN_TIMER_EXPIRED_PRC""`
- `VA_ITM_MOVE`
- `VA_SUM`
- `MS Sans Serif`
- `VA_EXP_DATE`
- `VA_BTCH_NO`
- `VA_WEIGH`
- `VA_FREE_QTY`
- `VA_DISC`
- `VA_VAT`
- `ADD_PROC`
- `/NSPC11/IAS_WT_PKG`
- `YS_GEN_PKG`
- `/NSPC11/MAKEARDUP`
- `"PKG INIT"ADD_PROC"V_DATE"V_A_CY"V_W_CODE"V_CC_CODE"V_PJ_NO"V_ACTV_NO"V_BILL_DOC_TYPE"I""`
- `Dflt_Options`
- `ORDER_DETAIL`
- `other_charges_othrs.sc_no`
- `other_charges_othrs.A_Code`
- `other_charges_othrs.amt`
- `other_charges_othrs.per`
- `other_charges_othrs.clear`
- `IMP_EXCEL.FEXCEL`
- `Order_Detail.FIELD_DTL`
- `Order_Detail.FREE_QTY`
- `SALES_ORDER`
- `TRANSFER_PRODUCT`
- `YS_GEN_PKGIAS20142GET_USR_FLD_PRVYS_`
- `Sales_Order.TRANSFER_PRODUCT`
- `SHOW_INACTIVE`
- `Sales_Order.SHOW_INACTIVE`
- `IAS_GEN_PKGIAS20142GET_SYSDATEIA`
- `Sales_Order.Order_NO`
- `Sales_Order.Order_DATE`
- `Sales_Order.So_Type`
- `Sales_Order.W_code`
- `Sales_Order.W_Code`
- `Sales_Order.Cc_code`
- `Sales_Order.Cc_Code`
- `Sales_Order.pj_no`
- `Sales_Order.Bill_Doc_Type`
- `Order_Detail.Measur_Price`
- `Order_Detail.I_price_Vat`
- `Order_Detail.I_price`
- `INSRNCE_DATA`
- `Sales_Order.Brn_No`
- `ADD_PROC2`
- `"PKG INIT"ADD_PROC2"CCODE"ACODE"CCUR"WCODE"OQTY"FQTY"ORDERSER"SHALT"V_INSRNCE_CARD_NO"V_INSRNCE_LOAD_PER"V_INSRNCE_MDCL_NO"I"<cursor ptr>"<SQL statement. Line 109>"C"I_CODE"I_QTY"P_QTY"P_SIZE"FREE_QTY"I_PRICE"I_PRICE_VAT"W_CODE"I_LENGTH"I_WIDTH"I_HEIGHT"I_NUMBER"DIS_PER"DIS_AMT_DTL"DIS_PER2"DIS_AMT_DTL2"DIS_PER3"DIS_AMT_DTL3"INSRNCE_LOAD_PER"INSRNCE_LOAD_AMT"INSRNCE_ADD_LMT"<SQL statement. Line 252>""`
- `ADD_F_DOC`
- `Sales_Order.ORDER_NO`
- `Sales_Order.ORDER_DATE`
- `Sales_Order`
- `Order_Detail`
- `Order_Detail.I_Qty`
- `Sales_Order.Bill_Due_Date`
- `Sales_Order.Cheque_Due_Date`
- `<$T$`
- `X$p$`
- `SELECT SO_TYPE,ORDER_CUR,C_CODE,C_NAME,A_DESC,CC_CODE,REF_DOC_NO,REF_DOC_DATE,DISC_AMT,DELIVERY_DATE,REP_CODE,PREPARE_DATE,REC_DEALER_DATE,W_CODE,BILL_DOC_TYPE,INSRNCE_CMP_NO,INSRNCE_BNF_NO,INSRNCE_PRSON_NM,INSRNCE_CARD_NO,INSRNCE_CARD_NO_FMLY,INSRNCE_RLTN_TYP,INSRNCE_CLSS_NO,INSRNCE_LOAD_PER,INSRNCE_DIAGNOSIS,INSRNCE_MDCL_NO,INSRNCE_CSH_AMT FROM SALES_ORDER WHERE ORDER_SER = :b1 AND ROWNUM <= 1"SELECT I_CODE,NVL(I_QTY,0) I_QTY,NVL(P_QTY,0) P_QTY,NVL(P_SIZE,0) P_SIZE,NVL(FREE_QTY,0) FREE_QTY,I_PRICE,I_PRICE_VAT,W_CODE,I_LENGTH,I_WIDTH,I_HEIGHT,I_NUMBER,DIS_PER,DIS_AMT_DTL,DIS_PER2,DIS_AMT_DTL2,DIS_PER3,DIS_AMT_DTL3,INSRNCE_LOAD_PER,INSRNCE_LOAD_AMT,INSRNCE_ADD_LMT FROM ORDER_DETAIL WHERE ORDER_SER = :b1 ORDER BY RCRD_NO FOR UPDATE NOWAIT"SELECT DISC_AMT FROM SALES_ORDER WHERE ORDER_SER = :b1"`
- `CALL_BTN_SCR`
- `CALL_BAR_BTN_SCR`
- `IAS_USR_PKG`
- `IAS_SMS_MAIL_PKG`
- `"PKG INIT"CALL_BTN_SCR"P_SCR_TYP"V_APPROVED_PRV"V_VRFY_PRV"V_INACTIVE_PRV"V_APPRV"V_CNT""`
- `CALL_OBJ.DUMMY`
- `FALSE`
- `IAS_USR_PKGIAS20142CHK_USR_FRM_PRVIA`
- `Select 1 From Ias_Chk_Doc_Inout_Qty_Mst`
- `Where Doc_Type_Ref=53`
- `And Doc_Ser_Ref =`
- `IAS_SMS_MAIL_PKGIAS20142GET_PRMTRIAS`
- `IAS_SMS_MAIL_PKGIAS20142INSRT_SO_INSRNC_ALRT`
- `IAS_SMS_MAIL_PKGIAS20142INSRT_SO_ALRTIAS`
- `Err. Insrt Msg sms,email ,`
- `Parameter.Vrfy_Flag`