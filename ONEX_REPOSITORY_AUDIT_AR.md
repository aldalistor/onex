# تقرير فحص مستودع `aldalistor/onex`

**تاريخ الفحص:** 13 سبتمبر 2026  
**النطاق:** بنية المستودع، سلامة الأرشيفات، الكتالوجات، سكربتات التحقق، ونواة Oracle المحاسبية.

## النتيجة التنفيذية

المستودع ناجح بوصفه **مشروع Reverse Engineering ومواصفات إعادة بناء** لنظام Oracle Forms قديم. يحتوي على كتالوجات واسعة ومواصفات مولدة وربط للنوافذ، لكنه لا يمثل بعدُ تطبيقًا إنتاجيًا مكتملًا ولا توجد في النسخة الحالية اختبارات تنفيذية تعمل ضد قاعدة Oracle فعلية.

## ما تم التحقق منه

| الفحص | النتيجة |
|---|---|
| استنساخ المستودع من GitHub | نجح، الفرع `main` نظيف قبل الفحص |
| عدد النوافذ في الكتالوج | 1,490 |
| سجلات التتبع | 6,077 |
| مواصفات الوحدات الحرجة | 18 |
| التحقق المدمج `validate_project.py` | نجح |
| إعادة بناء `Forms.zip` | نجحت، SHA-256 مطابق |
| إعادة بناء `Lib.zip` | نجحت، SHA-256 مطابق |
| إعادة بناء `مجلد١.zip` | نجحت، SHA-256 مطابق |
| فحص صياغة سكربت Bash | نجح |

## نقاط القوة

يقدم المستودع مصدرًا مفيدًا لحفظ معرفة النظام القديم، بما في ذلك كتالوج النوافذ، تتبع الدوال والمراجع، عقود التشغيل، مواصفات النوافذ، وملفات PL/SQL المولدة. كما أن README يميز بوضوح بين الكود المستخرج فعليًا والكود المولد أو الذي يحتاج مراجعة يدوية، وهذه نقطة مهمة لتقليل مخاطر الاعتماد على استنتاجات غير مؤكدة.

تقسيم الأرشيفات مع ملفات SHA-256 يعمل بشكل صحيح. كما أن وجود `validate_project.py` يضمن اتساق أعداد المواصفات والملفات المولدة مع عدد النوافذ المسجلة.

## ملاحظات حرجة

### 1. مسار العكس متعارض مع Trigger عدم قابلية التعديل — حرج

في `01_onex_accounting_core.sql` يمنع Trigger باسم `ONEX_AR_DOC_IMMUTABLE` أي تحديث أو حذف عندما تكون الحالة القديمة `POSTED` أو `REVERSED`. لكن `ONEX_AR_WINDOW_API.reverse_invoice` يحاول تنفيذ:

```sql
UPDATE ONEX_AR_DOC SET STATUS='REVERSED' WHERE DOC_ID=p_doc_id;
```

وبما أن المستند يجب أن يكون `POSTED` قبل عكسه، فسيرفض الـ Trigger العملية قبل إتمامها. يجب تصميم العكس كعملية مسموحة صراحةً، أو استخدام إجراء داخلي محكوم لا يسمح إلا بالانتقال `POSTED → REVERSED` مع إنشاء قيد عكسي كامل.

### 2. العكس لا ينشئ قيد GL عكسي — حرج محاسبيًا

الإجراء الحالي يغير حالة المستند ويسجل Audit وOutbox فقط. لا ينشئ Journal جديدًا بعكس القيد الأصلي، ولا يربط القيد العكسي بالمستند الأصلي، ولا ينشئ أثرًا للاستحقاقات أو المخزون. هذا يجعل الأرصدة المحاسبية غير صحيحة حتى بعد حل مشكلة الـ Trigger.

### 3. الترحيل يعتمد على تفاصيل الفاتورة دون ضمان اكتمالها — عالٍ

`post_invoice` ينشئ سطر مدين لحساب العميل ثم أسطر دائنة من `ONEX_AR_DOC_LINE`. لا توجد في الإجراء خطوة تحقق صريحة من أن مجموع تفاصيل الفاتورة يساوي الإجمالي، ولا معالجة واضحة للضريبة أو الخصم أو تكلفة المخزون. إذا كانت الفاتورة بلا تفاصيل فسيفشل التوازن، لكن الفشل لا يشرح نقص البيانات ولا يضمن اكتمال دورة AR/Inventory.

### 4. لا توجد حركة مخزون أو استحقاقات ضمن ترحيل الفاتورة — عالٍ

رغم أن النموذج يحتوي على `ONEX_ITEM` و`ONEX_AR_DUE`، فإن `create_invoice` لا ينشئ تفاصيل فاتورة أو استحقاقات، و`post_invoice` لا ينقص المخزون ولا ينشئ أو يحدث due schedule. لذلك فالنواة الحالية هي بداية لطبقة AR/GL وليست دورة فاتورة مكتملة.

### 5. ضعف التحقق من اتساق الشركة والفرع والفترة والعميل — عالٍ

توجد مفاتيح أجنبية على المعرفات، لكنها لا تمنع مثلًا ربط فرع تابع لشركة بفاتورة تخص شركة أخرى، أو ربط عميل من شركة مختلفة، أو الترحيل في فترة مغلقة. يلزم إضافة تحقق عابر للكيانات داخل API أو قيود تصميم مناسبة، مع منع الترحيل خارج فترة `OPEN`.

### 6. مرجع حساب الإيراد في سطر الفاتورة غير مقيد بمفتاح أجنبي — متوسط/عالٍ

الحقل `ONEX_AR_DOC_LINE.REVENUE_ACCOUNT_CODE` نصي فقط، ولا يوجد Foreign Key مركب إلى `ONEX_GL_ACCOUNT(COMPANY_ID, ACCOUNT_CODE)`. يمكن بالتالي إدخال حساب غير موجود أو غير تابع للشركة، ثم يفشل الترحيل أو ينتج بيانات غير موثوقة.

### 7. اختبار المشروع حاليًا اختبار بنيوي فقط — متوسط

`validate_project.py` يتحقق من أحجام الملفات وعددها، لكنه لا يشغل Oracle ولا يختبر المعاملات الذرية أو العكس أو التوازن أو idempotency. ملف `atomic_window_test_plan.md` خطة اختبار جيدة، لكنه ليس Suite تنفيذية.

### 8. الملفات المولدة كثيرة ويجب إبقاء حالتها واضحة — متوسط

يوجد عدد كبير من ملفات PL/SQL الموسومة `SCAFFOLD_ONLY` و`GENERATED`. يجب منع نشرها أو تنفيذها على قاعدة فعلية قبل مراجعة المصدر الأصلي، وتثبيت حالة كل ملف في Manifest قابل للتحقق مع مالك ومراجعة وتاريخ اعتماد.

## الأولويات المقترحة

1. إصلاح نموذج العكس أولًا: السماح بالانتقال المحكوم، إنشاء قيد GL عكسي، وربط المستندين.
2. إضافة اختبارات Oracle فعلية لسيناريوهات النجاح والفشل والـRollback وIdempotency.
3. إكمال دورة `create_invoice`: تفاصيل الفاتورة، الاستحقاقات، والتحقق من المجاميع.
4. إضافة تحقق الشركة/الفرع/الفترة/العميل والحسابات قبل أي DML مؤثر.
5. إضافة حركة المخزون أو فصلها بوضوح كخدمة ذرية ضمن نفس حدود المعاملة.
6. إنشاء CI يشغل الفحوصات البنيوية، ويتحقق من JSONL وCSV وSHA-256، ويمنع الملفات السرية.
7. البدء بنافذة `ARST004` بقاعدة اختبار Oracle ومقارنة النتائج مع النظام القديم قبل تعميم النمط.

## الحكم النهائي

المستودع **قوي كخريطة تقنية ومواصفات إعادة بناء**، ونجح فحص الاتساق وإعادة بناء الأرشيفات. لكنه حاليًا **ليس نظامًا محاسبيًا إنتاجيًا**؛ توجد فجوة واضحة بين الـScaffold/Reference Core وبين دورة الأعمال المكتملة، وأهم مانع فوري هو تعارض عملية العكس مع Trigger الحماية وعدم إنشاء قيد عكسي.
