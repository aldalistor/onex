# موديول الذمم والتحصيل

أضيفت دورة أولية متكاملة للذمم المدينة والدائنة للعملاء والموردين. يدعم النظام إنشاء دفعة قبض للعميل أو دفعة سداد للمورد، تخصيص الدفعة على فواتير متعددة، احتساب المبلغ غير المخصص، ثم ترحيل الدفعة وتحديث `paid_amount` و`outstanding_amount` على الفواتير داخل معاملة Oracle.

## الوظائف

- إنشاء دفعة مسودة.
- التحقق من نوع الجهة ونوع الفاتورة.
- منع تخصيص مبلغ أكبر من المتبقي.
- منع تكرار الفاتورة في نفس الدفعة.
- ترحيل الدفعة وتحديث الفاتورة.
- قراءة الدفعات حسب الفترة.
- تقرير أعمار الذمم للعملاء أو الموردين.
- تصنيفات: حالي، 1–30، 31–60، 61–90، وأكثر من 90 يومًا.
- حدود ائتمانية وأيام استحقاق على جهة الاتصال.
- تسجيل عمليات الإنشاء والترحيل في سجل التدقيق.

## قنوات IPC

```text
receivables:payments:list
receivables:aging
receivables:payment:create
receivables:payment:post
```

تتطلب القراءة صلاحية `VIEW_REPORTS`، بينما الإنشاء والترحيل يتطلبان `POST_JOURNALS`.

## الترحيل

أضيف `oracle/migrations/015_receivables_payables.sql` إلى `db-setup.js`، ويجب تشغيل الترحيلات حتى الإصدار `015` قبل استخدام الموديول على Oracle.

## حدود النسخة الحالية

تخصيص الدفعات يعمل على الفواتير التجارية المرحّلة أو المفتوحة، بينما إنشاء القيد النقدي التفصيلي للدفعة وربطه بسند قبض أو صرف مستقل يمكن توسيعه في المرحلة التالية مع سياسة اعتماد الدفعات. لا يُنصح بتفعيل الترحيل على الإنتاج قبل اختبار Oracle فعليًا والتحقق من الحسابات النقدية والقيود.
