# إدارة الفترات المالية والإقفال – Migration 016

أضيفت إدارة الفترات المالية في Oracle حتى الإصدار `016`. يدعم المخطط حالات الفترة `OPEN` و`SOFT_CLOSED` و`CLOSED` و`REOPENED`، مع تسجيل عمليات الفحص والإقفال وإعادة الفتح.

## الجداول

- `ONYX_FISCAL_PERIOD`: نطاق الفترة وحالتها وربطها بالشركة والفرع والسنة.
- `ONYX_PERIOD_CLOSE_RUN`: تشغيل فحص أو إقفال الفترة ونتيجته.
- `ONYX_PERIOD_CLOSE_CHECK`: نتيجة كل فحص مع العدد ورسالة الخطأ.
- `ONYX_PERIOD_REOPEN_LOG`: سبب إعادة الفتح والمستخدم المعني.

## الفحوص الحالية

- القيود المرحّلة غير المتوازنة.
- الفواتير المسودة.
- الأرصدة المخزنية السالبة.
- دفعات الذمم غير المرحّلة.
- عدم تطابق إجمالي الفاتورة مع المدفوع والمتبقي.

الأخطاء الحرجة تمنع الإقفال، بينما التحذيرات تسجل ولا تمنع الإقفال تلقائيًا.

## قنوات IPC

```text
periods:list
periods:close:precheck
periods:close:execute
periods:reopen
periods:close-history
```

تتطلب عملية الإغلاق `CLOSE_PERIOD`، وإعادة الفتح `REOPEN_PERIOD`، وعرض السجل `VIEW_AUDIT_LOG`.

## ملاحظة تكامل

تم تنفيذ إدارة الفترة وقواعد التحقق المركزية وقنواتها. الخطوة التالية هي استدعاء `canPostToPeriod` داخل كل مسار ترحيل فعلي، مثل الفاتورة والقيد والدفعة وحركة المخزون، بحيث يُرفض الترحيل آليًا عندما يكون تاريخ المستند داخل فترة مغلقة.
