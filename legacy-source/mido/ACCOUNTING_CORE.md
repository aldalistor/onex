# نواة Mido المحاسبية

أصبحت عمليات القيود في Mido تمر عبر طبقة محاسبية مركزية قبل الوصول إلى قاعدة البيانات. الهدف هو منع القيود غير المتوازنة أو الأسطر غير الصالحة، وحفظ مصدر العملية، وفصل مرحلة المسودة عن مرحلة الترحيل.

## قواعد النواة

تتحقق `accounting-core.js` من وجود بيان للقيد وسطرين على الأقل، وتمنع القيم السالبة، وتمنع جمع المدين والدائن في السطر نفسه، وترفض السطر الصفري، ثم تتحقق من تساوي إجمالي المدين والدائن بهامش دقة صغير. كل قيد يحمل مصدرًا مثل `MANUAL` أو `YEAR_CLOSE` أو `OPENING_BALANCE`.

## الترحيل

تستخدم دالة `postJournal` مستودعًا ذا معاملتين `begin/commit/rollback`. لا يعتبر القيد مرحلًا إلا بعد نجاح الإدراج والاعتماد، وعند وقوع خطأ يتم تنفيذ التراجع. طبقة Oracle تستخدم جدول القيود الحديث، بينما يضيف الترحيل رقم القيد ومصدره وتاريخ الترحيل والمستخدم المنفذ.

## قاعدة البيانات

الترحيل `oracle/migrations/008_accounting_posting_core.sql` يضيف العملات وأسعار الصرف، بيانات العملة وسعر الصرف لكل فاتورة وقيد، قيود الإقفال السنوي، سجل التدقيق، ومرئيّة ميزان المراجعة. كما يضيف حارسًا يمنع ترحيل قيد فارغ أو غير متوازن.

ينبغي تنفيذ الترحيلات بالترتيب من `001` إلى `008` في بيئة تطوير أولًا، ثم اختبارها على نسخة احتياطية قبل الإنتاج. يجب الاحتفاظ ببيانات الاتصال وكلمات المرور خارج المستودع.

## الإقفال السنوي

تُنشئ النواة قيدًا منظمًا لإقفال الإيرادات والمصروفات في ملخص الدخل، ثم ترحيل صافي الربح أو الخسارة إلى الأرباح المحتجزة. ولا تُخلط الأرصدة الافتتاحية بحسابات الإيرادات والمصروفات؛ بل تُبنى من حسابات الأصول والالتزامات وحقوق الملكية بعد اعتماد الإقفال.

## حدود المرحلة الحالية

ما زال ربط الفواتير بالقيود التشغيلية الكاملة، وحركات المخزون، وسير الموافقات، والتدقيق التفصيلي، يحتاج إلى طبقات لاحقة فوق هذه النواة. لذلك لا تستخدم الوضع التجريبي لتسجيل عمليات مالية حقيقية، ولا تعتبر الشاشة وحدها بديلًا عن اختبار Oracle في بيئة إنتاج.
