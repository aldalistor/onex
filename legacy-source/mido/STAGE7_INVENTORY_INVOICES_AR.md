# المرحلة السابعة: المخزون وفواتير البيع والشراء

تم تعزيز مسار فواتير البيع والشراء في `db.js` بحيث يحدد المستودع بواسطة `warehouseCode` بدل اختيار أول مستودع بشكل صامت. عند إنشاء الفاتورة في Oracle تُحفظ حركة المخزون وتُحدّث كمية الصنف ورصيد `ONYX_ITEM_BALANCE` داخل المعاملة نفسها، مع منع الرصيد السالب والتراجع عند فشل أي خطوة.

أضيفت قراءات Oracle للمستودعات، وحركات المخزون، وفواتير البيع والشراء. ترتبط هذه القراءات بقنوات Electron IPC وتحترم صلاحيات `VIEW_INVENTORY` و`VIEW_REPORTS`. شاشة المخزون تعرض كميات الأصناف من Oracle، وشاشتا المبيعات والمشتريات تعرضان فواتير Oracle بدل البيانات التجريبية عند الاتصال.

يحتوي نموذج الفاتورة على رمز المستودع، ويُمرر إلى مسار إنشاء الفاتورة. يجب أن يكون الرمز موجودًا ونشطًا في فرع السياق الحالي، وإلا تُرفض العملية قبل إنشاء الفاتورة.

## الاختبار المطلوب على Oracle

بعد تشغيل migrations حتى الإصدار `014`، يجب إنشاء مستودع نشط وربطه بالفرع، ثم اختبار فاتورة شراء تزيد الرصيد، وفاتورة بيع تنقصه، ورفض فاتورة بيع تتجاوز الرصيد، والتحقق من تطابق `ONYX_ITEM.QUANTITY` و`ONYX_ITEM_BALANCE.QUANTITY` مع سجل `ONYX_STOCK_MOVEMENT`.

الاختبارات المحلية الحالية تنجح عبر `npm test`. لا يُعد ذلك بديلًا عن اختبار تكاملي على Oracle فعلية لأن بيئة التطوير الحالية لا تحتوي على اتصال قاعدة بيانات إنتاجية.
