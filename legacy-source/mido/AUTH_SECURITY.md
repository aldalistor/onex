# المصادقة والصلاحيات

تستخدم النسخة الحديثة جداول `ONYX_USER` و`ONYX_ROLE` و`ONYX_PERMISSION` و`ONYX_USER_ROLE` و`ONYX_ROLE_PERMISSION` و`ONYX_LOGIN_HISTORY` داخل `XEPDB1`. كلمات المرور لا تُخزن كنص صريح؛ طبقة Node تنشئ قيمة `pbkdf2$iterations$salt$hash` باستخدام SHA-256 و210000 دورة، وتتحقق منها بمقارنة ثابتة زمنياً.

بعد خمس محاولات فاشلة يُقفل الحساب 15 دقيقة، وتُسجل كل محاولة في `ONYX_LOGIN_HISTORY`. جلسة Electron تحفظ في الذاكرة الرئيسية فقط، ولا تُرسل كلمة المرور إلى renderer بعد المصادقة. معالجات الكتابة تتحقق من الصلاحية المطلوبة داخل عملية Electron قبل استدعاء Oracle.

في أول تشغيل لا توجد مستخدمات، فتظهر شاشة تهيئة لإنشاء المدير الأول. ينشأ المستخدم ثم يُمنح دور `ADMIN` تلقائياً. بعد ذلك لا يمكن إنشاء مستخدم أو تعيين دور إلا لمستخدم يملك `MANAGE_USERS` أو `MANAGE_ROLES`. الأدوار الافتراضية هي `ADMIN` و`ACCOUNTANT` و`VIEWER`، والصلاحيات تغطي لوحة التحكم، الحسابات، الجهات، المخزون، الفواتير، القيود، التقارير، وإدارة الأمان.

بيانات اعتماد Oracle تبقى خارج Git في متغيرات البيئة: `ONYX_DB_USER` و`ONYX_DB_PASSWORD` و`ONYX_DB_CONNECT_STRING`.
