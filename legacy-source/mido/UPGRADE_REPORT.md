# تقرير تحديث النسخة الحالية

## التحديثات المنفذة

تم تحديث Electron إلى `44.2.0` و`electron-builder` إلى `26.15.3`، وإضافة `oracledb 6.10.0` لاستخدام اتصال Oracle الحديث. أضيف `preload.js` كجسر IPC محدود، وأصبح `main.js` يستخدم `contextIsolation` و`sandbox` و`nodeIntegration: false` مع إغلاق تجمع الاتصالات عند إنهاء التطبيق.

أضيفت طبقة `db.js` للاستعلام الآمن من Oracle، مع اختبارات اتصال للوحة التحكم ودليل الحسابات والعملاء والقيود. كما أضيف مؤشر حالة اتصال Oracle إلى الواجهة، وتحميل حي للبيانات عند فتح وحدات الحسابات والعملاء والقيود عند توفر متغيرات البيئة.

## الاختبارات

نجح اتصال طبقة Node بـ `ONYX_LEGACY_STAGE` على `127.0.0.1:1521/XEPDB1`، وأعاد 254 حساباً و129 عميلاً و104 قيود. نجح فحص الصياغة لجميع ملفات JavaScript، ونجح تغليف نسخة Linux التجريبية باستخدام Electron 44.2.0.

## المتطلبات المتبقية للتشغيل النهائي

يجب توفير `ONYX_DB_USER` و`ONYX_DB_PASSWORD` و`ONYX_DB_CONNECT_STRING` خارج المستودع عند تشغيل التطبيق. لا يُحفظ أي سر في الكود أو Git. ما زالت عمليات الكتابة المحاسبية الكاملة، شاشة الدخول، الصلاحيات الدقيقة، دفتر التدقيق، التقارير، وطبقة تحويل البيانات من الجداول المرحلية إلى نموذج التطبيق بحاجة إلى تنفيذ واختبار مستقل قبل اعتماد النظام إنتاجياً.
